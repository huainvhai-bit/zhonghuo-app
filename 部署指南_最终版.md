# 🚀 终活 App 后端部署指南

**修复完成时间**：2026-03-17 19:00  
**GitHub 提交**：`646d3a4` - 🔧 修复所有 API 对接问题

---

## ✅ 修复完成内容

### 后端修复（已推送到 GitHub）

**提交**：`646d3a4`  
**仓库**：https://github.com/huainvhai-bit/zhonghuo-backend-php.git

**修复内容**：
1. ✅ **api/users.php** - 恢复为完整版本（7KB，260 行）
   - 真实数据库连接
   - JWT Token 生成
   - 密码哈希
   - 注册返回完整 user 对象（包含 id 字段）

2. ✅ **admin/users.php** - 修复表名查询
   - `check_in_records` → `check_ins`
   - `will_templates` → `wills`

3. ✅ **config.php** - 数据库配置已正确
   - 数据库名：`zhonghuo_db`
   - 用户名：`zhonghuo_user`
   - 密码：`Zhonghuo@2026`

---

## 📦 部署步骤（只需一次）

### 方式 1：全新部署（推荐）

```bash
# === 1. SSH 登录服务器 ===
ssh root@8.136.41.211

# === 2. 进入网站根目录 ===
cd /www/wwwroot/zhonghuo.cn

# === 3. 删除旧后端目录 ===
rm -rf backend

# === 4. 重新克隆 ===
git clone https://github.com/huainvhai-bit/zhonghuo-backend-php.git backend
cd backend

# === 5. 设置权限 ===
chown -R www:www .
chmod 644 api/users.php
chmod 644 config.php
chmod 644 admin/users.php

# === 6. 验证文件 ===
ls -lh api/users.php
# 应该显示约 7KB

# === 7. 测试 API ===
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138009","name":"测试 9","password":"test123"}'

# 应该返回包含 "user" 对象的 JSON
```

### 方式 2：更新现有部署

```bash
# === 1. SSH 登录服务器 ===
ssh root@8.136.41.211

# === 2. 进入后端目录 ===
cd /www/wwwroot/zhonghuo.cn/backend

# === 3. 拉取最新代码 ===
git pull origin main

# === 4. 设置权限 ===
chown -R www:www .
chmod 644 api/users.php

# === 5. 验证 ===
ls -lh api/users.php
# 应该显示约 7KB
```

---

## 🧪 验证部署

### 测试 1：注册 API

```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "phone": "13800138009",
    "name": "测试用户 9",
    "password": "test123"
  }'
```

**✅ 正确响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试用户 9",
      "phone": "13800138009"
    }
  }
}
```

**注意**：必须包含 `"user": { "id": "...", ... }` 字段！

### 测试 2：登录 API

```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138006",
    "login_type": "password",
    "password": "test123456"
  }'
```

**✅ 正确响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "user_id": "user_test001",
    "user": {
      "id": "user_test001",
      "name": "测试用户",
      "phone": "13800138006",
      "check_in_interval": 48
    }
  }
}
```

### 测试 3：查看数据库

```bash
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db \
  -e "SELECT id, name, phone, created_at FROM users LIMIT 5;"
```

**✅ 正确结果**：
```
+---------------+----------+-------------+---------------------+
| id            | name     | phone       | created_at          |
+---------------+----------+-------------+---------------------+
| user_test001  | 测试用户  | 13800138006 | 2026-03-17 18:00:00 |
| user_xxx      | 测试用户 9 | 13800138009 | 2026-03-17 19:00:00 |
+---------------+----------+-------------+---------------------+
```

### 测试 4：访问 admin 后台

浏览器访问：http://8.136.41.211:3395/admin/

**✅ 正确结果**：
- 可以打开登录页面
- 登录后可以看到用户列表
- 显示签到统计、最后登录时间等信息

---

## 📋 部署检查清单

部署完成后请确认：

- [ ] `api/users.php` 文件大小约 7KB（不是 1.8KB）
- [ ] 注册 API 返回包含 `user.id` 字段
- [ ] 登录 API 返回包含 `user.id` 字段
- [ ] 数据库中可以看到新用户
- [ ] admin 后台可以访问
- [ ] admin 后台显示用户列表
- [ ] 前端 App 注册成功
- [ ] 前端 App 登录成功

---

## 🎯 关键文件

### 后端文件结构
```
backend/
├── api/
│   ├── users.php           # ✅ 7KB，完整版
│   ├── capsules.php        # ✅ 时光胶囊 API
│   ├── checkin.php         # ✅ 签到 API
│   ├── will.php            # ✅ 遗嘱 API
│   └── sms.php             # ✅ 短信 API
├── admin/
│   └── users.php           # ✅ 已修复表名
├── config.php              # ✅ 数据库配置
└── install/
    └── schema.sql          # ✅ 数据库结构
```

### 前端文件结构
```
frontend/
├── AuthView.swift          # ✅ 登录注册界面
├── DataManager.swift       # ✅ 数据管理
├── Models.swift            # ✅ 数据模型
└── ...
```

---

## 🔧 故障排查

### 问题 1：注册仍然返回测试数据

**检查**：
```bash
ls -lh /www/wwwroot/zhonghuo.cn/backend/api/users.php
# 如果显示 1-2KB，说明还是测试版
```

**解决**：
```bash
cd /www/wwwroot/zhonghuo.cn/backend
git pull origin main
```

### 问题 2：数据库连接失败

**检查**：
```bash
mysql -u zhonghuo_user -p'Zhonghuo@2026' -e "SELECT 1"
```

**解决**：
```bash
# 如果数据库不存在
mysql -u root -p < /www/wwwroot/zhonghuo.cn/backend/install/schema.sql
```

### 问题 3：admin 后台显示空白

**检查错误日志**：
```bash
tail -50 /www/wwwlogs/zhonghuo.cn.error.log
```

**修复表名**：
```bash
cd /www/wwwroot/zhonghuo.cn/backend
sed -i 's/check_in_records/check_ins/g' admin/users.php
sed -i 's/will_templates/wills/g' admin/users.php
```

---

## 📞 需要帮助？

如果部署过程中遇到问题，请提供：

1. **部署步骤**：你执行了哪些命令
2. **错误信息**：完整的错误输出
3. **API 响应**：curl 测试的完整响应
4. **日志文件**：`tail -50 /www/wwwlogs/zhonghuo.cn.error.log`

---

## 🎉 部署完成！

部署并验证通过后，前端 App 应该可以：
- ✅ 正常注册新用户
- ✅ 正常登录
- ✅ 显示用户信息
- ✅ 使用所有功能

**后端仓库**：https://github.com/huainvhai-bit/zhonghuo-backend-php  
**前端仓库**：https://github.com/huainvhai-bit/zhonghuo-app

---

*最后更新：2026-03-17 19:00*
