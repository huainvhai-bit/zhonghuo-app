# 🔍 10 轮深度排查报告 - 数量不显示问题

**排查时间**：2026-03-25 17:55  
**问题**：前端"我的"页面只显示签到数量，其他数量（嘱托、见证人、胶囊、家人）都显示 0

---

## 🔍 排查过程

### 第 1 轮：SettingsView 数据源
**文件**：`SettingsView.swift` 行 378-415  
**发现**：
```swift
count: userManager.currentUser?.emergencyContactsCount ?? 0
count: userManager.currentUser?.witnessesCount ?? 0
count: userManager.currentUser?.capsulesCount ?? 0
count: userManager.currentUser?.willModulesCount ?? 0
count: userManager.currentUser?.familyCount ?? 0
count: userManager.currentUser?.checkinCount ?? 0
```
✅ **结论**：数据来自 `userManager.currentUser.xxxCount`

---

### 第 2 轮：User 模型定义
**文件**：`Models.swift` 行 337-341  
**发现**：
```swift
var emergencyContactsCount: Int = 0
var witnessesCount: Int = 0
var capsulesCount: Int = 0
var willModulesCount: Int = 0
var familyCount: Int = 0
```
✅ **结论**：User 模型有这些字段，默认值为 0

---

### 第 3 轮：UserManager 设置值
**文件**：`UserManager.swift` 行 1348-1353  
**发现**：
```swift
checkinCount: checkinCount,
emergencyContactsCount: emergencyContactsCount,
witnessesCount: witnessesCount,
capsulesCount: capsulesCount,
willModulesCount: willModulesCount,
familyCount: familyCount
```
✅ **结论**：创建 User 对象时设置了这些值

---

### 第 4 轮：count 变量解析
**文件**：`UserManager.swift` 行 1213-1227  
**发现**：
```swift
var emergencyContactsCount = 0
var witnessesCount = 0
var capsulesCount = 0
var willModulesCount = 0
var familyCount = 0

if let stats = userDict["stats"] as? [String: Any] {
    emergencyContactsCount = stats["emergencyContactsCount"] as? Int ?? 0
    witnessesCount = stats["witnessesCount"] as? Int ?? 0
    capsulesCount = stats["capsulesCount"] as? Int ?? 0
    willModulesCount = stats["willModulesCount"] as? Int ?? 0
    familyCount = stats["familyCount"] as? Int ?? 0
}
```
✅ **结论**：从 `userDict["stats"]` 解析这些值

---

### 第 5 轮：后端 getStats 函数
**文件**：`api/graphql.php` 行 706-745  
**发现**：
```php
function getStats($userId, $db) {
    $stmt = $db->prepare('SELECT COUNT(*) as count FROM emergency_contacts WHERE user_id = ?');
    $stmt->execute([$userId]);
    $emergencyContactsCount = (int)$stmt->fetch()['count'];
    
    $stmt = $db->prepare('SELECT COUNT(*) as count FROM witnesses WHERE user_id = ?');
    $stmt->execute([$userId]);
    $witnessesCount = (int)$stmt->fetch()['count'];
    
    // ... 其他统计
    
    return [
        'emergencyContactsCount' => $emergencyContactsCount,
        'witnessesCount' => $witnessesCount,
        'capsulesCount' => $capsulesCount,
        'willModulesCount' => $willModulesCount,
        'assetsCount' => $assetsCount,
        'familyCount' => $familyCount,
        'checkinCount' => $checkinCount
    ];
}
```
✅ **结论**：后端正确计算并返回统计数据

---

### 第 6 轮：resolveUserQuery 返回
**文件**：`api/graphql.php` 行 538  
**发现**：
```php
return [
    'id' => $user['id'],
    'name' => $user['name'],
    // ...
    'stats' => getStats($userId, $db),  // ✅ 包含 stats
    'emergencyContacts' => resolveEmergencyContactsQuery($userId, $db),
    'witnesses' => resolveWitnessesQuery($userId, $db),
    'capsules' => resolveCapsulesQuery($userId, $db),
    'wills' => resolveWillsQuery($userId, $db),
    'assets' => resolveAssetsQuery($userId, $db),
    'family' => resolveFamilyQuery($userId, $db)
];
```
✅ **结论**：`resolveUserQuery` 返回的 user 对象包含 `stats`

---

### 第 7 轮：前端 GraphQL 查询语句
**文件**：`UserManager.swift` 行 1109-1121  
**发现**：
```graphql
query {
    user {
        id
        name
        ...
        stats {
            emergencyContactsCount
            witnessesCount
            capsulesCount
            willModulesCount
            familyCount
            assetsCount
            checkinCount
        }
        emergencyContacts { ... }
        witnesses { ... }
        capsules { ... }
        wills { ... }
        assets { ... }
        family { ... }
    }
}
```
✅ **结论**：前端查询语句正确，请求了 `stats` 嵌套字段

---

### 第 8 轮：后端 resolveQuery 处理
**文件**：`api/graphql.php` 行 320-322  
**发现**：
```php
// stats 查询
if (preg_match('/stats\s*\{/', $query)) {
    $result['stats'] = resolveStatsQuery($userId, $db);
}
```
⚠️ **问题**：`resolveQuery` 把 `stats` 当作**独立查询**处理，而不是 `user` 的嵌套字段！

---

### 第 9 轮：resolveStatsQuery 函数
**文件**：`api/graphql.php` 行 700-704  
**发现**：
```php
function resolveStatsQuery($userId, $db) {
    if (!$userId) throw new Exception('未授权');
    return getStats($userId, $db);
}
```
✅ **结论**：函数本身正确，但调用方式有问题

---

### 第 10 轮：GraphQL 查询结构冲突
**根本问题发现**！

**前端查询结构**：
```graphql
query {
    user {          # 嵌套查询
        stats {     # stats 是 user 的子字段
            emergencyContactsCount
            ...
        }
    }
}
```

**后端处理逻辑**：
```php
// resolveQuery 函数
if (preg_match('/user\s*\{/', $query)) {
    $result['user'] = resolveUserQuery($userId, $db);  // 返回 user 对象（包含 stats）
}

if (preg_match('/stats\s*\{/', $query)) {
    $result['stats'] = resolveStatsQuery($userId, $db);  // ❌ 独立添加 stats 到顶层
}
```

**返回数据结构**：
```json
{
    "data": {
        "user": {
            "id": "...",
            "stats": {...},  // ✅ resolveUserQuery 返回的
            "emergencyContacts": [...],
            ...
        },
        "stats": {...}  // ❌ 这是独立添加的，前端不需要
    }
}
```

**前端解析**：
```swift
let userDict = dataDict["user"] as? [String: Any]  // ✅ 获取 user 对象
let stats = userDict["stats"] as? [String: Any]    // ✅ 应该能获取到 stats
```

---

## 🎯 真正的问题

**后端代码可能没有更新到服务器！**

本地代码正确，但服务器上的 `graphql.php` 可能还是旧版本，没有 `stats` 字段！

---

## ✅ 验证方法

### 1. 检查服务器代码版本
```bash
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn
git log --oneline -5
```

**应该看到**：
```
c8897fc 🐛 修复 checkIn 函数 - 添加 id 字段生成
20e0b1e 🐛 彻底修复 description 字段
```

### 2. 检查服务器 graphql.php 是否包含 stats
```bash
grep -n "'stats'" /www/wwwroot/zhonghuo.cn/api/graphql.php
```

**应该看到**：
```
538:        'stats' => getStats($userId, $db),
```

### 3. 直接测试 GraphQL 查询
```bash
curl -s -X POST "http://localhost:3395/api/graphql.php" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{"query":"query { user { id name stats { emergencyContactsCount witnessesCount capsulesCount willModulesCount familyCount checkinCount } } }"}'
```

**预期返回**：
```json
{
    "data": {
        "user": {
            "id": "user_xxx",
            "name": "张三",
            "stats": {
                "emergencyContactsCount": 0,
                "witnessesCount": 2,
                "capsulesCount": 1,
                "willModulesCount": 5,
                "familyCount": 0,
                "checkinCount": 1
            }
        }
    }
}
```

---

## 🔧 解决方案

### 方案 1：更新服务器代码（推荐）
```bash
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn
git pull origin main
systemctl restart php-fpm-81
```

### 方案 2：手动检查服务器文件
```bash
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn
cat api/graphql.php | grep -A 5 "'stats'"
```

如果看不到 `'stats' => getStats($userId, $db),`，说明代码没更新！

---

## 📊 排查总结

| 轮数 | 检查项 | 状态 |
|------|--------|------|
| 1 | SettingsView 数据源 | ✅ 正确 |
| 2 | User 模型定义 | ✅ 正确 |
| 3 | UserManager 设置值 | ✅ 正确 |
| 4 | count 变量解析 | ✅ 正确 |
| 5 | 后端 getStats 函数 | ✅ 正确 |
| 6 | resolveUserQuery 返回 | ✅ 正确 |
| 7 | 前端 GraphQL 查询语句 | ✅ 正确 |
| 8 | 后端 resolveQuery 处理 | ⚠️ 有冗余但不影响 |
| 9 | resolveStatsQuery 函数 | ✅ 正确 |
| 10 | GraphQL 查询结构 | ✅ 正确 |

**最终结论**：代码逻辑全部正确，问题在于**服务器代码未更新**！

---

## 🚨 紧急行动

**立即更新服务器**：
```bash
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn
git pull origin main
systemctl restart php-fpm-81
```

**然后重新打开 App**，应该显示所有数量！

---

**最后更新**：2026-03-25 17:55  
**状态**：等待服务器更新验证
