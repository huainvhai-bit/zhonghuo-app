# 🔧 功能丢失修复方案

**时间**：2026-03-18 02:30  
**优先级**：🔴 紧急

---

## 📋 问题总结

用户报告 2026-03-17 已修复的功能又失效了：

1. ❌ 签到间隔无法修改
2. ❌ 签到倒计时没有自动签到
3. ❌ 后端倒计时没有实时同步
4. ❌ 最后登录时间和 IP 没有获取
5. ❌ 胶囊和嘱托没有实时上传

---

## 🔍 根本原因

**服务器代码未更新** - 本地代码已修复，但服务器上还是旧版本

### 本地代码状态 ✅
- SettingsView.swift - 有 `updateCheckInInterval()` 函数
- UserManager.swift - 有 `recordCheckIn(isAuto:)` 函数
- admin.php - 有 `update_checkin_interval` API
- checkin.php - 有 `is_auto` 支持

### 服务器代码状态 ❌
- 可能还是旧版本
- 需要 Git pull + 重启 PHP-FPM

---

## 🛠️ 立即修复步骤

### 步骤 1：在服务器上执行

```bash
# 1. SSH 登录
ssh root@8.136.41.211

# 2. 进入应用目录
cd /www/wwwroot/zhonghuo.cn

# 3. 拉取最新后端代码
git pull origin main

# 4. 检查关键文件
ls -la api/admin.php api/checkin.php api/users.php

# 5. 验证 API 内容
grep -n "update_checkin_interval" api/admin.php
grep -n "is_auto" api/checkin.php
grep -n "last_login_ip" api/users.php

# 6. 设置权限
chmod 755 api/*.php
chown -R www:www .

# 7. 重启 PHP-FPM
/etc/init.d/php-81-fpm restart

# 8. 测试 API
curl http://8.136.41.211:3395/api/check-config.php
```

### 步骤 2：检查数据库字段

```bash
# 登录 MySQL
mysql -uzhonghuo -pzhonghuo zhonghuo

# 检查 users 表结构
DESC users;

# 应该看到以下字段：
# - last_checkin_at
# - check_in_streak
# - check_in_interval
# - last_login_at
# - last_login_ip
# - location_lat
# - location_lng
# - address

# 如果缺少字段，执行：
ALTER TABLE users 
ADD COLUMN IF NOT EXISTS last_login_at DATETIME NULL,
ADD COLUMN IF NOT EXISTS last_login_ip VARCHAR(45) NULL,
ADD COLUMN IF NOT EXISTS check_in_interval VARCHAR(10) DEFAULT '48h',
ADD COLUMN IF NOT EXISTS location_lat DECIMAL(10, 8) NULL,
ADD COLUMN IF NOT EXISTS location_lng DECIMAL(11, 8) NULL,
ADD COLUMN IF NOT EXISTS address VARCHAR(255) NULL;

# 退出
EXIT;
```

### 步骤 3：前端重新编译

```bash
cd /Users/lishimin/Documents/zhonghuo-app

# 清理缓存
rm -rf ~/Library/Developer/Xcode/DerivedData/终活-*

# 重新编译
xcodebuild -scheme 终活 \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' \
  clean build

# 安装到模拟器
xcrun simctl uninstall "iPhone 17 Pro" com.zhonghuo.app
xcrun simctl install "iPhone 17 Pro" \
  ~/Library/Developer/Xcode/DerivedData/终活-*/Build/Products/Debug-iphonesimulator/终活.app
```

### 步骤 4：功能测试

#### 1. 测试签到间隔修改

```
1. 打开 App → 设置
2. 修改签到间隔（如 48 小时 → 24 小时）
3. 检查控制台日志：
   - 🔵 开始更新签到间隔：24h
   - ✅ 服务器同步成功
4. 刷新页面，确认间隔已更新
```

#### 2. 测试自动签到

```
1. 等待倒计时结束
2. 检查控制台日志：
   - ⏰ 检查自动签到
   - ✅ 执行自动签到
   - 🔄 同步到服务器：checkin.php
3. 检查服务器数据库：
   SELECT * FROM check_ins ORDER BY id DESC LIMIT 5;
```

#### 3. 测试最后登录信息

```
1. 重新登录
2. 检查数据库：
   SELECT id, phone, last_login_at, last_login_ip FROM users LIMIT 5;
3. 应该看到最后登录时间和 IP
```

#### 4. 测试胶囊/遗嘱同步

```
1. 添加新的时光胶囊
2. 检查控制台：
   - 🔄 同步胶囊到服务器
3. 检查数据库：
   SELECT * FROM capsules ORDER BY id DESC LIMIT 5;
```

---

## 📊 数据库表结构

### users 表（完整字段）

```sql
CREATE TABLE users (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    
    -- 签到相关
    last_checkin_at DATETIME NULL,
    check_in_streak INT DEFAULT 0,
    check_in_interval VARCHAR(10) DEFAULT '48h',
    
    -- 登录相关
    last_login_at DATETIME NULL,
    last_login_ip VARCHAR(45) NULL,
    
    -- 定位相关
    location_lat DECIMAL(10, 8) NULL,
    location_lng DECIMAL(11, 8) NULL,
    address VARCHAR(255) NULL,
    
    -- 时间戳
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_phone (phone),
    INDEX idx_last_checkin (last_checkin_at)
);
```

### check_ins 表

```sql
CREATE TABLE check_ins (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id VARCHAR(64) NOT NULL,
    check_in_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    location_lat DECIMAL(10, 8) NULL,
    location_lng DECIMAL(11, 8) NULL,
    address VARCHAR(255) NULL,
    is_auto TINYINT(1) DEFAULT 0,
    
    FOREIGN KEY (user_id) REFERENCES users(id),
    INDEX idx_user_checkin (user_id, check_in_at)
);
```

---

## 🎯 验证清单

- [ ] 服务器 Git pull 成功
- [ ] PHP-FPM 已重启
- [ ] 数据库字段完整
- [ ] 签到间隔可修改
- [ ] 自动签到正常工作
- [ ] 最后登录时间/IP 记录
- [ ] 胶囊实时上传
- [ ] 遗嘱实时上传
- [ ] 前端编译成功
- [ ] App 测试通过

---

## 📝 快速修复命令（复制粘贴执行）

```bash
# ========== 服务器端 ==========
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn
git pull origin main
chmod 755 api/*.php
chown -R www:www .
/etc/init.d/php-81-fpm restart
curl http://8.136.41.211:3395/api/check-config.php

# 检查数据库
mysql -uzhonghuo -pzhonghuo zhonghuo -e "DESC users;"

# ========== 本地 Mac ==========
cd /Users/lishimin/Documents/zhonghuo-app
rm -rf ~/Library/Developer/Xcode/DerivedData/终活-*
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
xcrun simctl uninstall "iPhone 17 Pro" com.zhonghuo.app
xcrun simctl install "iPhone 17 Pro" ~/Library/Developer/Xcode/DerivedData/终活-*/Build/Products/Debug-iphonesimulator/终活.app
```

---

**修复完成后请逐项验证功能！** ✅
