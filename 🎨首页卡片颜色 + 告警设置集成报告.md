# 🎨 首页卡片颜色 + 告警设置集成报告

**修改时间**: 2026-03-22 01:25  
**需求**:
1. 签到间隔时间由用户在 App 上自行设定，不需要后台设置
2. 后台告警功能保存没反应，检查并集成到系统配置内
3. 首页卡片颜色根据倒计时状态变化（绿/橙/红）

**状态**: ✅ 已完成并推送

---

## ✅ 任务 1: 移除后台签到间隔设置

### 修改内容

**文件**: `admin/settings.php`

**修改前**:
```html
<div class="config-item">
    <label>📅 签到间隔时间（小时）</label>
    <input type="number" name="setting_checkin_interval_hours" 
           value="48" min="1" max="168">
    <div class="help">用户需要多久签到一次（默认 48 小时）</div>
</div>
```

**修改后**:
```html
<div class="config-item">
    <label>📅 签到间隔时间（小时）</label>
    <div style="padding: 10px; background: #f3f4f6; border-radius: 6px; color: #666;">
        ℹ️ 由用户在 App 上自行设定，后台不限制
    </div>
</div>
```

### 说明
- ✅ 后台不再设置签到间隔
- ✅ 用户可以在 App 内自由设定自己的签到间隔
- ✅ 更符合个性化需求

---

## ✅ 任务 2: 修复告警设置并集成到系统配置

### 问题分析

**告警保存失败原因**:
1. `alerts.php` 从 `system_config` 表读取配置
2. `save_alert_config.php` 保存到 `system_config` 表
3. 但 `system_config` 表可能不存在或未初始化
4. 应该统一使用 `app_config` 表

### 修复方案

#### 1️⃣ 统一使用 app_config 表

**save_alert_config.php**:
```php
// 修复前：保存到 system_config 表
INSERT INTO system_config (config_key, config_value, config_desc)
VALUES ('alert_offline_timeout', ?, ...),
       ('alert_abnormal_distance', ?, ...)

// 修复后：保存到 app_config 表
INSERT INTO app_config (config_key, config_value, description)
VALUES 
    ('alert_offline_timeout', ?, '离线超时告警阈值（小时）'),
    ('alert_abnormal_distance', ?, '异常位置距离阈值（公里）')
ON DUPLICATE KEY UPDATE 
    config_value = VALUES(config_value),
    updated_at = CURRENT_TIMESTAMP
```

#### 2️⃣ alerts.php 从 app_config 读取

```php
// 获取告警配置（从 app_config 表）
try {
    $configStmt = $db->query("
        SELECT config_key, config_value 
        FROM app_config 
        WHERE config_key IN ('alert_offline_timeout', 'alert_abnormal_distance')
    ");
    while ($configRow = $configStmt->fetch()) {
        if ($configRow['config_key'] === 'alert_offline_timeout') {
            $offlineTimeout = (int)$configRow['config_value'];
        } elseif ($configRow['config_key'] === 'alert_abnormal_distance') {
            $abnormalDistance = (int)$configRow['config_value'];
        }
    }
} catch (Exception $e) {
    // 表不存在时使用默认值
}
```

#### 3️⃣ 集成告警设置到系统配置页面

**settings.php 新增告警设置区块**:

```html
<!-- 告警设置（集成到系统配置） -->
<div class="config-section">
    <h2>🚨 告警设置</h2>
    
    <div class="config-item">
        <label>⏰ 离线超时告警阈值（小时）</label>
        <input type="number" name="setting_alert_offline_timeout" 
               value="24" min="1" max="168">
        <div class="help">用户超过多少小时未签到触发告警</div>
    </div>
    
    <div class="config-item">
        <label>📍 异常位置告警阈值（公里）</label>
        <input type="number" name="setting_alert_abnormal_distance" 
               value="100" min="10" max="1000">
        <div class="help">1 小时内移动超过多少公里触发异常告警</div>
    </div>
    
    <div class="config-item">
        <label>🔔 最大提醒次数</label>
        <input type="number" name="setting_notify_max_reminders" 
               value="10" min="1" max="20">
        <div class="help">签到提醒最多推送多少次</div>
    </div>
    
    <div class="config-item">
        <label>⚠️ 最大超时通知次数</label>
        <input type="number" name="setting_notify_max_overdue" 
               value="5" min="1" max="20">
        <div class="help">超时后最多通知监护人多少次</div>
    </div>
</div>
```

#### 4️⃣ 初始化告警配置

**database_init_config.sql**:
```sql
INSERT INTO app_config (config_key, config_value, description) VALUES
-- 告警设置
('alert_offline_timeout', '24', '离线超时告警阈值（小时）'),
('alert_abnormal_distance', '100', '异常位置距离阈值（公里）'),
...
ON DUPLICATE KEY UPDATE 
    config_value = VALUES(config_value),
    updated_at = CURRENT_TIMESTAMP;
```

---

## ✅ 任务 3: 首页卡片颜色根据倒计时变化

### 颜色逻辑

**文件**: `HomeStatusView.swift`

**颜色规则**:
```
安全签到倒计时 > 提醒阈值（12 小时） → 🟢 绿色
安全签到倒计时 < 提醒阈值（12 小时） → 🟠 橙色
安全签到倒计时结束（<0）           → 🔴 红色
```

### 实现代码

```swift
// 🎨 签到卡片颜色（根据倒计时状态变化）
private var checkInColors: [Color] {
    let hoursRemaining = secondsRemaining / 3600
    let reminderThreshold = dataManager.systemConfig.checkinReminderThresholdHours  // 提醒阈值（默认 12 小时）
    
    if hoursRemaining > reminderThreshold {
        // 🟢 绿色：安全签到倒计时（高于提醒阈值）
        return [Color(hex: "34C759"), Color(hex: "28A74A")]
    } else if hoursRemaining > 0 {
        // 🟠 橙色：警告状态（低于提醒阈值但还未到期）
        return [Color(hex: "FF9500"), Color(hex: "FF8800")]
    } else {
        // 🔴 红色：危险状态（倒计时结束，已超时）
        return [Color(hex: "FF3B30"), Color(hex: "FF2D55")]
    }
}

private var checkInShadowColor: Color {
    let hoursRemaining = secondsRemaining / 3600
    let reminderThreshold = dataManager.systemConfig.checkinReminderThresholdHours
    
    if hoursRemaining > reminderThreshold {
        return Color(hex: "34C759")  // 🟢 绿色阴影
    } else if hoursRemaining > 0 {
        return Color(hex: "FF9500")  // 🟠 橙色阴影
    } else {
        return Color(hex: "FF3B30")  // 🔴 红色阴影
    }
}
```

### 视觉效果

```
┌─────────────────────────────────────┐
│  🟢 安全状态（剩余 >12 小时）          │
│  ┌─────────────────────────────┐    │
│  │ ✓ 安全签到                   │    │
│  │                              │    │
│  │       35:42:18              │    │
│  │                              │    │
│  │  打开 App 即可自动签到         │    │
│  └─────────────────────────────┘    │
│  渐变绿色背景 + 绿色阴影              │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🟠 警告状态（剩余 <12 小时）          │
│  ┌─────────────────────────────┐    │
│  │ ✓ 安全签到                   │    │
│  │                              │    │
│  │        08:15:32             │    │
│  │                              │    │
│  │  打开 App 即可自动签到         │    │
│  └─────────────────────────────┘    │
│  渐变橙色背景 + 橙色阴影              │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  🔴 超时状态（已过期）                │
│  ┌─────────────────────────────┐    │
│  │ ✓ 安全签到                   │    │
│  │                              │    │
│  │       -02:30:45             │    │
│  │                              │    │
│  │  打开 App 即可自动签到         │    │
│  └─────────────────────────────┘    │
│  渐变红色背景 + 红色阴影              │
└─────────────────────────────────────┘
```

---

## 📊 完整配置项列表

### 系统配置页面结构

```
⚙️ 系统配置
├─ 📱 客户端配置（公开）
│  ├─ 🔔 签到提醒阈值（小时）: 12
│  ├─ ⏰ 签到提醒间隔（小时）: 2
│  ├─ 📅 签到间隔时间（小时）: ℹ️ 由用户在 App 设定
│  ├─ ⚠️ 离线超时阈值（小时）: 24
│  ├─ 📱 超时通知间隔（小时）: 1
│  ├─ 💬 短信通知开关：启用
│  ├─ 最新版本号：2.0.0
│  └─ 强制更新最低版本：1.0.0
│
├─ 🚨 告警设置
│  ├─ ⏰ 离线超时告警阈值（小时）: 24
│  ├─ 📍 异常位置告警阈值（公里）: 100
│  ├─ 🔔 最大提醒次数：10
│  └─ ⚠️ 最大超时通知次数：5
│
└─ 🔒 系统配置（私密）
   ├─ 维护模式：关闭
   └─ 维护模式提示信息：...
```

---

## 🗺️ app_config 配置项完整列表

| 配置项 | 默认值 | 说明 |
|-------|-------|------|
| **签到相关** |
| checkin_interval_hours | 48 | 签到间隔（由用户设定） |
| notify_checkin_threshold | 12 | 开始提醒阈值（剩余 12 小时） |
| notify_reminder_interval | 2 | 提醒间隔（每 2 小时） |
| **超时通知** |
| notify_overdue_threshold | 24 | 离线超时阈值（24 小时） |
| notify_overdue_interval | 1 | 超时通知间隔（每 1 小时） |
| **短信通知** |
| notify_enable_sms | 1 | 是否启用短信通知 |
| sms_provider | aliyun | 短信服务商 |
| **限制相关** |
| notify_max_reminders | 10 | 最大提醒次数 |
| notify_max_overdue | 5 | 最大超时通知次数 |
| min_emergency_contacts | 2 | 最少紧急联系人数 |
| **告警设置** |
| alert_offline_timeout | 24 | 离线超时告警阈值 |
| alert_abnormal_distance | 100 | 异常位置距离阈值 |
| **App 版本** |
| app_version_latest | 2.0.0 | 最新版本号 |
| app_version_force | 1.0.0 | 强制更新版本 |
| app_maintenance | 0 | 维护模式开关 |
| app_maintenance_msg | ... | 维护消息 |

---

## 🚀 部署步骤

### 1. 前端部署（iOS App）

```bash
cd /Users/lishimin/Documents/zhonghuo-app

# 编译测试
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build

# 安装到模拟器/设备
xcrun simctl install "iPhone 17 Pro" \
  ~/Library/Developer/Xcode/DerivedData/终活-*/Build/Products/Debug-iphonesimulator/终活.app
```

### 2. 后端部署（服务器）

```bash
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn

# 拉取最新代码
git pull origin main

# 更新数据库配置（执行 SQL）
mysql -uzhonghuo -pzhonghuo zhonghuo < database_init_config.sql

# 验证配置表
mysql -uzhonghuo -pzhonghuo zhonghuo -e "SELECT config_key, config_value FROM app_config WHERE config_key LIKE 'alert_%'"

# 访问设置页面
# http://8.136.41.211:3395/admin/settings.php
```

---

## 🧪 测试验证

### 测试场景 1: 首页卡片颜色

```
场景：安全签到倒计时 > 12 小时
预期：
- 卡片背景：渐变绿色
- 阴影颜色：绿色
- 视觉感受：安全、舒适

场景：安全签到倒计时 < 12 小时
预期：
- 卡片背景：渐变橙色
- 阴影颜色：橙色
- 视觉感受：警告、注意

场景：安全签到倒计时结束（<0）
预期：
- 卡片背景：渐变红色
- 阴影颜色：红色
- 视觉感受：危险、紧急
```

### 测试场景 2: 告警设置保存

```
步骤:
1. 访问 admin/settings.php
2. 滚动到「🚨 告警设置」部分
3. 修改「离线超时告警阈值」为 36
4. 修改「异常位置告警阈值」为 150
5. 点击「保存设置」
6. 刷新页面，确认设置已保存

预期:
✅ 设置保存成功提示
✅ 刷新后数值保持修改后的值
✅ app_config 表中数据已更新
✅ alerts.php 读取新配置生效
```

### 测试场景 3: 签到间隔设置

```
步骤:
1. 访问 admin/settings.php
2. 查看「📅 签到间隔时间（小时）」
3. 确认显示「ℹ️ 由用户在 App 上自行设定，后台不限制」

预期:
✅ 不再是输入框
✅ 显示提示信息
✅ 后台无法修改
```

---

## 📝 修改文件清单

### 前端（1 个文件）
- `HomeStatusView.swift` - 卡片颜色逻辑修改

### 后端（4 个文件）
- `admin/settings.php` - 移除签到间隔 + 集成告警设置
- `admin/alerts.php` - 从 app_config 读取配置
- `admin/save_alert_config.php` - 保存到 app_config 表
- `database_init_config.sql` - 添加告警配置项

---

## 📄 相关提交

- 前端：`c95d811` - 🎨 首页卡片颜色根据提醒阈值变化
- 后端：`23b4464` - ⚙️ 移除签到间隔设置 + 集成告警设置

---

## 💡 使用说明

### 首页卡片颜色含义

| 颜色 | 状态 | 含义 | 用户操作 |
|------|------|------|---------|
| 🟢 绿色 | 安全 | 倒计时 > 12 小时 | 无需操作，正常使用 |
| 🟠 橙色 | 警告 | 倒计时 < 12 小时 | 建议尽快签到 |
| 🔴 红色 | 超时 | 倒计时已结束 | 立即签到，避免通知监护人 |

### 告警设置建议

| 配置项 | 建议值 | 说明 |
|-------|-------|------|
| 离线超时告警阈值 | 24-48 小时 | 给用户足够缓冲时间 |
| 异常位置告警阈值 | 100-200 公里 | 避免误报（如跨城出行） |
| 最大提醒次数 | 10 次 | 避免过度打扰 |
| 最大超时通知次数 | 5 次 | 及时通知但不过度 |

---

**修改完成**: ✅ 前后端均已推送  
**下一步**: 部署到服务器并测试验证

**核心改进**:
- 🎨 首页卡片颜色动态变化（绿/橙/红）
- ⚙️ 告警设置集成到系统配置
- 📱 签到间隔由用户自行设定
