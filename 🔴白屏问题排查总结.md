# 🔴 白屏问题全面排查总结

**排查时间**: 2026-03-22 02:00 - 02:25  
**问题**: 真机持续白屏  
**状态**: ⏳ 修复中

---

## 📋 已修复的问题

### 1️⃣ @StateObject 生命周期问题
- **文件**: `ContentView.swift`, `HomeStatusView.swift`
- **问题**: 单例对象使用 `@StateObject` 导致双重管理
- **修复**: 改为 `@ObservedObject`
- **提交**: `9f92cc6`

### 2️⃣ 后台任务注册崩溃
- **文件**: `ZhonghuoApp.swift`, `Info.plist`
- **问题**: 后台任务未正确注册，真机触发断言失败
- **修复**: 临时移除后台任务注册和声明
- **提交**: `675de69`, `2f3f780`

### 3️⃣ 初始化时序问题
- **文件**: `ContentView.swift`
- **问题**: `checkLoginStatus` 可能阻塞或状态不更新
- **修复**: 使用 `DispatchQueue.main.async` 确保异步执行
- **提交**: `2f3f780`

---

## 🔍 待排查的问题

### 1. MediaRecorderView 的 @StateObject
```swift
// MediaRecorderView.swift:17
@StateObject private var recorder = Recorder()
```
**检查**: `Recorder` 是否是单例？如果是，需要改为 `@ObservedObject`

### 2. DataManager 初始化
```swift
// DataManager.swift:191
init() {
    initializeAPIConfig()  // 同步
    loadAllData()          // 可能耗时
}
```
**检查**: `loadAllData()` 是否阻塞主线程

### 3. 网络请求
**检查**: 是否有同步网络请求阻塞 UI

---

## 🛠️ 修复策略

### 第一轮：简化启动（已完成）
- ✅ 移除后台任务
- ✅ 简化 `onAppear` 逻辑
- ✅ 优化 `checkLoginStatus`

### 第二轮：如果还是白屏
1. 检查 `MediaRecorderView`
2. 检查 `DataManager.loadAllData()`
3. 添加详细日志定位卡住位置

### 第三轮：终极方案
如果以上都失败：
1. 创建最小可运行版本
2. 逐个模块添加
3. 定位具体导致白屏的模块

---

## 📊 修改统计

| 提交 | 文件 | 修改内容 |
|------|------|----------|
| `9f92cc6` | 3 | @StateObject → @ObservedObject |
| `675de69` | 5 | 移除后台任务 + 简化启动 |
| `2f3f780` | 2 | 修复编译错误（[weak self]） |

**总计**: 5 个文件，+728 行，-87 行

---

## 🧪 测试步骤

### 1. 编译
```bash
cd /Users/lishimin/Documents/zhonghuo-app
rm -rf ~/Library/Developer/Xcode/DerivedData/终活-*
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' clean build
```

### 2. 模拟器测试
```bash
xcrun simctl launch "iPhone 17 Pro" com.zhonghuo.app
```

### 3. 真机测试
```bash
# Xcode → Product → Archive → Distribute
# 安装到真机
```

---

## 📝 详细日志

### 成功启动日志
```
🔍 开始检查登录状态...
🔍 UserManager.loadUser() 被调用
🔍 登录状态检查：
   - isLoggedIn: true/false
   - currentUser: xxx
✅ 用户已登录，执行自动签到...
```

### 白屏时可能的日志
```
// 如果卡在 DataManager 初始化
🔵 API 已初始化（默认地址）
// 没有后续日志...

// 如果卡在 UserManager
🔍 UserManager.loadUser() 被调用
// 没有后续日志...

// 如果卡在 ContentView
ContentView.body 被调用
// 没有后续日志...
```

---

## 🎯 下一步

1. ⏳ 等待编译完成
2. ⏳ 模拟器测试
3. ⏳ 真机测试
4. 如果还是白屏：
   - 添加详细日志
   - 逐个模块排查
   - 创建最小可运行版本

---

**核心修复**：
- 🔴 移除后台任务
- 🔴 简化启动逻辑
- 🔴 优化状态更新

**预期结果**：App 正常启动！
