# 🔧 2026-03-20 功能完善计划

## 用户需求（2026-03-20 15:46）

1. ✅ **时光胶囊卡片颜色改为白色** - 已完成
2. ⏳ **紧急联系人数量同步到后端** - 待实现
3. ⏳ **时光胶囊媒体文件持久化保存并同步** - 待实现
4. ⏳ **后端告警中心对接前端推送配置** - 待实现

---

## 1. ✅ 时光胶囊卡片颜色改为白色

**修改文件**: `TimeCapsuleView.swift`

**修改内容**:
- `StatItem` 组件中所有颜色改为白色
- 图标：`.foregroundColor(.white)`
- 数字：`.foregroundColor(.white)`
- 标签：`.foregroundColor(.white.opacity(0.85))`

**状态**: ✅ 已完成

---

## 2. ⏳ 紧急联系人数量同步到后端

### 当前状态
- 前端：`User.EmergencyContact` 模型已定义
- 前端：`AuthView.swift` 中 `emergencyContacts: []` 初始化
- 后端：`api/emergency_contacts.php` 已实现批量同步接口
- ❌ **问题**: 紧急联系人数据未实际同步到后端

### 实现方案

#### 前端修改
1. **AuthView.swift** - 登录后同步紧急联系人
   ```swift
   // 同步紧急联系人
   if !user.emergencyContacts.isEmpty {
       await DataManager.shared.batchSyncEmergencyContacts()
   }
   ```

2. **DataManager.swift** - 添加同步方法
   ```swift
   func batchSyncEmergencyContacts() async -> (total: Int, created: Int, updated: Int)?
   ```

3. **SettingsView.swift** - 见证人页面添加紧急联系人管理
   - 显示已同步的紧急联系人列表
   - 支持添加、编辑、删除
   - 实时同步到后端

#### 后端修改
1. **api/emergency_contacts.php** - 已有接口，无需修改
   - `batch_sync`: 批量同步
   - `list`: 获取列表
   - `create/update/delete`: 单个操作

2. **admin/emergency_contacts.php** - 创建管理页面
   - 查看所有用户的紧急联系人
   - 支持搜索、筛选、导出

### 数据库表结构
```sql
CREATE TABLE emergency_contacts (
    id VARCHAR(36) PRIMARY KEY,
    user_id VARCHAR(36) NOT NULL,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) NOT NULL,
    relationship VARCHAR(50) NOT NULL,
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    INDEX idx_user_id (user_id)
);
```

---

## 3. ⏳ 时光胶囊媒体文件持久化保存并同步

### 当前状态
- 前端：`MediaRecorderView` 支持录音和录像
- 前端：录制的文件保存在临时目录
- ❌ **问题**: 文件未持久化，App 重启后丢失
- ❌ **问题**: 未同步到后端服务器

### 实现方案

#### 前端修改
1. **TimeCapsuleView.swift** - 持久化存储
   ```swift
   // 将临时文件移动到持久化目录
   let documentsPath = FileManager.default.urls(for: .documentDirectory, in: .userDomainMask)[0]
   let permanentURL = documentsPath.appendingPathComponent("capsules/\(capsuleId).\(extension)")
   try FileManager.default.moveItem(at: tempURL, to: permanentURL)
   ```

2. **DataManager.swift** - 添加媒体文件同步
   ```swift
   func syncCapsuleMedia(capsule: TimeCapsule) async -> Bool {
       // 1. 读取本地文件
       // 2. 上传到后端 /api/upload.php
       // 3. 更新胶囊的 mediaUrl 字段
   }
   ```

3. **AddCapsuleModal/EditCapsuleModal** - 创建胶囊时同步上传
   ```swift
   // 创建胶囊后
   if let mediaURL = recordedAudioURL ?? recordedVideoURL {
       await DataManager.shared.syncCapsuleMedia(capsule: newCapsule)
   }
   ```

#### 后端修改
1. **api/upload.php** - 增强文件上传
   ```php
   // 支持胶囊媒体文件上传
   // 验证 token
   // 保存文件到 /uploads/capsules/{user_id}/{file_id}.{ext}
   // 返回文件 URL
   ```

2. **api/capsules.php** - 胶囊创建/更新时支持 media_url
   ```php
   // create_capsule: 接收 media_url 参数
   // update_capsule: 支持更新 media_url
   // get_capsules: 返回 media_url
   ```

3. **database.sql** - 添加媒体文件字段
   ```sql
   ALTER TABLE capsules ADD COLUMN media_url VARCHAR(500) DEFAULT NULL;
   ALTER TABLE capsules ADD COLUMN media_type VARCHAR(20) DEFAULT NULL; -- audio/video
   ALTER TABLE capsules ADD COLUMN media_duration INT DEFAULT NULL; -- 时长（秒）
   ```

### 文件存储结构
```
后端服务器：
/www/wwwroot/zhonghuo.cn/uploads/capsules/
├── {user_id_1}/
│   ├── {capsule_id_1}.m4a
│   └── {capsule_id_2}.mp4
└── {user_id_2}/
    └── ...

前端本地：
Documents/capsules/
├── {capsule_id_1}.m4a
└── {capsule_id_2}.mp4
```

---

## 4. ⏳ 后端告警中心对接前端推送配置

### 当前状态
- 前端：`SystemConfig` 有推送配置字段
  - `checkinReminderThresholdHours: Double = 12.0`
  - `checkinReminderIntervalHours: Double = 2.0`
- 前端：`NotificationManager.shared.scheduleCheckInReminders()`
- ❌ **问题**: 后端无告警中心功能
- ❌ **问题**: 推送配置未从后端加载

### 实现方案

#### 前端修改
1. **DataManager.swift** - 加载系统配置
   ```swift
   func loadSystemConfig() async {
       // 从 /api/config.php 加载配置
       // 更新 systemConfig @Published 属性
   }
   ```

2. **HomeStatusView.swift** - 使用后端配置
   ```swift
   let threshold = dataManager.systemConfig.checkinReminderThresholdHours
   let interval = dataManager.systemConfig.checkinReminderIntervalHours
   NotificationManager.shared.scheduleCheckInReminders(
       thresholdHours: threshold,
       intervalHours: interval
   )
   ```

#### 后端修改
1. **api/config.php** - 系统配置接口
   ```php
   // GET /api/config.php
   // 返回系统配置 JSON
   {
       "status": "success",
       "data": {
           "checkinReminderThresholdHours": 12.0,
           "checkinReminderIntervalHours": 2.0,
           "offlineTimeoutHours": 24.0,
           ...
       }
   }
   ```

2. **admin/alerts.php** - 告警中心管理页面
   ```php
   // 查看所有用户的签到状态
   // 筛选即将超时的用户
   // 手动发送提醒通知
   // 查看通知历史记录
   ```

3. **database.sql** - 添加告警记录表
   ```sql
   CREATE TABLE checkin_alerts (
       id INT AUTO_INCREMENT PRIMARY KEY,
       user_id VARCHAR(36) NOT NULL,
       alert_type VARCHAR(50) NOT NULL, -- reminder/urgent/offline
       sent_at DATETIME NOT NULL,
       channel VARCHAR(20) NOT NULL, -- push/sms/email
       status VARCHAR(20) NOT NULL, -- pending/sent/failed
       message TEXT,
       INDEX idx_user_id (user_id),
       INDEX idx_sent_at (sent_at)
   );
   ```

4. **cron/checkin_alerts.php** - 定时任务
   ```php
   // 每分钟执行一次
   // 检查所有用户的签到状态
   // 根据配置发送提醒
   // 记录到 checkin_alerts 表
   ```

### 推送通知流程
```
1. 用户登录 → 加载系统配置（阈值 12h，间隔 2h）
2. 签到成功 → 安排提醒（剩余 12h 时开始，每 2h 推送一次）
3. 倒计时 < 12h → 推送第一条提醒
4. 倒计时 < 10h → 推送第二条提醒
5. 倒计时 < 0h → 推送警告通知
6. 倒计时 < -24h → 推送危险通知（红色）

后端告警中心：
- 实时监控所有用户签到状态
- 自动发送提醒（推送/短信/邮件）
- 管理员可查看告警历史
- 支持手动触发提醒
```

---

## 实施顺序

### 第一阶段：紧急联系人同步（优先级：高）
1. ✅ 检查后端 `emergency_contacts.php` 接口
2. ⏳ 前端 `DataManager` 添加 `batchSyncEmergencyContacts()` 方法
3. ⏳ `AuthView` 登录后同步紧急联系人
4. ⏳ `SettingsView` 见证人页面显示紧急联系人
5. ⏳ 后端 `admin/emergency_contacts.php` 管理页面

### 第二阶段：时光胶囊媒体持久化（优先级：高）
1. ⏳ 前端 `TimeCapsuleView` 持久化存储媒体文件
2. ⏳ 后端 `api/upload.php` 支持文件上传
3. ⏳ 数据库添加 `media_url` 等字段
4. ⏳ 创建胶囊时自动上传媒体文件
5. ⏳ 胶囊列表支持播放媒体文件

### 第三阶段：告警中心对接（优先级：中）
1. ⏳ 后端 `api/config.php` 返回系统配置
2. ⏳ 前端 `DataManager.loadSystemConfig()` 加载配置
3. ⏳ 后端 `admin/alerts.php` 告警中心页面
4. ⏳ 数据库添加 `checkin_alerts` 表
5. ⏳ 定时任务 `cron/checkin_alerts.php`

---

## 预计完成时间
- **第一阶段**: 2-3 小时
- **第二阶段**: 3-4 小时
- **第三阶段**: 2-3 小时
- **总计**: 7-10 小时

---

*创建时间：2026-03-20 15:55*
