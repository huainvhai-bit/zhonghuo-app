# 🔧 自定义数据库配置指南

**更新**：现在支持自定义数据库名和密码，不再写死！

---

## 🚀 使用方法（3 选 1）

### 方法 1：配置生成器（推荐）

**在服务器上执行**：

```bash
# 1. SSH 登录
ssh root@8.136.41.211

# 2. 进入目录
cd /www/wwwroot/zhonghuo.cn

# 3. 拉取最新代码
git pull origin main

# 4. 运行配置生成器
bash setup-config.sh
```

**脚本会提示输入**：
- 数据库主机（默认：localhost）
- 数据库名（默认：zhonghuo_db）
- 数据库用户名（默认：zhonghuo_user）
- 数据库密码（直接回车随机生成）
- MySQL root 密码

**然后自动**：
- ✅ 生成 config.php
- ✅ 创建数据库
- ✅ 创建用户
- ✅ 创建数据表
- ✅ 设置权限
- ✅ 验证连接

---

### 方法 2：手动编辑 config.php

**编辑文件**：
```bash
cd /www/wwwroot/zhonghuo.cn
vi config.php
```

**修改内容**：
```php
<?php
// 改成你自己的配置
$GLOBALS['db_host'] = 'localhost';
$GLOBALS['db_name'] = '你的数据库名';
$GLOBALS['db_user'] = '你的用户名';
$GLOBALS['db_pass'] = '你的密码';
```

**然后创建数据库**：
```bash
mysql -u root -p << 'EOF'
CREATE DATABASE 你的数据库名 CHARACTER SET utf8mb4;
CREATE USER '你的用户名'@'localhost' IDENTIFIED BY '你的密码';
GRANT ALL PRIVILEGES ON 你的数据库名.* TO '你的用户名'@'localhost';
FLUSH PRIVILEGES;
EXIT;
EOF
```

---

### 方法 3：使用环境变量（生产环境推荐）

**在服务器上设置环境变量**：

```bash
# 添加到 /etc/profile 或 ~/.bashrc
export DB_HOST=localhost
export DB_NAME=zhonghuo_db
export DB_USER=zhonghuo_user
export DB_PASS=你的密码

# 生效
source /etc/profile
```

**config.php 会自动读取环境变量**。

---

## ✅ 验证配置

**访问诊断页面**：
```
http://8.136.41.211:3395/api/test_db.php
```

**或测试 API**：
```bash
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试","password":"test123","verify_code":"123456"}'
```

---

## 📊 config.php 说明

```php
<?php
/**
 * 终活 App 后端数据库配置
 * 
 * 说明：
 * - 数据库配置从环境变量读取（推荐）
 * - 如果没有环境变量，使用以下默认值
 * - 生产环境建议使用 .env 文件或环境变量
 */

// 数据库配置（支持环境变量）
$GLOBALS['db_host'] = getenv('DB_HOST') ?: 'localhost';
$GLOBALS['db_name'] = getenv('DB_NAME') ?: 'zhonghuo_db';
$GLOBALS['db_user'] = getenv('DB_USER') ?: 'zhonghuo_user';
$GLOBALS['db_pass'] = getenv('DB_PASS') ?: 'Zhonghuo@2026';
```

**优先级**：
1. 环境变量（最高优先级）
2. config.php 中的默认值

---

## 🔐 安全建议

1. **生产环境使用环境变量**：
   ```bash
   export DB_PASS="强密码"
   ```

2. **不要提交 config.php 到 Git**：
   ```bash
   # 添加到 .gitignore
   echo "config.php" >> .gitignore
   ```

3. **使用强密码**：
   - 至少 16 位
   - 包含大小写字母、数字、特殊字符
   - 使用 `setup-config.sh` 自动生成

4. **定期备份数据库**：
   ```bash
   mysqldump -u root -p 数据库名 > backup.sql
   ```

---

## 📝 已推送文件

**后端 GitHub**：https://github.com/huainvhai-bit/zhonghuo-backend-php

**修改文件**：
- ✅ `config.php` - 支持环境变量
- ✅ `setup-config.sh` - 配置生成器

**最新提交**：`4e59bec` - 🔧 支持自定义数据库配置

---

*数据库配置不再写死，可以自定义任何值！*
