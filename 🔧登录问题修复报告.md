# 🔧 登录问题修复报告

## 📅 修复时间
2026-03-21 23:30

## 📊 问题分析

### 问题 1: 后台任务未注册 ❌
```
Registration rejected; com.zhonghuo.app.sms_notify is not advertised
Registration rejected; com.zhonghuo.app.refresh_notifications is not advertised
```

**原因**：
- Info.plist 缺少 `BGTaskSchedulerPermittedIdentifiers` 配置
- 后台短信和通知任务无法注册

**修复**：
```xml
<key>BGTaskSchedulerPermittedIdentifiers</key>
<array>
    <string>com.zhonghuo.app.sms_notify</string>
    <string>com.zhonghuo.app.refresh_notifications</string>
    <string>com.zhonghuo.app.checkin</string>
</array>
```

---

### 问题 2: 登录密码错误 ❌
```
📥 响应内容：{"error":"密码错误","code":"INVALID_PASSWORD"}
```

**测试账号**：
- 手机号：`13800138006`
- 密码：`test123456` 或 `123456`

**可能原因**：
1. 数据库中密码哈希不正确
2. 后端 password_verify 验证失败
3. 账号不存在

**排查步骤**：
```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 检查数据库中的用户
mysql -u zhonghuo -p zhonghuo
SELECT id, name, phone, password_hash FROM users WHERE phone='13800138006';

# 如果密码哈希为空或错误，重新设置
UPDATE users SET password_hash = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi' WHERE phone='13800138006';
# 密码：password
```

---

### 问题 3: Token 为 nil ❌
```
- Token: nil
❌ 签到同步：缺少必要参数
⚠️ 无 token，跳过位置上传
```

**原因**：
- 登录失败导致 Token 未保存
- 退出登录时清除了所有状态

**修复**：
- ✅ 先修复登录问题
- ✅ Token 会自动保存

---

## ✅ 修复方案

### 1. Info.plist 后台任务配置

**文件**：`/Users/lishimin/Documents/zhonghuo-app/Info.plist`

**修复内容**：
```xml
<key>BGTaskSchedulerPermittedIdentifiers</key>
<array>
    <string>com.zhonghuo.app.sms_notify</string>
    <string>com.zhonghuo.app.refresh_notifications</string>
    <string>com.zhonghuo.app.checkin</string>
</array>
```

**提交**：前端（待提交）

---

### 2. 后端密码修复

**服务器命令**：
```bash
# SSH 登录
ssh root@8.136.41.211

# 检查用户
mysql -u zhonghuo -p zhonghuo -e "SELECT id, name, phone, password_hash FROM users WHERE phone='13800138006';"

# 如果密码错误，重新设置（密码：test123456）
mysql -u zhonghuo -p zhonghuo -e "UPDATE users SET password_hash = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi' WHERE phone='13800138006';"

# 或者创建新测试账号
mysql -u zhonghuo -p zhonghuo -e "INSERT INTO users (id, name, phone, password_hash, created_at) VALUES ('test_user_001', '测试用户', '18888888888', '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', NOW());"
```

---

### 3. 前端编译部署

**编译命令**：
```bash
cd /Users/lishimin/Documents/zhonghuo-app

# 清理缓存
rm -rf ~/Library/Developer/Xcode/DerivedData/终活-*

# 编译
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' clean build

# 安装
xcrun simctl uninstall "iPhone 17 Pro" com.zhonghuo.app
xcrun simctl install "iPhone 17 Pro" <app-path>
```

---

## 🧪 测试验证

### 1. 后台任务注册
- [ ] 启动 App
- [ ] 检查日志中无 "Registration rejected" 错误
- [ ] 设置签到提醒
- [ ] 切换到后台
- [ ] 等待提醒时间

### 2. 登录功能
- [ ] 使用测试账号登录
- [ ] 检查日志中 Token 已保存
- [ ] 检查 UserDefaults 中有 userToken
- [ ] 检查 UserManager.isLoggedIn = true

### 3. 数据同步
- [ ] 登录后创建时光胶囊
- [ ] 检查同步日志
- [ ] 确认无 "无 token" 错误

---

## 📝 测试账号

| 手机号 | 密码 | 状态 |
|--------|------|------|
| 18888888889 | 123456 | ⚠️ 注册频率限制 |
| 13800138006 | test123456 | ❌ 密码错误（待修复） |
| 18888888888 | 123456 | ⏳ 待创建 |

**建议**：
1. 等待 1 小时后重试注册（18888888889）
2. 或修复数据库密码（13800138006）
3. 或创建新测试账号

---

## 🚀 部署步骤

### 后端（必须）
```bash
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn

# 拉取代码
git pull origin main

# 修复测试账号密码
mysql -u zhonghuo -p zhonghuo
> UPDATE users SET password_hash = '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi' WHERE phone='13800138006';
> SELECT phone, password_hash FROM users LIMIT 5;
> EXIT;
```

### 前端
```bash
cd /Users/lishimin/Documents/zhonghuo-app
git add Info.plist
git commit -m "🔧 Info.plist 添加后台任务配置"
git push origin main

# 编译安装
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
xcrun simctl install "iPhone 17 Pro" <app-path>
```

---

## 🎯 下一步

1. ✅ Info.plist 后台任务配置 - 已修复
2. ⏳ 后端密码修复 - 待执行
3. ⏳ 前端编译部署 - 待执行
4. ⏳ 真机测试验证 - 待执行

---

*最后更新：2026-03-21 23:30*
