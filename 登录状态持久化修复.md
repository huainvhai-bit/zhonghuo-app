# 2026-03-21 - 修复重新打开 App 会重新登录的问题

## 工作时间
下午 16:56 - 17:00

## 问题描述
用户报告：重新打开 App 会重新登录，登录状态无法保持。

## 问题根源

### 1. StateObject 使用问题 ❌
```swift
// 错误：创建了新的 @StateObject，可能导致状态不同步
@StateObject private var userManager = UserManager.shared
```

**问题**：`@StateObject` 会创建新的实例管理，与 `UserManager.shared` 单例模式冲突。

### 2. 登录状态检查时机问题 ❌
```swift
// 错误：在 UserManager.loadUser() 完成前就检查状态
if !userManager.isLoggedIn {
    AuthView()
}
```

**问题**：
- `UserManager.loadUser()` 在 `init()` 中异步调用
- `ContentView` 可能在加载完成前就检查了 `isLoggedIn`
- 导致 `isLoggedIn` 还是初始值 `false`

### 3. 缺少加载状态 ❌
```swift
// 错误：没有等待登录状态检查完成
var body: some View {
    if !userManager.isLoggedIn {
        AuthView()  // 可能误判
    }
}
```

## 修复方案

### 1. 使用 shared 单例而不是 @StateObject ✅
```swift
// 正确：直接使用 shared 单例
private var userManager: UserManager { UserManager.shared }
```

### 2. 添加加载状态 ✅
```swift
@State private var isCheckingAuth = true  // 加载状态

var body: some View {
    if isCheckingAuth {
        LoadingView()  // 显示加载界面
    } else if forceLogout || !userManager.isLoggedIn || userManager.currentUser == nil {
        AuthView()  // 确保 isLoggedIn 和 currentUser 都有效
    } else {
        mainTabView
    }
}
```

### 3. 延迟检查登录状态 ✅
```swift
.onAppear {
    // 等待 UserManager 完成加载
    DispatchQueue.main.asyncAfter(deadline: .now() + 0.3) {
        // 再次确认登录状态（从 Token 和本地文件恢复）
        userManager.loadUser()
        
        // 确保 isLoggedIn 和 currentUser 都有效
        let isLoggedIn = userManager.isLoggedIn && userManager.currentUser != nil
        print("🔍 登录状态检查：isLoggedIn=\(isLoggedIn)")
        
        isCheckingAuth = false
        
        // 用户已登录时，执行自动签到
        if isLoggedIn {
            Task {
                await userManager.performAutoSignIn()
            }
        }
    }
}
```

### 4. 添加 LoadingView ✅
```swift
struct LoadingView: View {
    var body: some View {
        VStack(spacing: 20) {
            ProgressView()
                .scaleEffect(1.5)
            
            Text("正在加载...")
                .font(.system(size: 16, weight: .medium))
                .foregroundColor(.gray)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color(hex: "F5F5F7"))
    }
}
```

## 修改的文件

### ContentView.swift
1. 删除 `@StateObject private var userManager = UserManager.shared`
2. 添加 `private var userManager: UserManager { UserManager.shared }`
3. 添加 `@State private var isCheckingAuth = true`
4. 在 `body` 中添加加载状态检查
5. 在 `onAppear` 中延迟检查登录状态
6. 添加 `LoadingView` 视图

## 登录流程（修复后）

```
App 启动
    ↓
ContentView.onAppear
    ↓
延迟 0.3 秒（等待 UserManager 初始化）
    ↓
userManager.loadUser()
    ├─ 检查 Token（UserDefaults）
    ├─ 检查本地文件（user.json）
    └─ 设置 isLoggedIn 和 currentUser
    ↓
检查登录状态（isLoggedIn && currentUser != nil）
    ↓
已登录 → mainTabView + 自动签到
未登录 → AuthView
```

## 关键修复点

1. **单例模式一致性**：始终使用 `UserManager.shared`，避免 `@StateObject` 创建新实例
2. **双重检查**：同时检查 `isLoggedIn` 和 `currentUser`，确保状态完整
3. **延迟检查**：给 `UserManager.loadUser()` 足够的时间完成加载
4. **加载界面**：避免在检查期间显示错误界面

## 编译状态
✅ BUILD SUCCEEDED

## 安装状态
✅ iPhone 17 Pro 模拟器

## 测试建议

1. **登录测试**：
   - 使用账号 `18888888889` / `123456` 登录
   - 关闭 App（完全退出）
   - 重新打开 App
   - ✅ 应该保持在 mainTabView，不需要重新登录

2. **退出登录测试**：
   - 在设置中点击"退出登录"
   - 关闭 App
   - 重新打开 App
   - ✅ 应该显示 AuthView（登录界面）

3. **Token 持久化测试**：
   - 登录后检查 UserDefaults 中是否有 `userToken`
   - 关闭 App
   - 重新打开 App
   - ✅ Token 应该存在，登录状态保持

## 相关文档
- `UserManager.swift` - 用户管理（loadUser, login, logout）
- `AuthView.swift` - 登录注册界面
- `ZhonghuoApp.swift` - App 入口和账号验证

---

**修复完成！** 🎉
