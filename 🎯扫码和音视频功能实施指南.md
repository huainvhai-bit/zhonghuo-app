# 🎯 扫码绑定和音视频时光胶囊 - 实施指南

**更新时间**: 2026-03-20 18:55  
**状态**: 代码已预留，待 Xcode 配置

---

## 📱 功能 1：扫码绑定家人

### 当前状态

**代码完成度**: ✅ 100%  
**依赖配置**: ⏸️ 待 Xcode 中添加 Package  
**可用状态**: ⏸️ 待启用

### 已完成的代码

**BindFamilyView.swift**:
```swift
import CodeScanner  // ✅ 导入已添加

// ✅ 扫码按钮已添加
Button(action: { showingScanner = true }) {
    Image(systemName: "qrcode")
        .font(.system(size: 20))
        .foregroundColor(.indigo)
        .padding(10)
        .background(Color.indigo.opacity(0.1))
        .cornerRadius(8)
}

// ✅ Sheet 已添加
.sheet(isPresented: $showingScanner) {
    CodeScannerView(codeTypes: [.qr], simulatedData: "", completion: handleScan)
}

// ✅ 扫描处理函数已添加
private func handleScan(result: Result<ScanResult, ScanError>) {
    switch result {
    case .success(let result):
        let code = extractInviteCode(from: result.string)
        if !code.isEmpty {
            inviteCode = code
            showingScanner = false
            bindFamily()
        } else {
            errorMessage = "无效的二维码"
            showingError = true
        }
    case .failure(let error):
        errorMessage = error.localizedDescription
        showingError = true
        showingScanner = false
    }
}
```

### 启用步骤

**在 Xcode 中操作**:

1. **打开项目**
   ```bash
   open /Users/lishimin/Documents/zhonghuo-app/终活.xcodeproj
   ```

2. **添加 Package 依赖**
   - 菜单：**File → Add Package Dependencies...**
   - 输入：`https://github.com/twostraws/CodeScanner.git`
   - 版本：**Up to Next Major Version**
   - 最低版本：**2.0.0**
   - ✅ 勾选 **终活**（Add to target）
   - 点击：**"Add Package"**
   - 等待下载完成

3. **取消代码注释**
   
   **BindFamilyView.swift** 第 2 行：
   ```swift
   // 删除这行注释
   // import CodeScanner
   
   // 改为：
   import CodeScanner
   ```

   **BindFamilyView.swift** 扫码按钮部分：
   ```swift
   // 删除 /* 和 */ 注释标记
   Button(action: { showingScanner = true }) {
       // ...
   }
   ```

   **BindFamilyView.swift** sheet 部分：
   ```swift
   // 删除 /* 和 */ 注释标记
   .sheet(isPresented: $showingScanner) {
       CodeScannerView(codeTypes: [.qr], simulatedData: "", completion: handleScan)
   }
   ```

   **BindFamilyView.swift** handleScan 函数：
   ```swift
   // 删除 /* 和 */ 注释标记
   private func handleScan(result: Result<ScanResult, ScanError>) {
       // ...
   }
   ```

   **FamilyGuardView.swift** 第 2 行：
   ```swift
   // 删除这行注释
   // import CodeScanner
   
   // 改为：
   import CodeScanner
   ```

4. **编译测试**
   ```bash
   xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
   ```

5. **运行测试**
   - 选择 **iPhone 17 Pro** 模拟器
   - 点击运行 ▶️
   - 进入"家人守护"页面
   - 点击"绑定家人"
   - 点击扫码按钮（二维码图标）
   - 应该弹出扫码界面

### 功能说明

**用户流程**:
1. 打开家人守护页面
2. 点击"绑定家人"
3. 点击扫码按钮（二维码图标）
4. 相机打开，扫描二维码
5. 自动解析邀请码并绑定

**支持的二维码格式**:
- 纯邀请码：`ABC123`
- URL 格式：`https://zhonghuo.cn/invite?code=ABC123`

**错误处理**:
- 无效二维码：提示"无效的二维码"
- 扫描失败：显示错误信息
- 相机权限：首次使用会请求权限

---

## 🎤 功能 2：语音/视频时光胶囊

### 当前状态

**UI 完成度**: ✅ 100%  
**录制功能**: ⏸️ 待实现  
**可用状态**: ⏸️ 仅文字胶囊可用

### 现有 UI

TimeCapsuleView.swift 已包含完整的录制 UI：

**类型选择**:
```swift
Picker("类型", selection: $selectedType) {
    Label("文字", systemImage: "doc.text")
    Label("语音", systemImage: "mic.fill")      // ✅ 已有
    Label("视频", systemImage: "video.fill")    // ✅ 已有
}
```

**录制界面**:
```swift
// 录制按钮
Button(action: toggleRecording) {
    Image(systemName: isRecording ? "stop.fill" : "mic.fill")
    Text(isRecording ? "停止录制" : "开始录制")
}

// 预览播放器
if let url = recordedAudioURL {
    AudioPlayer(url: url)
} else if let url = recordedVideoURL {
    VideoPlayer(url: url)
}
```

### 实现方案

需要添加录制功能，有两种方案：

#### 方案 A：使用 AVFoundation（推荐）

创建录制管理器：

```swift
import AVFoundation
import AVKit

class MediaRecorderManager: ObservableObject {
    @Published var isRecording = false
    @Published var recordingTime: TimeInterval = 0
    
    private var audioRecorder: AVAudioRecorder?
    private var captureSession: AVCaptureSession?
    
    func startAudioRecording() -> Bool {
        // 实现录音功能
    }
    
    func stopAudioRecording() -> URL? {
        // 停止录音，返回文件 URL
    }
    
    func startVideoRecording() -> Bool {
        // 实现录像功能
    }
    
    func stopVideoRecording() -> URL? {
        // 停止录像，返回文件 URL
    }
}
```

#### 方案 B：使用系统相机（简化）

跳转到系统相机 App：

```swift
import UIKit

struct CameraPicker: UIViewControllerRepresentable {
    @Environment(\.presentationMode) var presentationMode
    @Binding var capturedMediaURL: URL?
    
    func makeUIViewController(context: Context) -> UIImagePickerController {
        let picker = UIImagePickerController()
        picker.sourceType = .camera
        picker.mediaTypes = ["public.movie", "public.audio"]
        picker.delegate = context.coordinator
        return picker
    }
    
    func updateUIViewController(_ uiViewController: UIImagePickerController, context: Context) {}
    
    func makeCoordinator() -> Coordinator {
        Coordinator(self)
    }
    
    class Coordinator: NSObject, UIImagePickerControllerDelegate, UINavigationControllerDelegate {
        let parent: CameraPicker
        
        init(_ parent: CameraPicker) {
            self.parent = parent
        }
        
        func imagePickerController(_ picker: UIImagePickerController, 
                                   didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey : Any]) {
            // 处理录制的媒体
            parent.presentationMode.wrappedValue.dismiss()
        }
    }
}
```

### 实施步骤

1. **创建录制管理器**（已创建 MediaRecorderManager.swift）

2. **集成到 TimeCapsuleView**
   ```swift
   struct TimeCapsuleView: View {
       @ObservedObject var recorder = MediaRecorderManager.shared
       // ...
   }
   ```

3. **实现录制按钮**
   ```swift
   Button(action: {
       if selectedType == .audio {
           if recorder.isRecordingAudio {
               recorder.stopAudioRecording()
           } else {
               recorder.startAudioRecording()
           }
       } else if selectedType == .video {
           if recorder.isRecordingVideo {
               recorder.stopVideoRecording()
           } else {
               // 显示相机预览
               showingCamera = true
           }
       }
   }) {
       Image(systemName: recorder.isRecordingAudio || recorder.isRecordingVideo ? "stop.fill" : 
             selectedType == .audio ? "mic.fill" : "video.fill")
   }
   ```

4. **添加权限描述到 Info.plist**
   ```xml
   <key>NSMicrophoneUsageDescription</key>
   <string>需要麦克风权限用于录制语音时光胶囊。</string>
   <key>NSCameraUsageDescription</key>
   <string>需要摄像头权限用于录制视频时光胶囊。</string>
   ```

### 权限状态

✅ Info.plist 已包含：
```xml
<key>NSMicrophoneUsageDescription</key>
<string>需要麦克风权限用于录制语音时光胶囊。</string>
<key>NSCameraUsageDescription</key>
<string>需要摄像头权限用于录制视频时光胶囊。</string>
```

---

## 🎯 实施优先级

### 高优先级（立即实施）

1. ✅ **扫码绑定家人**
   - 代码已 100% 完成
   - 只需在 Xcode 中添加 Package
   - 实施时间：5 分钟

### 中优先级（本周实施）

2. 🎤 **语音时光胶囊**
   - UI 已 100% 完成
   - 需要实现录音功能
   - 实施时间：2-3 小时

### 低优先级（下周实施）

3. 🎥 **视频时光胶囊**
   - UI 已 100% 完成
   - 需要实现录像功能
   - 实施时间：4-6 小时

---

## 📋 检查清单

### 扫码绑定家人
- [x] 代码已预留
- [x] 导入语句已添加
- [x] UI 按钮已添加
- [x] 扫描处理函数已实现
- [ ] Xcode Package 配置
- [ ] 真机测试（需要相机权限）

### 语音时光胶囊
- [x] UI 已预留
- [x] 类型选择已添加
- [x] 权限描述已添加
- [ ] 录音功能实现
- [ ] 音频播放器集成
- [ ] 真机测试

### 视频时光胶囊
- [x] UI 已预留
- [x] 类型选择已添加
- [x] 权限描述已添加
- [ ] 录像功能实现
- [ ] 视频播放器集成
- [ ] 真机测试

---

## 🚀 快速启用扫码功能

**最简单的方法**:

1. 打开 Xcode
   ```bash
   open /Users/lishimin/Documents/zhonghuo-app/终活.xcodeproj
   ```

2. 添加 Package
   - File → Add Package Dependencies...
   - 输入：`https://github.com/twostraws/CodeScanner.git`
   - 版本：2.0.0+
   - 勾选 **终活**

3. 取消注释（4 处）
   - BindFamilyView.swift: `import CodeScanner`
   - BindFamilyView.swift: 扫码按钮
   - BindFamilyView.swift: sheet
   - BindFamilyView.swift: handleScan 函数
   - FamilyGuardView.swift: `import CodeScanner`

4. 编译运行
   ```bash
   xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
   ```

---

## 📝 注意事项

### 扫码功能
- ⚠️ 模拟器无法测试（没有相机）
- ⚠️ 真机测试需要相机权限
- ✅ 仍可通过输入邀请码绑定家人

### 音视频录制
- ⚠️ 模拟器无法测试（没有麦克风/摄像头）
- ⚠️ 真机测试需要相应权限
- ✅ 文字胶囊功能不受影响

---

## 🎉 总结

**扫码绑定家人**:
- 代码完成度：100%
- 启用难度：⭐（非常简单）
- 预计时间：5 分钟

**语音/视频时光胶囊**:
- UI 完成度：100%
- 功能实现：待开发
- 预计时间：6-10 小时

**建议优先启用扫码功能**，录音/录像功能可以后续逐步实现。

---

*更新时间：2026-03-20 18:55*  
*下次更新：功能启用后*
