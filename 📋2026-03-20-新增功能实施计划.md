# 📋 2026-03-20 新增功能实施计划

**创建时间**: 2026-03-20 16:40  
**优先级**: 高

---

## 🎯 需求分析

### 1. 后端可查看用户紧急联系人具体信息 ✅

**状态**: 已完成
- ✅ 后端已有 `admin/emergency_contacts.php` 管理页面
- ✅ 可查看联系人姓名、电话、关系、所属用户等信息
- ✅ 支持搜索和导出 CSV

**访问**: `http://8.136.41.211:3395/admin/emergency_contacts.php`

---

### 2. 前端设置内增加静默开关 ⏳

**需求**: 
- 设置页面增加"静默模式"开关
- 打开后，App 任何情况下都不再推送签到通知
- 需要持久化保存设置

**实施方案**:
1. `SettingsView.swift` - 增加开关 UI
2. `UserDefaults` - 保存设置（key: `silentMode`）
3. `NotificationManager.swift` - 发送通知前检查静默状态
4. `DataManager.swift` - 添加静默状态检查方法

**修改文件**:
- `SettingsView.swift` - 新增开关
- `NotificationManager.swift` - 添加静默检查
- `DataManager.swift` - 添加静默状态方法

**预计工作量**: 30 分钟

---

### 3. 前端实时获取手机运动步数、电量等信息 ⏳

**需求**:
- 设置页面显示：
  - 实时运动步数
  - 手机电量
  - 充电状态（未充电/充电中/已充满）
- 需要实时刷新

**实施方案**:
1. `SettingsView.swift` - 增加显示区域
2. `DeviceMonitor.swift` - 新建监控类
   - 使用 `CMPedometer` 获取步数
   - 使用 `UIDevice` 获取电量
   - 使用 `UIDevice.batteryState` 获取充电状态
3. 定时器刷新（每 5 秒）

**修改文件**:
- `SettingsView.swift` - 新增显示区域
- 新建 `DeviceMonitor.swift` - 设备监控类

**预计工作量**: 1 小时

---

### 4. 家人守护功能 ⏳

**需求**:
- 每个用户自动生成二维码和邀请码
- 其他用户可扫码或输入邀请码绑定家人
- 绑定后可查看：
  - 设备电量
  - 运动步数
  - 实时位置
  - 其他信息

**实施方案**:

#### 后端实现
1. **数据库表**:
   ```sql
   -- 家人关系表
   CREATE TABLE family_relations (
     id VARCHAR(36) PRIMARY KEY,
     inviter_id VARCHAR(36) NOT NULL,      -- 邀请人
     invitee_id VARCHAR(36) NOT NULL,      -- 被邀请人
     invite_code VARCHAR(10) NOT NULL,     -- 邀请码
     status TINYINT DEFAULT 1,             -- 1=待接受，2=已接受，3=已拒绝
     created_at DATETIME,
     updated_at DATETIME
   );
   
   -- 用户设备信息表（可选缓存）
   CREATE TABLE user_devices (
     user_id VARCHAR(36) PRIMARY KEY,
     step_count INT DEFAULT 0,
     battery_level FLOAT DEFAULT 0,
     battery_state TINYINT DEFAULT 0,      -- 0=未知，1=未充电，2=充电中，3=充满
     last_update DATETIME
   );
   ```

2. **API 接口**:
   - `api/family.php` - 家人关系管理
     - `get_invite_code` - 获取我的邀请码
     - `get_qr_code` - 获取二维码（生成 URL）
     - `bind_family` - 绑定家人（输入邀请码）
     - `list_family` - 我的家人列表
     - `accept_invite` - 接受邀请
     - `reject_invite` - 拒绝邀请
     - `remove_family` - 解除关系
   
   - `api/device_info.php` - 设备信息同步
     - `upload` - 上传设备信息（步数、电量等）
     - `get` - 获取家人设备信息

3. **管理页面**:
   - `admin/family.php` - 家人关系管理

#### 前端实现
1. **数据模型**:
   - `FamilyMember.swift` - 家人数据模型
   - `DeviceInfo.swift` - 设备信息模型

2. **UI 页面**:
   - `SettingsView.swift` - 增加"家人守护"入口
   - `FamilyGuardView.swift` - 家人守护主页
   - `FamilyMemberListView.swift` - 家人列表
   - `InviteCodeView.swift` - 邀请码页面（二维码 + 邀请码）
   - `BindFamilyView.swift` - 绑定家人页面（输入邀请码）
   - `FamilyMemberDetailView.swift` - 家人详情页（显示设备信息）

3. **数据管理**:
   - `DataManager.swift` - 家人关系管理方法
   - `DeviceMonitor.swift` - 设备信息采集和上传

4. **功能流程**:
   ```
   用户 A 生成邀请码
   ↓
   用户 B 扫码或输入邀请码
   ↓
   用户 A 收到邀请通知
   ↓
   用户 A 接受邀请
   ↓
   绑定成功
   ↓
   用户 B 可查看用户 A 的设备信息
   ```

**修改文件**:
- 新建 `Models/FamilyMember.swift`
- 新建 `Models/DeviceInfo.swift`
- 新建 `Views/FamilyGuardView.swift`
- 新建 `Views/InviteCodeView.swift`
- 新建 `Views/BindFamilyView.swift`
- 新建 `Views/FamilyMemberDetailView.swift`
- `DataManager.swift` - 家人关系方法
- `SettingsView.swift` - 增加入口

**预计工作量**: 3-4 小时

---

## 📅 实施顺序

1. ✅ **紧急联系人管理页面** - 已完成
2. ⏳ **静默开关** - 30 分钟
3. ⏳ **设备信息监控** - 1 小时
4. ⏳ **家人守护功能** - 3-4 小时

**总预计时间**: 5 小时

---

## 🎯 详细实施步骤

### Step 1: 静默开关 (30 分钟)

1. `SettingsView.swift` 添加开关
2. `DataManager.swift` 添加静默状态方法
3. `NotificationManager.swift` 添加静默检查
4. 测试静默模式

### Step 2: 设备信息监控 (1 小时)

1. 新建 `DeviceMonitor.swift`
2. 实现步数获取（CMPedometer）
3. 实现电量监控（UIDevice）
4. `SettingsView.swift` 显示设备信息
5. 测试数据准确性

### Step 3: 家人守护功能 - 后端 (1.5 小时)

1. 数据库表结构（database.sql）
2. `api/family.php` - 家人关系 API
3. `api/device_info.php` - 设备信息 API
4. `admin/family.php` - 管理页面
5. 测试 API

### Step 4: 家人守护功能 - 前端 (2 小时)

1. 数据模型（FamilyMember, DeviceInfo）
2. 家人守护主页
3. 邀请码生成和显示
4. 绑定家人页面
5. 家人详情页
6. 设备信息同步
7. 测试完整流程

---

## 📊 数据库设计

### family_relations 表
```sql
CREATE TABLE family_relations (
    id VARCHAR(36) PRIMARY KEY,
    inviter_id VARCHAR(36) NOT NULL COMMENT '邀请人 ID',
    invitee_id VARCHAR(36) NOT NULL COMMENT '被邀请人 ID',
    invite_code VARCHAR(10) NOT NULL COMMENT '邀请码',
    status TINYINT DEFAULT 1 COMMENT '1=待接受，2=已接受，3=已拒绝',
    created_at DATETIME NOT NULL,
    updated_at DATETIME NOT NULL,
    INDEX idx_inviter (inviter_id),
    INDEX idx_invitee (invitee_id),
    INDEX idx_invite_code (invite_code)
);
```

### user_devices 表（可选）
```sql
CREATE TABLE user_devices (
    user_id VARCHAR(36) PRIMARY KEY,
    step_count INT DEFAULT 0 COMMENT '步数',
    battery_level FLOAT DEFAULT 0 COMMENT '电量百分比',
    battery_state TINYINT DEFAULT 0 COMMENT '0=未知，1=未充电，2=充电中，3=充满',
    location_latitude DECIMAL(10, 8) COMMENT '纬度',
    location_longitude DECIMAL(11, 8) COMMENT '经度',
    last_update DATETIME NOT NULL,
    INDEX idx_last_update (last_update)
);
```

---

## 🔐 安全和隐私

1. **家人关系验证**:
   - 只有绑定成功后才能查看信息
   - 支持单向绑定（A 可查看 B，B 不一定能查看 A）
   - 支持双向绑定（互相查看）

2. **数据权限**:
   - 用户可随时解除家人关系
   - 用户可设置可见信息范围（电量/步数/位置）
   - 敏感信息（位置）需要额外授权

3. **邀请码安全**:
   - 6 位随机码（数字 + 大写字母）
   - 有效期 7 天
   - 每个用户只有一个有效邀请码

---

## 📱 UI 设计

### 设置页面新增
```
设置
├── 账号管理
├── 通知设置
│   └── 静默模式 [开关]
├── 家人守护 >
├── 设备信息
│   ├── 步数：8,520 步
│   ├── 电量：85% 🔋
│   └── 状态：充电中
└── 关于我们
```

### 家人守护页面
```
家人守护
├── 我的家人 [3]
│   ├── 张三（父亲）- 在线
│   ├── 李四（母亲）- 在线
│   └── 王五（儿子）- 离线
├── 绑定家人 [+]
└── 我的邀请码 [二维码]
```

### 家人详情页
```
张三（父亲）
├── 设备电量：85% 🔋 充电中
├── 今日步数：8,520 步
├── 实时位置：北京市朝阳区...
└── 最后更新：刚刚
```

---

## ✅ 验收标准

1. **静默开关**:
   - ✅ 开关可切换
   - ✅ 设置持久化
   - ✅ 静默模式下无签到通知

2. **设备信息**:
   - ✅ 步数准确（与系统健康数据一致）
   - ✅ 电量准确（与状态栏一致）
   - ✅ 充电状态正确
   - ✅ 实时刷新（5 秒间隔）

3. **家人守护**:
   - ✅ 可生成邀请码和二维码
   - ✅ 可扫码绑定
   - ✅ 可输入邀请码绑定
   - ✅ 绑定后可查看设备信息
   - ✅ 可解除绑定
   - ✅ 家人列表实时更新

---

*计划创建时间：2026-03-20 16:40*
