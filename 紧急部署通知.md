# 🚨 紧急部署通知

**状态**：⚠️ 服务器代码未更新  
**时间**：2026-03-17 19:14

---

## 🔴 问题

**API 返回错误**：
```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试","password":"test123"}'
```

**当前响应**（错误）：
```json
{
  "error": "注册失败，请稍后重试",
  "code": "REGISTER_FAILED"
}
```

**原因**：服务器上的代码还是旧版本，需要从 GitHub 拉取最新修复代码

---

## 🚀 立即部署（必须执行）

### 方式 1：完全重新部署（推荐）

```bash
# === 1. SSH 登录服务器 ===
ssh root@8.136.41.211
# 密码：wangzi1314 (如果提示)

# === 2. 进入网站根目录 ===
cd /www/wwwroot/zhonghuo.cn

# === 3. 删除旧后端 ===
rm -rf backend

# === 4. 重新克隆 ===
git clone https://github.com/huainvhai-bit/zhonghuo-backend-php.git backend
cd backend

# === 5. 设置权限 ===
chown -R www:www .
chmod 644 api/users.php
chmod 644 config.php

# === 6. 验证文件大小 ===
ls -lh api/users.php
# 应该显示：-rwxr-xr-x 1 www www 7.0K ...
# 如果是 1.8K，说明还是旧版本！

# === 7. 测试 API ===
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"部署测试","password":"test123"}'
```

**✅ 正确响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "部署测试",
      "phone": "13800138099"
    }
  }
}
```

---

### 方式 2：Git 更新（如果已有 backend 目录）

```bash
# === 1. SSH 登录 ===
ssh root@8.136.41.211

# === 2. 进入后端目录 ===
cd /www/wwwroot/zhonghuo.cn/backend

# === 3. 拉取最新代码 ===
git pull origin main

# === 4. 验证 ===
ls -lh api/users.php
# 应该显示约 7KB

# === 5. 重启 PHP-FPM（可选） ===
systemctl restart php-fpm

# === 6. 测试 API ===
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"更新测试","password":"test123"}'
```

---

## 🧪 验证部署成功

### 测试 1：注册 API

```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "phone": "13800138099",
    "name": "验证测试",
    "password": "test123"
  }'
```

**必须返回包含 `user.id` 的 JSON**

### 测试 2：登录 API

```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138006",
    "password": "test123456"
  }'
```

### 测试 3：查看数据库

```bash
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db \
  -e "SELECT id, name, phone, created_at FROM users ORDER BY created_at DESC LIMIT 5;"
```

### 测试 4：访问 admin 后台

浏览器访问：http://8.136.41.211:3395/admin/

---

## 📋 部署检查清单

部署完成后请逐项确认：

- [ ] `api/users.php` 文件大小约 **7KB**（不是 1.8KB）
- [ ] 注册 API 返回包含 `"user": { "id": "...", ... }`
- [ ] 登录 API 返回包含 `"user": { "id": "...", ... }`
- [ ] 数据库中可以看到新用户
- [ ] admin 后台可以正常访问
- [ ] 前端 App 注册成功
- [ ] 前端 App 登录成功

---

## 🔍 故障排查

### 问题 1：文件大小不对

```bash
# 检查文件大小
ls -lh /www/wwwroot/zhonghuo.cn/backend/api/users.php

# 如果显示 1-2KB，说明是旧版本
# 重新拉取代码
cd /www/wwwroot/zhonghuo.cn/backend
git pull origin main
```

### 问题 2：数据库连接失败

```bash
# 检查数据库是否存在
mysql -u root -p -e "SHOW DATABASES LIKE 'zhonghuo_db';"

# 如果不存在，运行安装脚本
mysql -u root -p < /www/wwwroot/zhonghuo.cn/backend/install/schema.sql
```

### 问题 3：权限错误

```bash
# 设置正确权限
cd /www/wwwroot/zhonghuo.cn/backend
chown -R www:www .
chmod 644 api/*.php
chmod 644 config.php
```

---

## 📞 部署完成后

1. **确认 API 正常**：运行上面的测试命令
2. **测试模拟器**：在 iOS 模拟器中测试注册和登录
3. **查看日志**：Console.app 查看 App 日志

---

**⚠️ 重要**：部署完成前，模拟器测试会失败！

**请立即执行部署步骤！** 🚀

---

*创建时间：2026-03-17 19:14*
