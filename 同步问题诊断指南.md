# 同步问题诊断指南

**日期**: 2026-03-19 14:05  
**问题**: 前端数据无法同步到后端服务器  
**状态**: 已添加详细日志，需要用户测试并提供日志

---

## 后端验证

**✅ 后端 API 正常工作** - 已通过 curl 测试验证：

```bash
# 测试遗嘱同步 API
curl -X POST "http://8.136.41.211:3395/api/will.php?action=batch_sync" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"token": "<token>", "wills": [...]}'

# 响应
{
    "success": true,
    "message": "同步成功",
    "data": {
        "total": 1,
        "created": 1,
        "updated": 0
    }
}
```

**结论**: 后端 API 完全正常，问题在前端！

---

## 前端测试步骤

### 1. 打开 Xcode 控制台
```
Cmd + Shift + Y
```

### 2. 登录账号
- 手机号：`13800138006`
- 密码：`test123456`

### 3. 编辑遗嘱模块
- 底部导航 → 嘱托与资产
- 点击任意模块（如"财产分配"）
- 填写内容
- 点击"完成"保存

### 4. 查看控制台日志

**应该看到以下日志**：

```
📜 遗嘱模块已更新到本地，准备同步到服务器...
📊 当前 willModules.count: 10
📊 当前模块内容：财产分配 - 完成：true
🔑 检查 Token: eyJhbGciOiJIUzI1NiIsIn...
🔄 开始执行 batchSyncWills()...
🔄 开始同步遗嘱：共 10 个模块
🌐 API URL: http://8.136.41.211:3395/api
🔑 Token 长度：217
📍 请求 URL: http://8.136.41.211:3395/api/will.php?action=batch_sync
📋 Headers: Content-Type=application/json, Authorization=Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
📤 遗嘱同步请求：10 个模块
📦 请求体：{"wills":[{"id":"...","type":"财产分配","title":"财产分配",...
📡 遗嘱同步响应状态码：200
📄 遗嘱同步响应：{"success":true,"message":"同步成功","data":{"total":10,"created":10,"updated":0}}
✅ 遗嘱同步成功：总计 10, 新增 10, 更新 0
✅ 遗嘱同步成功：总计 10 个，创建 10 个，更新 0 个
```

---

## 可能的错误日志

### 错误 1: 无 Token
```
⚠️ 遗嘱同步失败：无 token
```
**原因**: 用户未登录或 token 未保存  
**解决**: 重新登录

### 错误 2: API URL 为空
```
⚠️ 遗嘱同步失败：API URL 为空
```
**原因**: DataManager.apiURL 未初始化  
**解决**: 检查 App 启动时是否获取了配置

### 错误 3: 网络错误
```
❌ 遗嘱同步失败：The operation couldn't be completed. (NSURLErrorDomain error -1009.)
```
**原因**: 无网络连接  
**解决**: 检查网络

### 错误 4: Token 无效
```
📡 遗嘱同步响应状态码：401
📄 遗嘱同步响应：{"error":"Token 无效","code":"INVALID_TOKEN"}
```
**原因**: Token 过期或格式错误  
**解决**: 重新登录

### 错误 5: 无数据需要同步
```
ℹ️ 遗嘱同步：无数据需要同步
```
**原因**: willModules 数组为空  
**解决**: 检查是否成功加载了遗嘱模块

---

## 关键检查点

### ✅ Token 是否正确
日志中应该看到：
```
🔑 检查 Token: eyJhbGciOiJIUzI1NiIsIn...
🔑 Token 长度：217
```

如果 Token 是 `nil` 或长度不对，说明登录有问题。

### ✅ API URL 是否正确
日志中应该看到：
```
🌐 API URL: http://8.136.41.211:3395/api
📍 请求 URL: http://8.136.41.211:3395/api/will.php?action=batch_sync
```

如果 URL 不对，说明配置有问题。

### ✅ 请求是否发送
日志中应该看到：
```
📤 遗嘱同步请求：10 个模块
📦 请求体：{"wills":[...]}
```

如果没有看到，说明请求没有发送。

### ✅ 响应是否收到
日志中应该看到：
```
📡 遗嘱同步响应状态码：200
📄 遗嘱同步响应：{"success":true,...}
```

如果没有看到，说明请求被防火墙拦截或服务器无响应。

### ✅ 同步是否成功
日志中应该看到：
```
✅ 遗嘱同步成功：总计 10, 新增 10, 更新 0
```

如果看到失败信息，查看具体错误。

---

## 请提供的信息

测试后，请截图或复制以下日志：

1. **登录日志**：
   ```
   🔵 登录成功
   🔑 Token: ...
   ```

2. **编辑遗嘱后的日志**：
   ```
   📜 遗嘱模块已更新到本地...
   🔄 开始执行 batchSyncWills()...
   ...
   ```

3. **完整的同步过程日志**（从开始到结束）

4. **后端诊断页面**：
   访问 `http://8.136.41.211:3395/api/diagnose-all.php`
   截图显示各表的记录数

---

## 预期结果

**如果一切正常**：
- ✅ 看到完整的同步日志
- ✅ 后端 `will_modules` 表应该有数据（10 条）
- ✅ 重启 App 后数据还在

**如果仍然失败**：
- ❌ 后端表仍然是 0 条
- ❌ 日志中有错误信息

请提供完整日志，我会根据日志进一步诊断！

---

## 临时解决方案

如果同步仍然失败，可以手动测试：

1. **登录获取 Token**（通过 curl 或 Postman）
2. **手动调用同步 API**
3. **验证后端数据**

这样可以确认是前端代码问题还是网络/配置问题。

---

**下一步**: 请按照上述步骤测试，并提供完整的 Xcode 控制台日志！📋
