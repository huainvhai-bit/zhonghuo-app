# 📝 代码推送规范

**重要**: 前端和后端每次修改代码后，必须推送到各自的 GitHub 仓库！

---

## 📱 前端代码推送（终活 App）

**仓库**: https://github.com/huainvhai-bit/zhonghuo-app  
**位置**: `/Users/lishimin/Documents/zhonghuo-app/`

### 推送步骤

```bash
cd /Users/lishimin/Documents/zhonghuo-app

# 1. 查看修改
git status

# 2. 添加所有修改
git add -A

# 3. 提交（写清楚修改内容）
git commit -m "🔧 修复 XXX 问题

- 修改文件 1
- 修改文件 2
- 具体修复内容"

# 4. 推送到 GitHub
git push origin main
```

### 示例

```bash
git commit -m "🔧 修复登录模块 3 个关键问题

1. 注册验证码必填 - 确保手机号是本人的
2. 登录网络错误 - 添加网络测试和改进错误处理
3. 退出登录无反应 - 添加@MainActor 和清除 UserDefaults

修改文件:
- AuthView.swift: 恢复验证码验证，添加调试日志
- SettingsView.swift: 退出登录时清除所有用户数据
- UserManager.swift: logout() 添加@MainActor 确保 UI 刷新"

git push origin main
```

---

## 🖥️ 后端代码推送（API 服务）

**仓库**: https://github.com/huainvhai-bit/zhonghuo-backend  
**位置**: `8.136.41.211:/www/wwwroot/zhonghuo.cn/`

### 推送步骤

```bash
# 1. SSH 登录服务器
ssh root@8.136.41.211
# 密码：Wangzi1314

# 2. 进入项目目录
cd /www/wwwroot/zhonghuo.cn

# 3. 查看修改
git status

# 4. 添加所有修改
git add -A

# 5. 提交
git commit -m "🔧 修复 XXX 问题

- 修改文件：api/users.php
- 具体修复内容"

# 6. 推送到 GitHub
git push origin main
# 输入 GitHub token 或使用 SSH key
```

### 快速推送脚本

创建脚本 `/root/push-backend.sh`:

```bash
#!/bin/bash
cd /www/wwwroot/zhonghuo.cn
git add -A
git commit -m "🔧 自动提交后端修改"
git push origin main
```

使用方法：
```bash
chmod +x /root/push-backend.sh
/root/push-backend.sh
```

---

## ✅ 推送检查清单

每次修改后，检查：

- [ ] 前端代码已推送到 zhonghuo-app 仓库
- [ ] 后端代码已推送到 zhonghuo-backend 仓库
- [ ] Commit message 清晰描述修改内容
- [ ] 推送成功（GitHub 页面能看到最新 commit）

---

## 🔧 后端 Git 初始化（仅需一次）

如果后端仓库还未初始化：

```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 进入项目目录
cd /www/wwwroot/zhonghuo.cn

# 初始化 git
git config --global --add safe.directory /www/wwwroot/zhonghuo.cn
git init
git config user.email "lishimin@example.com"
git config user.name "李世民的服务器"

# 添加远程仓库
git remote add origin https://github.com/huainvhai-bit/zhonghuo-backend.git

# 首次推送
git add -A
git commit -m "📦 初始提交后端代码"
git branch -M main
git push -u origin main
```

---

## 📊 最近推送记录

### 前端 (zhonghuo-app)
- ✅ 2026-03-17 11:33 - commit: 465d2c2 - 修复登录模块 3 个关键问题
- ✅ 2026-03-17 11:24 - commit: ea6f60f - 彻底修复登录模块所有问题
- ✅ 2026-03-17 11:22 - commit: 89df52d - 清理备份文件

### 后端 (zhonghuo-backend)
- ⏳ 待初始化推送

---

**记住**: 修改代码 → 测试 → 推送 GitHub ✅
