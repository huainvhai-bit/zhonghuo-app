# 📝 代码推送规范

**重要**: 前端和后端每次修改代码后，必须推送到各自的 GitHub 仓库！

---

## 📱 前端代码推送（终活 App）

**仓库**: https://github.com/huainvhai-bit/zhonghuo-app  
**位置**: `/Users/lishimin/Documents/zhonghuo-app/`

### 推送步骤

```bash
cd /Users/lishimin/Documents/zhonghuo-app

# 1. 查看修改
git status

# 2. 添加所有修改
git add -A

# 3. 提交（写清楚修改内容）
git commit -m "🔧 修复 XXX 问题

- 修改文件 1
- 修改文件 2
- 具体修复内容"

# 4. 推送到 GitHub
git push origin main
```

### 示例

```bash
git commit -m "🔧 修复登录模块 3 个关键问题

1. 注册验证码必填 - 确保手机号是本人的
2. 登录网络错误 - 添加网络测试和改进错误处理
3. 退出登录无反应 - 添加@MainActor 和清除 UserDefaults

修改文件:
- AuthView.swift: 恢复验证码验证，添加调试日志
- SettingsView.swift: 退出登录时清除所有用户数据
- UserManager.swift: logout() 添加@MainActor 确保 UI 刷新"

git push origin main
```

---

## 🖥️ 后端代码推送（API 服务）

**仓库**: https://github.com/huainvhai-bit/zhonghuo-backend  
**位置**: `8.136.41.211:/www/wwwroot/zhonghuo.cn/`

### 推送步骤

**方法 1: 手动推送（推荐）**

```bash
# 1. SSH 登录服务器
ssh root@8.136.41.211
# 密码：Wangzi1314

# 2. 进入项目目录
cd /www/wwwroot/zhonghuo.cn

# 3. 查看修改
git status

# 4. 添加所有修改
git add -A

# 5. 提交
git commit -m "🔧 修复 XXX 问题

- 修改文件：api/users.php
- 具体修复内容"

# 6. 推送到 GitHub
git push origin main
# 输入 GitHub username: huainvhai-bit
# 输入 GitHub token: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**方法 2: 使用本地脚本推送**

在本地创建脚本 `push-backend.sh`：

```bash
#!/bin/bash
# 用法：./push-backend.sh "提交信息"

MESSAGE="${1:-🔧 后端代码更新}"

expect << ENDSCRIPT
set timeout 60
spawn ssh root@8.136.41.211
expect {
    "password:" { send "Wangzi1314\r"; expect "#*" }
    timeout { exit 1 }
}

send "cd /www/wwwroot/zhonghuo.cn\n"
expect "#*"
send "git add -A && git commit -m \"$MESSAGE\"\n"
expect "#*"
send "git push origin main\n"
expect {
    "Username for" { send "huainvhai-bit\r"; expect "Password for" }
    "Everything up-to-date" { }
    timeout { exit 1 }
}
send "YOUR_GITHUB_TOKEN_HERE\r"  # 替换为你的 token
expect "#*"
send "exit\r"
expect eof
ENDSCRIPT
```

**方法 3: 配置 SSH Key（一劳永逸）**

1. 在 GitHub 添加 SSH Key: https://github.com/settings/keys
2. 在服务器生成 SSH Key:
   ```bash
   ssh-keygen -t ed25519 -C "zhonghuo-backend"
   cat ~/.ssh/id_ed25519.pub
   # 复制到 GitHub
   ```
3. 修改远程仓库为 SSH:
   ```bash
   cd /www/wwwroot/zhonghuo.cn
   git remote set-url origin git@github.com:huainvhai-bit/zhonghuo-backend.git
   git push -u origin main
   ```

### 快速推送脚本

创建脚本 `/root/push-backend.sh`:

```bash
#!/bin/bash
cd /www/wwwroot/zhonghuo.cn
git add -A
git commit -m "🔧 自动提交后端修改"
git push origin main
```

使用方法：
```bash
chmod +x /root/push-backend.sh
/root/push-backend.sh
```

---

## ✅ 推送检查清单

每次修改后，检查：

- [ ] 前端代码已推送到 zhonghuo-app 仓库
- [ ] 后端代码已推送到 zhonghuo-backend 仓库
- [ ] Commit message 清晰描述修改内容
- [ ] 推送成功（GitHub 页面能看到最新 commit）

---

## 🔧 后端 Git 初始化（仅需一次）

如果后端仓库还未初始化：

```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 进入项目目录
cd /www/wwwroot/zhonghuo.cn

# 初始化 git
git config --global --add safe.directory /www/wwwroot/zhonghuo.cn
git init
git config user.email "lishimin@example.com"
git config user.name "李世民的服务器"

# 添加远程仓库
git remote add origin https://github.com/huainvhai-bit/zhonghuo-backend.git

# 首次推送
git add -A
git commit -m "📦 初始提交后端代码"
git branch -M main
git push -u origin main
```

---

## 📊 最近推送记录

### 前端 (zhonghuo-app)
- ✅ 2026-03-17 11:33 - commit: 465d2c2 - 修复登录模块 3 个关键问题
- ✅ 2026-03-17 11:24 - commit: ea6f60f - 彻底修复登录模块所有问题
- ✅ 2026-03-17 11:22 - commit: 89df52d - 清理备份文件

### 后端 (zhonghuo-backend)
- ✅ 2026-03-17 11:38 - commit: 3fd0efd - 🔧 登录模块修复（已提交，待推送）
- ⏳ 需要手动推送（需要 GitHub token）

---

**记住**: 修改代码 → 测试 → 推送 GitHub ✅
