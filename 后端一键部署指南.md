# 🚀 终活 App 后端 - 一键部署指南

**特点**：首次访问自动初始化数据库，无需手动执行任何命令！

---

## 📦 部署步骤

### 1️⃣ 上传代码到服务器

```bash
# SSH 登录
ssh root@8.136.41.211

# 进入目录
cd /www/wwwroot/zhonghuo.cn

# 拉取最新代码
git pull origin main
```

### 2️⃣ 设置环境变量

**在服务器上执行**：

```bash
# 编辑环境变量文件
vi /etc/profile

# 在文件末尾添加
export MYSQL_ROOT_PASSWORD="你的 MySQL root 密码"

# 保存退出后生效
source /etc/profile
```

**或者在宝塔面板中设置**：
1. 打开宝塔面板
2. 进入 PHP 管理
3. 找到 `PHP-7.4`（或你的版本）
4. 点击"配置修改"
5. 添加环境变量：`MYSQL_ROOT_PASSWORD=你的密码`
6. 保存并重启 PHP

### 3️⃣ 访问初始化页面（可选）

**浏览器访问**：
```
http://8.136.41.211:3395/api/init-db.php
```

**或者直接使用 API**：
```
http://8.136.41.211:3395/api/users.php?action=register&phone=13800138099&name=测试&password=test123&verify_code=123456
```

**首次访问会自动初始化数据库！**

---

## ✅ 自动初始化流程

### 首次 API 请求时：

1. ✅ 检测到数据库不存在
2. ✅ 自动使用 root 用户连接 MySQL
3. ✅ 创建数据库 `zhonghuo_db`
4. ✅ 创建用户 `zhonghuo_user`
5. ✅ 授权用户访问数据库
6. ✅ 创建数据表（users、sms_codes）
7. ✅ 返回 API 响应

**完全自动化，无需手动干预！**

---

## 🧪 测试 API

### 测试 1：注册

```bash
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试用户","password":"test123","verify_code":"123456"}'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099"
    }
  }
}
```

### 测试 2：登录

```bash
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13800138099","login_type":"password","password":"test123"}'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "...",
    "user_id": "user_xxx",
    "is_new": false,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099"
    }
  }
}
```

---

## 🔍 故障排查

### 问题 1：提示"未设置 MYSQL_ROOT_PASSWORD"

**错误信息**：
```
数据库未初始化，请设置环境变量 MYSQL_ROOT_PASSWORD
```

**解决**：
```bash
# 设置环境变量
export MYSQL_ROOT_PASSWORD="你的密码"

# 或者在 /etc/profile 中添加
echo 'export MYSQL_ROOT_PASSWORD="你的密码"' >> /etc/profile
source /etc/profile

# 重启 PHP-FPM
systemctl restart php-fpm-74.service
```

---

### 问题 2：MySQL root 密码错误

**错误信息**：
```
Access denied for user 'root'@'localhost'
```

**解决**：
确认设置的 `MYSQL_ROOT_PASSWORD` 环境变量与实际的 MySQL root 密码一致。

---

### 问题 3：数据库已存在但用户不存在

**解决**：
访问初始化页面会自动处理：
```
http://8.136.41.211:3395/api/init-db.php
```

---

## 📊 数据库配置

| 配置项 | 值 |
|--------|-----|
| 主机 | localhost |
| 数据库名 | zhonghuo_db |
| 用户名 | zhonghuo_user |
| 密码 | Zhonghuo@2026 |
| 字符集 | utf8mb4 |
| 排序规则 | utf8mb4_unicode_ci |

---

## 🔐 安全建议

### 1. 初始化完成后删除 init-db.php

```bash
cd /www/wwwroot/zhonghuo.cn/api
rm init-db.php
rm init-db-auto.php
```

**或者限制访问**：

在 nginx 配置中添加：
```nginx
location ~ ^/api/init-db.*\.php$ {
    deny all;
    return 403;
}
```

### 2. 使用强密码

修改 `config.php` 中的默认密码：
```php
$GLOBALS['db_pass'] = '更强的密码';
```

### 3. 设置文件权限

```bash
chown -R www:www /www/wwwroot/zhonghuo.cn
chmod -R 755 /www/wwwroot/zhonghuo.cn/api
chmod 644 /www/wwwroot/zhonghuo.cn/config.php
```

---

## 📝 迁移服务器

**迁移到新服务器时**：

1. ✅ 上传代码（git pull）
2. ✅ 设置 `MYSQL_ROOT_PASSWORD` 环境变量
3. ✅ 访问 API（自动初始化）

**不需要手动执行任何数据库命令！**

---

## 🎯 总结

**传统部署**：
1. ❌ 手动创建数据库
2. ❌ 手动创建用户
3. ❌ 手动创建表
4. ❌ 手动授权
5. ❌ 容易出错

**新部署方式**：
1. ✅ 上传代码
2. ✅ 设置环境变量
3. ✅ 访问 API（自动完成所有操作）

**简单、安全、可靠！**

---

*部署完成后，访问 http://8.136.41.211:3395/api/check-config.php 验证配置！*
