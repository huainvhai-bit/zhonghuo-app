# 终活 App 后端问题诊断报告

## 🔍 问题总结

**用户报告**：前端注册了用户，但后端 admin 不显示

---

## 📊 审查结果

### 问题 1：API 文件错误 ⚠️ **严重**

**当前文件**：`api/users.php` (1.8KB)
- ❌ 这是**测试文件**，只返回模拟数据
- ❌ **没有连接数据库**
- ❌ 注册和登录都是硬编码响应

**正确文件**：`api/users.php.backup` (7KB)
- ✅ 完整的数据库操作
- ✅ 真正的用户注册和登录
- ✅ JWT Token 生成

**解决方案**：
```bash
cd /www/wwwroot/zhonghuo.cn/backend/api
mv users.php users.php.test
mv users.php.backup users.php
```

---

### 问题 2：数据库名称不一致 ⚠️ **严重**

**前端配置**：
```swift
// DataManager.swift
static var apiURL = "http://8.136.41.211:3395/api"
```

**后端配置** (`config.php`)：
```php
$GLOBALS['db_name'] = 'zhonghuo';  // 数据库名
```

**数据库初始化脚本** (`install/schema.sql`)：
```sql
CREATE DATABASE IF NOT EXISTS zhonghuo_db;  -- 数据库名是 zhonghuo_db
```

**问题**：
- 配置文件写的是 `zhonghuo`
- 初始化脚本创建的是 `zhonghuo_db`
- 导致后端连接不到数据库

**解决方案**：
```php
// 修改 config.php
$GLOBALS['db_name'] = 'zhonghuo_db';  // 改为 zhonghuo_db
```

---

### 问题 3：数据库用户权限 ⚠️ **严重**

**当前配置**：
```php
$GLOBALS['db_user'] = 'zhonghuo';
$GLOBALS['db_pass'] = 'wangzi1314';
```

**初始化脚本** (`install/schema.sql`)：
```sql
CREATE USER IF NOT EXISTS 'zhonghuo_user'@'localhost' IDENTIFIED BY 'Zhonghuo@2026';
```

**问题**：
- 配置文件用户：`zhonghuo` / `wangzi1314`
- 初始化脚本用户：`zhonghuo_user` / `Zhonghuo@2026`
- 用户不匹配，导致无法连接数据库

**解决方案**：
```php
// 修改 config.php
$GLOBALS['db_user'] = 'zhonghuo_user';
$GLOBALS['db_pass'] = 'Zhonghuo@2026';
```

---

### 问题 4：表名不一致 ⚠️ **中等**

**后端代码期望的表**：
- `users` - 用户表
- `capsules` - 时光胶囊
- `wills` - 遗嘱
- `check_ins` - 签到记录
- `sms_codes` - 验证码

**初始化脚本创建的表**：
- `users` ✅
- `capsules` ✅
- `will_templates` ❌ (代码期望 `wills`)
- `check_in_records` ❌ (代码期望 `check_ins`)
- `sms_codes` ✅

**解决方案**：修改初始化脚本，统一表名

---

### 问题 5：users.php 缺少 user 字段 ⚠️ **严重**

**前端期望的响应** (`AuthView.swift`)：
```swift
// 登录成功处理
let userData = data["user"] as? [String: Any] ?? [:]
let userId = userData["id"] as? String ?? data["user_id"] as? String ?? ""
let name = userData["name"] as? String ?? ""
```

**当前 users.php 返回**：
```php
success([
    'token' => 'test_token_' . time(),
    'user_id' => 'test_' . md5($phone),
    'user' => [
        'name' => '测试用户',  // 缺少 id 字段
        'phone' => $phone
    ]
]);
```

**users.php.backup 返回**：
```php
success([
    'token' => $token,
    'user_id' => $userId,
    'is_new' => true,
    'user' => [
        'id' => $userId,      // ✅ 包含 id
        'name' => $name,
        'phone' => $phone
    ]
]);
```

---

### 问题 6：admin 后台数据库查询错误 ⚠️ **中等**

**admin/users.php 查询**：
```sql
SELECT 
    u.*,
    (SELECT COUNT(*) FROM check_ins ci WHERE ci.user_id = u.id) as checkin_count,
    (SELECT MAX(created_at) FROM check_ins ci WHERE ci.user_id = u.id) as last_checkin_time,
    (SELECT COUNT(*) FROM capsules c WHERE c.user_id = u.id) as capsule_count,
    (SELECT COUNT(*) FROM wills w WHERE w.user_id = u.id) as will_count
FROM users u
```

**问题**：
- 如果表名是 `check_in_records` 而不是 `check_ins`，查询会失败
- 如果表名是 `will_templates` 而不是 `wills`，查询会失败

---

## 🔧 完整修复方案

### 步骤 1：修复 API 文件

```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 进入 API 目录
cd /www/wwwroot/zhonghuo.cn/backend/api

# 替换 users.php
mv users.php users.php.test
cp users.php.backup users.php

# 验证
php -l users.php
```

### 步骤 2：修复数据库配置

```bash
# 编辑 config.php
vi /www/wwwroot/zhonghuo.cn/backend/config.php
```

修改为：
```php
$GLOBALS['db_host'] = 'localhost';
$GLOBALS['db_name'] = 'zhonghuo_db';      // 改为 zhonghuo_db
$GLOBALS['db_user'] = 'zhonghuo_user';    // 改为 zhonghuo_user
$GLOBALS['db_pass'] = 'Zhonghuo@2026';    // 改为 Zhonghuo@2026
```

### 步骤 3：修复数据库表结构

```bash
# 登录 MySQL
mysql -u root -p

# 使用数据库
USE zhonghuo_db;

# 重命名表（如果存在）
RENAME TABLE 
    will_templates TO wills,
    check_in_records TO check_ins;

# 或者重新初始化
source /www/wwwroot/zhonghuo.cn/backend/install/schema.sql;
```

### 步骤 4：验证修复

```bash
# 测试注册
curl -X POST "http://8.136.41.211:3395/backend/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "phone": "13800138009",
    "name": "测试用户 9",
    "password": "test123"
  }'

# 测试登录
curl -X POST "http://8.136.41.211:3395/backend/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138006",
    "login_type": "password",
    "password": "test123456"
  }'

# 查看数据库
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db \
  -e "SELECT id, name, phone, created_at FROM users;"
```

---

## 📋 修复清单

- [ ] 替换 `api/users.php` 为 backup 版本
- [ ] 修改 `config.php` 数据库配置
- [ ] 统一数据库名称为 `zhonghuo_db`
- [ ] 统一数据库用户为 `zhonghuo_user`
- [ ] 统一数据库密码为 `Zhonghuo@2026`
- [ ] 统一表名（`wills`, `check_ins`）
- [ ] 测试注册功能
- [ ] 测试登录功能
- [ ] 验证 admin 后台显示用户

---

## 🎯 预期结果

修复后：
1. ✅ 前端注册成功，用户保存到数据库
2. ✅ 前端登录成功，返回真实用户数据
3. ✅ admin 后台显示所有注册用户
4. ✅ admin 后台显示签到记录、最后登录时间等信息

---

*诊断时间：2026-03-17 18:35*
