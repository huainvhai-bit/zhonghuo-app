# 🔥 数据库连接失败 - 终极解决方案

**问题**：注册时提示"数据库连接失败"  
**诊断**：`Access denied for user 'zhonghuo_user'@'localhost'`  
**原因**：服务器上没有创建数据库用户

---

## 🚨 立即执行（在服务器上）

### 步骤 1：SSH 登录服务器

```bash
ssh root@8.136.41.211
```

### 步骤 2：进入后端目录

```bash
cd /www/wwwroot/zhonghuo.cn
```

### 步骤 3：拉取最新代码

```bash
git pull origin main
```

### 步骤 4：登录 MySQL

```bash
mysql -u root -p
```

**输入 MySQL root 密码后，执行以下 SQL 命令**：

```sql
-- 删除旧的（如果有）
DROP DATABASE IF EXISTS zhonghuo_db;
DROP USER IF EXISTS 'zhonghuo_user'@'localhost';

-- 创建数据库
CREATE DATABASE zhonghuo_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER 'zhonghuo_user'@'localhost' IDENTIFIED BY 'Zhonghuo@2026';

-- 授权
GRANT ALL PRIVILEGES ON zhonghuo_db.* TO 'zhonghuo_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;

-- 验证
USE zhonghuo_db;
SHOW TABLES;

-- 应该看到：
-- +---------------------+
-- | Tables_in_zhonghuo_db |
-- +---------------------+
-- | (空，因为还没有表)   |
-- +---------------------+

-- 退出
EXIT;
```

### 步骤 5：创建数据表

```bash
mysql -u root -p << 'EOF'
USE zhonghuo_db;

CREATE TABLE users (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    last_login_at DATETIME NULL,
    INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE sms_codes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    phone VARCHAR(20) NOT NULL,
    code VARCHAR(10) NOT NULL,
    action VARCHAR(20) NOT NULL,
    expires_at DATETIME NOT NULL,
    is_used TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_phone_action (phone, action)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

SHOW TABLES;
EXIT;
EOF
```

**预期输出**：
```
+---------------------+
| Tables_in_zhonghuo_db |
+---------------------+
| sms_codes           |
| users               |
+---------------------+
```

### 步骤 6：设置文件权限

```bash
chown -R www:www /www/wwwroot/zhonghuo.cn
chmod -R 755 /www/wwwroot/zhonghuo.cn/api
```

### 步骤 7：测试 API

```bash
# 测试注册
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试用户","password":"test123","verify_code":"123456"}'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099"
    }
  }
}
```

---

## ✅ 验证清单

执行完后，检查以下项目：

- [ ] SSH 登录成功
- [ ] 进入 `/www/wwwroot/zhonghuo.cn` 目录
- [ ] `git pull` 成功（最新代码）
- [ ] MySQL 登录成功
- [ ] 数据库 `zhonghuo_db` 创建成功
- [ ] 用户 `zhonghuo_user` 创建成功
- [ ] 授权成功
- [ ] 表 `users` 创建成功
- [ ] 表 `sms_codes` 创建成功
- [ ] 文件权限设置成功
- [ ] API 测试成功（返回 `success: true`）

---

## 🔍 故障排查

### 问题 1：MySQL root 密码错误

**解决**：
```bash
# 重置 MySQL root 密码
mysqld_safe --skip-grant-tables &
mysql -u root << EOF
FLUSH PRIVILEGES;
ALTER USER 'root'@'localhost' IDENTIFIED BY '新密码';
EXIT;
EOF
```

### 问题 2：git pull 失败

**解决**：
```bash
cd /www/wwwroot/zhonghuo.cn
git fetch origin
git reset --hard origin/main
```

### 问题 3：API 仍然返回数据库错误

**检查数据库用户连接**：
```bash
mysql -u zhonghuo_user -p'Zhonghuo@2026' -e "USE zhonghuo_db; SELECT '连接成功';"
```

如果失败，重新执行步骤 4。

### 问题 4：文件权限错误

**解决**：
```bash
ls -la /www/wwwroot/zhonghuo.cn/config.php
# 应该是 www:www 或 www-data:www-data
```

---

## 📊 数据库配置信息

| 配置项 | 值 |
|--------|-----|
| 主机 | localhost |
| 数据库名 | zhonghuo_db |
| 用户名 | zhonghuo_user |
| 密码 | Zhonghuo@2026 |
| 字符集 | utf8mb4 |
| 排序规则 | utf8mb4_unicode_ci |

---

## 🌐 在线诊断

**访问诊断页面**：
```
http://8.136.41.211:3395/api/test_db.php
```

**成功标志**：
- ✅ config.php 存在
- ✅ 数据库连接成功
- ✅ 数据表存在（users、sms_codes）

---

## 🎯 完成标志

修复成功后：

1. ✅ 访问 `http://8.136.41.211:3395/api/test_db.php` 显示"数据库连接成功"
2. ✅ 在手机上获取验证码（显示：123456）
3. ✅ 在手机上注册成功（自动跳转到首页）
4. ✅ 在手机上登录成功

---

## 📝 快速复制版（一键执行）

```bash
# 登录服务器
ssh root@8.136.41.211

# 进入目录
cd /www/wwwroot/zhonghuo.cn

# 拉取代码
git pull origin main

# 创建数据库和用户（需要输入 MySQL root 密码）
mysql -u root -p << 'EOF'
DROP DATABASE IF EXISTS zhonghuo_db;
DROP USER IF EXISTS 'zhonghuo_user'@'localhost';
CREATE DATABASE zhonghuo_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'zhonghuo_user'@'localhost' IDENTIFIED BY 'Zhonghuo@2026';
GRANT ALL PRIVILEGES ON zhonghuo_db.* TO 'zhonghuo_user'@'localhost';
FLUSH PRIVILEGES;
USE zhonghuo_db;
CREATE TABLE users (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
CREATE TABLE sms_codes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    phone VARCHAR(20) NOT NULL,
    code VARCHAR(10) NOT NULL,
    action VARCHAR(20) NOT NULL,
    expires_at DATETIME NOT NULL,
    is_used TINYINT DEFAULT 0,
    INDEX idx_phone_action (phone, action)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
SHOW TABLES;
EXIT;
EOF

# 设置权限
chown -R www:www .
chmod -R 755 api/

# 测试 API
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试","password":"test123","verify_code":"123456"}'
```

---

*按照以上步骤执行，保证解决问题！*
