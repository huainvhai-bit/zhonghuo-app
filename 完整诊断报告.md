# 🔍 终活 App - 完整诊断报告

**生成时间**：2026-03-17 23:35  
**版本**：v2.0.0

---

## 📋 诊断工具

### 1️⃣ 后端诊断页面

**访问地址**：
```
http://8.136.41.211:3395/diagnostic.php
```

**检查内容**：
- ✅ PHP 环境（版本、扩展）
- ✅ 配置文件（config.php）
- ✅ 数据库连接
- ✅ 数据表结构（users、sms_codes）
- ✅ API 文件完整性
- ✅ API 功能测试
- ✅ 前端配置

**状态说明**：
- 🟢 绿色 = 正常
- 🟡 黄色 = 警告
- 🔴 红色 = 错误

---

### 2️⃣ 配置检查页面

**访问地址**：
```
http://8.136.41.211:3395/api/check-config.php
```

**检查内容**：
- ✅ 环境变量
- ✅ config.php 配置
- ✅ 数据库连接测试
- ✅ 数据表列表
- ✅ PHP 环境
- ✅ 文件权限

---

### 3️⃣ 安装向导

**访问地址**：
```
http://8.136.41.211:3395/install.php
```

**功能**：
- 📖 显示数据库配置信息
- 📝 指导在宝塔面板创建数据库
- ✅ 检查数据库是否已创建

---

## 🧪 API 测试

### 测试 1：发送验证码

**请求**：
```bash
curl -X POST "http://8.136.41.211:3395/api/sms.php" \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138099","action":"register"}'
```

**预期响应**（测试模式）：
```json
{
  "success": true,
  "message": "验证码已发送",
  "code": "123456",
  "expiresIn": 300,
  "isDevelopment": true
}
```

**预期响应**（生产模式）：
```json
{
  "success": true,
  "message": "验证码已发送",
  "expiresIn": 300,
  "isDevelopment": false
}
```

---

### 测试 2：注册

**请求**：
```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "name": "测试用户",
    "phone": "13800138099",
    "password": "test123456",
    "verify_code": "123456"
  }'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099",
      "check_in_interval": 48,
      "last_check_in_date": null
    }
  }
}
```

---

### 测试 3：登录（密码）

**请求**：
```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138099",
    "login_type": "password",
    "password": "test123456"
  }'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "user_id": "user_xxx",
    "is_new": false,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099",
      "check_in_interval": 48,
      "last_check_in_date": "2026-03-17 10:00:00"
    }
  }
}
```

---

### 测试 4：登录（验证码）

**请求**：
```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138099",
    "login_type": "verify_code",
    "verify_code": "123456"
  }'
```

**预期响应**：同上

---

## 🔧 常见问题诊断

### 问题 1：数据库连接失败

**症状**：
- 诊断页面显示"数据库连接失败"
- API 返回"数据库未初始化"

**检查**：
```
访问：http://8.136.41.211:3395/api/check-config.php
```

**解决**：
1. 在宝塔面板创建数据库
2. 数据库名：`zhonghuo_db`
3. 用户名：`zhonghuo_user`
4. 密码：`Zhonghuo@2026`
5. 刷新诊断页面

---

### 问题 2：数据表不存在

**症状**：
- 数据库连接成功
- 诊断页面显示"users 表不存在"

**检查**：
```
访问：http://8.136.41.211:3395/api/check-config.php
查看"数据表列表"
```

**解决**：
在宝塔面板执行 SQL：
```sql
USE zhonghuo_db;

CREATE TABLE users (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    last_login_at DATETIME NULL,
    INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

CREATE TABLE sms_codes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    phone VARCHAR(20) NOT NULL,
    code VARCHAR(10) NOT NULL,
    action VARCHAR(20) NOT NULL,
    expires_at DATETIME NOT NULL,
    is_used TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_phone_action (phone, action)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

### 问题 3：API 返回 404

**症状**：
- 诊断页面显示"API 文件不存在"
- 注册/登录返回 404

**检查**：
```
访问：http://8.136.41.211:3395/api/check-config.php
查看"文件权限检查"
```

**解决**：
```bash
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn
git pull origin main
chown -R www:www .
chmod -R 755 api/
```

---

### 问题 4：验证码发送失败

**症状**：
- 点击"获取验证码"无响应
- API 返回错误

**检查**：
```bash
curl -X POST "http://8.136.41.211:3395/api/sms.php" \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138099","action":"register"}'
```

**可能原因**：
1. 数据库未连接
2. sms_codes 表不存在
3. PHP 错误

**解决**：
1. 检查数据库连接
2. 检查数据表
3. 查看 PHP 错误日志

---

### 问题 5：前端无法连接后端

**症状**：
- App 显示"网络错误"
- 无法注册/登录

**检查**：
1. 诊断页面是否正常
2. API 测试是否成功
3. 前端 API 配置

**前端 API 配置**：
```swift
// AuthView.swift / DataManager.swift
DataManager.baseURL = "http://8.136.41.211:3395"
DataManager.apiURL = "http://8.136.41.211:3395/api"
```

**解决**：
1. 确认服务器 IP 正确
2. 确认端口 3395 开放
3. 确认 nginx 配置正确

---

## 📊 前端代码检查

### AuthView.swift

**关键功能**：
- ✅ 注册（姓名、手机号、密码、验证码）
- ✅ 登录（密码/验证码两种方式）
- ✅ 发送验证码
- ✅ 错误处理
- ✅ 详细日志

**API 调用**：
```swift
// 发送验证码
POST /api/sms.php
{
  "phone": "13800138099",
  "action": "register"  // 或 "login"
}

// 注册
POST /api/users.php
{
  "action": "register",
  "name": "测试用户",
  "phone": "13800138099",
  "password": "test123456",
  "verify_code": "123456"
}

// 登录
POST /api/users.php
{
  "action": "login",
  "phone": "13800138099",
  "login_type": "password",  // 或 "verify_code"
  "password": "test123456"   // 或 "verify_code": "123456"
}
```

---

### DataManager.swift

**关键功能**：
- ✅ API 地址初始化
- ✅ 配置刷新
- ✅ 网络连通性检查
- ✅ 数据持久化

**API 地址**：
```swift
DataManager.baseURL = "http://8.136.41.211:3395"
DataManager.apiURL = "http://8.136.41.211:3395/api"
```

---

## ✅ 完整诊断流程

### 步骤 1：访问诊断页面

```
http://8.136.41.211:3395/diagnostic.php
```

**检查所有项目是否为绿色**

---

### 步骤 2：测试 API

```bash
# 测试 config.php
curl http://8.136.41.211:3395/api/config.php

# 测试发送验证码
curl -X POST "http://8.136.41.211:3395/api/sms.php" \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138099","action":"register"}'

# 测试注册
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","name":"测试","phone":"13800138099","password":"test123","verify_code":"123456"}'

# 测试登录
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13800138099","login_type":"password","password":"test123"}'
```

---

### 步骤 3：检查前端

**在 Xcode 中运行 App**：
1. 打开 AuthView
2. 输入测试信息
3. 点击"获取验证码"
4. 输入验证码
5. 点击"注册"

**查看 Xcode 控制台日志**：
- 🟢 蓝色 = 正常流程
- ✅ 绿色 = 成功
- ❌ 红色 = 错误

---

### 步骤 4：查看日志

**后端日志**：
```bash
# PHP 错误日志
tail -f /www/server/php/74/var/log/php-fpm.log

# nginx 访问日志
tail -f /www/wwwlogs/zhonghuo.cn.log
```

**前端日志**：
- Xcode 控制台
- 打印的请求/响应信息

---

## 📝 总结

**诊断工具**：
1. ✅ `diagnostic.php` - 完整诊断
2. ✅ `check-config.php` - 配置检查
3. ✅ `install.php` - 安装向导

**API 测试**：
1. ✅ config.php - 配置信息
2. ✅ sms.php - 发送验证码
3. ✅ users.php - 注册/登录

**前端检查**：
1. ✅ AuthView.swift - 注册登录界面
2. ✅ DataManager.swift - API 管理

**常见问题**：
1. ❌ 数据库未创建 → 宝塔面板创建
2. ❌ 数据表不存在 → 执行 SQL 创建
3. ❌ API 文件缺失 → git pull 更新
4. ❌ 前端无法连接 → 检查 IP 和端口

---

*使用诊断页面快速定位问题！*
