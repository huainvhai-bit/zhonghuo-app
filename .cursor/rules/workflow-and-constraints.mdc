---
description: 任务范围、澄清、少文件、源码修改、Xcode 前端、审查与推送、自主执行、Skill 补全
alwaysApply: true
---

# 工作流与约束（全局）

## 0. 终活 app 仓库与路径（绝不可混用）
| 角色 | 本地路径 | GitHub |
| --- | --- | --- |
| **前端（iOS 等）** | `/Users/lishimin/Documents/zhonghuo-app` | `https://github.com/huainvhai-bit/zhonghuo-app` |
| **后端（PHP）** | `/Users/lishimin/Documents/zhonghuo-backend-php` | `https://github.com/huainvhai-bit/zhonghuo-backend-php` |

- 改前端只在 `zhonghuo-app` 目录；改后端只在 `zhonghuo-backend-php` 目录。推送时**分别进入对应目录**对 `origin` 推送，**禁止**把某一端的改动推到另一端仓库。

## 1. 范围不擅自扩展
- 未明确要求**新增**或**删除**的功能/文件/能力，**不得**自行增删。

## 2. 模糊指令先澄清
- 若需求有多种互斥实现或会改变产品范围/接口，**列出可选方案**，**用户确认后再执行**。

## 3. 不随意加文件
- 完成任务时**不新增**与任务无关的文件；不为了“组织方便”多建层目录或样板文件。

## 4. 在源码中直接修复
- 修 bug/改行为时**直接改**对应源码（如 `.php`、`.sql`、`.swift` 等），**不**以旁路脚本替代正式修改。

## 5. 少新增 `.sh` / `.md` 等
- **尽量**不新增 shell 脚本、说明文档等；**除非用户明确要求**或项目已有惯例必须如此。

## 6. 前端在 Xcode
- 前端/iOS 相关开发以 **Xcode 为主**；其他工具仅作辅助，不替代 Xcode 对工程/签名的主流程。

## 7. 每轮以审查收尾，并推到 GitHub 的 **main**

- **每一轮**完成**实质性**代码改动后：对本轮改动做**简要审查**（风险点、自测点）。
- 审查通过后，在**第 0 条**所列、**确实有改动的仓库**目录内依次执行：
  1. `git status`，只暂存**与任务相关**的文件；不要提交 `.cursor` 本地杂项、`DerivedData`、`xcuserdata` 等；若仅规则/配置类 `.mdc` 为有意修改，则纳入。
  2. **`git commit -m "…"`**（信息写清本轮做了什么）。
  3. **`git push origin main`**（远端分支 **`main`**；若某端本轮无改动，写明「某端未改、未推送」即可）。
- **禁止**只改不推、或只留在工作区不 commit；也**禁止**把 A 仓库的改动推到 B 仓库。
- 若 `git push` 因认证/网络失败：在回复中写明**错误摘要**与**建议你在本机执行的一条命令**，不要假装已推送成功。

## 8. 能自己做的不甩给用户
- 能在当前环境**自行完成**的（修代码、查日志、跑构建/测试、拉依赖等）**直接执行**；**本地构建/验证通过后再推送**。
- 与第 2 条不冲突：仅有**多方案/高风险**需用户选定时才停步询问。

## 9. 缺少的 Skill
- 若任务依赖某 **Skill** 而本地**尚未具备**：先**查找**官方或用户 skills 目录中的对应 `SKILL.md` / 安装说明，**能自动安装/复制则完成配置**后再继续；无法在无交互环境完成时，在回复中写明**已尝试的步骤**与**你本地需补的一步**（最少必要）。
