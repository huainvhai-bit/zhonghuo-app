# 终活 App - 服务器部署速查卡

## 🚀 快速部署（3 步完成）

```bash
# 1️⃣ 拉取最新代码
cd /www/wwwroot/zhonghuo.cn
git pull origin main

# 2️⃣ 初始化数据库
mysql -u root -p < backend/init_zhonghuo_db.sql

# 3️⃣ 部署 API（自动备份旧代码）
cp api/users.php api/users.php.bak.$(date +%Y%m%d_%H%M%S)
cp backend/users.php api/users.php
chown www:www api/users.php
chmod 644 api/users.php

# 4️⃣ 创建配置文件
cat > api/config.php << 'EOF'
<?php
define('SKIP_VERIFY_CODE', true);  // 测试模式：跳过验证码
define('DEBUG_MODE', true);
EOF

# ✅ 验证部署
php -l api/users.php
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13800138006","login_type":"password","password":"test123456"}'
```

---

## 📊 验证清单

- [ ] 数据库创建成功
- [ ] 用户表结构正确
- [ ] API 语法检查通过
- [ ] 测试账号可以登录
- [ ] 新用户注册成功并保存到数据库

---

## 🧪 测试命令

```bash
# 测试注册（新账号）
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138009","name":"测试 9","password":"test123","verify_code":"123456"}'

# 测试登录
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13800138006","login_type":"password","password":"test123456"}'

# 查看数据库用户
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db -e "SELECT id, name, phone, created_at FROM users;"
```

---

## 🔧 故障排查

```bash
# 查看错误日志
tail -f /www/wwwlogs/zhonghuo.cn.error.log

# 检查数据库连接
mysql -u zhonghuo_user -p'Zhonghuo@2026' -e "SELECT 1"

# 检查 PHP-FPM 状态
systemctl status php-fpm

# 检查 nginx 状态
systemctl status nginx
```

---

**祝部署顺利！🎉**
