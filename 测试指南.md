# 🧪 测试指南 - 登录和签到间隔修复

## ✅ 已修复的问题

### 1. 登录错误提示增强
- 添加 404 错误检测
- 添加网络响应异常检测
- 更详细的错误日志输出

### 2. 签到间隔修改
- 添加用户登录状态检查
- 添加保存结果反馈
- 详细的控制台日志

## 📋 测试步骤

### 步骤 1: 重启模拟器
```bash
xcrun simctl shutdown "iPhone 17 Pro"
xcrun simctl boot "iPhone 17 Pro"
```

### 步骤 2: 打开 App 并测试登录

**测试账号**：
- 手机号：`13800138008`
- 密码：`test123`

**预期结果**：
- ✅ 能够成功登录
- ❌ 如果失败，会显示详细的错误信息

**可能的错误及原因**：
1. **"服务器配置错误（404）"** → 后端 API 文件找不到
2. **"网络响应异常"** → 模拟器网络问题
3. **"网络错误：..."** → 查看具体错误描述

### 步骤 3: 测试签到间隔修改

1. 登录成功后，点击底部"我的"
2. 找到"签到间隔"选项
3. 尝试修改为不同的间隔（1 分钟、1 天、7 天等）
4. 查看控制台输出

**预期结果**：
- ✅ 修改后立即显示新值
- ✅ 控制台显示"✅ 签到间隔保存成功"
- ❌ 如果显示"❌ 用户未登录"，说明登录状态有问题

### 步骤 4: 查看控制台日志

打开 Xcode → Window → Devices and Simulators → 选择模拟器 → 查看控制台

**关键日志**：
```
🔵 开始登录流程...
🌐 请求 URL: http://8.136.41.211:3395/api/users.php
✅ JSON 解析成功
登录成功！

🔵 更新签到间隔：48 小时 → 1 天
✅ 签到间隔保存成功
```

## 🔧 故障排查

### 登录失败排查

1. **测试后端 API**：
```bash
curl -X POST http://8.136.41.211:3395/api/users.php -d 'action=login&phone=13800138008&password=test123'
```

2. **检查模拟器网络**：
- 打开 Safari，访问 `http://8.136.41.211:3395/api/config.php`
- 应该返回 JSON 数据

3. **重置网络**：
```bash
xcrun simctl shutdown "iPhone 17 Pro"
xcrun simctl boot "iPhone 17 Pro"
```

### 签到间隔无法修改排查

1. **检查登录状态**：
- 确保已成功登录
- 查看"我的"页面是否显示用户信息

2. **查看控制台日志**：
- 如果显示"❌ 用户未登录"，需要重新登录
- 如果显示"❌ 保存失败"，检查文件权限

## 📊 后端验证

```bash
# 1. 测试注册
curl -X POST http://8.136.41.211:3395/api/users.php \
  -d 'action=register&name=测试用户&phone=13800138009&password=test123'

# 2. 测试登录
curl -X POST http://8.136.41.211:3395/api/users.php \
  -d 'action=login&phone=13800138009&password=test123'

# 3. 查看数据库
ssh root@8.136.41.211
mysql -u zhonghuo -p'wangzi1314' zhonghuo -e "SELECT * FROM users ORDER BY created_at DESC LIMIT 5"
```

## ✅ 成功标志

- [ ] 能够成功登录
- [ ] 登录后显示用户信息
- [ ] 能够修改签到间隔
- [ ] 修改后立即生效
- [ ] 重新打开 App 后设置保留

## 📝 报告问题

如果测试失败，请提供：
1. 具体的错误信息
2. 控制台日志截图
3. 测试步骤
4. 测试账号
