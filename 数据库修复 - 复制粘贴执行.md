# 🚨 数据库修复 - 复制粘贴执行

**诊断结果**：
- ✅ 环境变量：未设置（使用默认值）
- ✅ config.php：配置正确
- ❌ 数据库连接：失败（用户不存在）
- ❌ 数据表：没有 users 表

**根本原因**：服务器上没有执行数据库初始化脚本！

---

## 📋 在服务器上执行（复制粘贴）

### 1️⃣ SSH 登录

```bash
ssh root@8.136.41.211
```

### 2️⃣ 进入目录

```bash
cd /www/wwwroot/zhonghuo.cn
```

### 3️⃣ 拉取最新代码

```bash
git pull origin main
```

### 4️⃣ 执行 SQL 命令（需要输入 MySQL root 密码）

**复制以下所有命令，一次性粘贴执行**：

```bash
# 提示输入密码
read -s -p "请输入 MySQL root 密码：" MYSQL_ROOT_PWD
echo ""

# 执行 SQL
mysql -u root -p"${MYSQL_ROOT_PWD}" << 'EOF'
-- 删除旧的
DROP DATABASE IF EXISTS zhonghuo_db;
DROP USER IF EXISTS 'zhonghuo_user'@'localhost';

-- 创建数据库
CREATE DATABASE zhonghuo_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER 'zhonghuo_user'@'localhost' IDENTIFIED BY 'Zhonghuo@2026';

-- 授权
GRANT ALL PRIVILEGES ON zhonghuo_db.* TO 'zhonghuo_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;

-- 使用数据库
USE zhonghuo_db;

-- 创建 users 表
CREATE TABLE users (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    last_login_at DATETIME NULL,
    INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 创建 sms_codes 表
CREATE TABLE sms_codes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    phone VARCHAR(20) NOT NULL,
    code VARCHAR(10) NOT NULL,
    action VARCHAR(20) NOT NULL,
    expires_at DATETIME NOT NULL,
    is_used TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_phone_action (phone, action)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 验证
SHOW TABLES;

-- 测试插入
INSERT INTO users (id, name, phone, password_hash) VALUES ('test_user', '测试用户', '13800138000', 'test');

-- 验证插入
SELECT * FROM users;

EXIT;
EOF

# 设置权限
chown -R www:www .
chmod -R 755 api/

echo "✅ 数据库创建完成！"
```

### 5️⃣ 测试 API

```bash
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试用户","password":"test123","verify_code":"123456"}'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099"
    }
  }
}
```

---

## ✅ 验证步骤

### 1. 检查数据表

```bash
mysql -u zhonghuo_user -p'Zhonghuo@2026' -e "USE zhonghuo_db; SHOW TABLES;"
```

**应该看到**：
```
+---------------------+
| Tables_in_zhonghuo_db |
+---------------------+
| sms_codes           |
| users               |
+---------------------+
```

### 2. 访问诊断页面

```
http://8.136.41.211:3395/api/check-config.php
```

**应该显示**：
- ✅ 数据库连接成功
- ✅ users 表存在
- ✅ sms_codes 表存在

### 3. 测试注册

在手机上打开 App，测试注册功能！

---

## 🔍 故障排查

### 问题 1：MySQL root 密码错误

**解决**：
```bash
# 重置 MySQL root 密码
mysqld_safe --skip-grant-tables &
mysql -u root << EOF
FLUSH PRIVILEGES;
ALTER USER 'root'@'localhost' IDENTIFIED BY '新密码';
EXIT;
EOF
```

### 问题 2：git pull 失败

**解决**：
```bash
cd /www/wwwroot/zhonghuo.cn
git fetch origin
git reset --hard origin/main
```

### 问题 3：权限错误

**解决**：
```bash
chown -R www:www /www/wwwroot/zhonghuo.cn
chmod -R 755 /www/wwwroot/zhonghuo.cn/api
```

---

## 📊 数据库配置

| 配置项 | 值 |
|--------|-----|
| 主机 | localhost |
| 数据库名 | zhonghuo_db |
| 用户名 | zhonghuo_user |
| 密码 | Zhonghuo@2026 |
| 字符集 | utf8mb4 |
| 排序规则 | utf8mb4_unicode_ci |

---

## 📝 表结构

### users 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(64) | 用户 ID（主键） |
| name | VARCHAR(100) | 用户名 |
| phone | VARCHAR(20) | 手机号（唯一） |
| password_hash | VARCHAR(255) | 密码哈希 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| last_login_at | DATETIME | 最后登录时间 |

### sms_codes 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 验证码 ID（主键） |
| phone | VARCHAR(20) | 手机号 |
| code | VARCHAR(10) | 验证码 |
| action | VARCHAR(20) | 操作类型 |
| expires_at | DATETIME | 过期时间 |
| is_used | TINYINT | 是否已使用 |
| created_at | DATETIME | 创建时间 |

---

*执行完上面的命令后，注册功能立即可用！*
