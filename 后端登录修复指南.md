# 🔧 后端登录 API 修复指南

**问题**: POST 请求到 `/api/users.php` 返回 404

**时间**: 2026-03-17

---

## 📋 已完成修复

### 1. PHP-FPM 配置 ✅
```bash
# SSH 登录
ssh root@8.136.41.211
# 密码：Wangzi1314

# 修改 PHP-FPM 为 dynamic 模式
sed -i 's/pm = ondemand/pm = dynamic/' /www/server/php/74/etc/php-fpm.conf
sed -i 's/pm.min_spare_servers = 5/pm.min_spare_servers = 2/' /www/server/php/74/etc/php-fpm.conf

# 重启 PHP-FPM
/etc/init.d/php-fpm-74 restart

# 验证（应该有 5 个 worker）
ps aux | grep php-fpm
```

### 2. 创建必需文件 ✅

**jwt.php** (`/www/wwwroot/zhonghuo.cn/api/jwt.php`):
```php
<?php
define('JWT_SECRET', 'zhonghuo_app_secret_key_2026');
define('JWT_ALGO', 'HS256');

function createJWT($payload) {
    $header = ['alg' => JWT_ALGO, 'typ' => 'JWT'];
    $headerEncoded = base64UrlEncode(json_encode($header));
    $payloadEncoded = base64UrlEncode(json_encode($payload));
    $signature = hash_hmac('sha256', "$headerEncoded.$payloadEncoded", JWT_SECRET, true);
    $signatureEncoded = base64UrlEncode($signature);
    return "$headerEncoded.$payloadEncoded.$signatureEncoded";
}

function verifyJWT($token) {
    $parts = explode('.', $token);
    if (count($parts) !== 3) throw new Exception('Invalid token');
    list($headerEncoded, $payloadEncoded, $signatureEncoded) = $parts;
    $signature = base64UrlDecode($signatureEncoded);
    $expectedSignature = hash_hmac('sha256', "$headerEncoded.$payloadEncoded", JWT_SECRET, true);
    if (!hash_equals($signature, $expectedSignature)) throw new Exception('Invalid signature');
    $payload = json_decode(base64UrlDecode($payloadEncoded), true);
    if (isset($payload['exp']) && $payload['exp'] < time()) throw new Exception('Token expired');
    return $payload;
}

function base64UrlEncode($data) { return rtrim(strtr(base64_encode($data), '+/', '-_'), '='); }
function base64UrlDecode($data) { return base64_decode(strtr($data, '-_', '+/')); }
```

**config.php** (`/www/wwwroot/zhonghuo.cn/config.php`):
```php
<?php
$GLOBALS['db_host'] = 'localhost';
$GLOBALS['db_name'] = 'zhonghuo';
$GLOBALS['db_user'] = 'zhonghuo';
$GLOBALS['db_pass'] = 'wangzi1314';
```

### 3. users.php 头部修复 ✅

确保 `/www/wwwroot/zhonghuo.cn/api/users.php` 开头是：
```php
<?php
/**
 * 终活 App 后端 - 用户认证 API
 */

// 开发模式配置
define('SKIP_VERIFY_CODE', true);

require_once __DIR__ . '/core.php';
```

---

## 🔍 待解决：nginx POST 404 问题

### 测试命令
```bash
# GET 请求（正常）
curl -s 'http://localhost:3395/api/users.php?action=test'

# POST 请求（返回 404）
curl -s -X POST http://localhost:3395/api/users.php \
  -H 'Content-Type: application/json' \
  -d '{"action":"login","phone":"13800138006","password":"test123456"}'
```

### 可能原因
1. nginx 配置中 location 块不匹配 POST 请求
2. fastcgi 配置问题
3. 文件权限问题

### 解决方案

#### 方案 1: 检查 nginx 配置
```bash
nginx -T | grep -A 20 'location ~ .*\.php'
```

#### 方案 2: 检查文件权限
```bash
ls -la /www/wwwroot/zhonghuo.cn/api/users.php
# 应该是 644 或 755
chmod 644 /www/wwwroot/zhonghuo.cn/api/users.php
```

#### 方案 3: 重新加载 nginx
```bash
nginx -s reload
```

#### 方案 4: 检查错误日志
```bash
tail -20 /www/wwwlogs/zhonghuo.cn.error.log
```

---

## 🧪 完整测试流程

```bash
# 1. 测试 config.php（应该返回 JSON）
curl -s http://localhost:3395/api/config.php

# 2. 测试 users.php GET（应该返回 JSON 错误）
curl -s 'http://localhost:3395/api/users.php?action=test'

# 3. 测试 users.php POST（目标：返回登录成功/失败）
curl -s -X POST http://localhost:3395/api/users.php \
  -H 'Content-Type: application/json' \
  -d '{"action":"login","phone":"13800138006","password":"test123456"}'

# 4. 测试登录（使用 form-data）
curl -s -X POST http://localhost:3395/api/users.php \
  -d 'action=login&phone=13800138006&password=test123456&login_type=password'
```

---

## 📝 当前状态

- ✅ PHP-FPM: 5 workers 运行中
- ✅ jwt.php: 已创建
- ✅ config.php: 已创建
- ✅ users.php: 语法正确
- ❌ POST /api/users.php: 返回 404（待解决）

---

## 🚀 临时解决方案

如果 nginx POST 问题无法快速解决，可以修改前端使用 form-data 格式：

在 `AuthView.swift` 中修改登录请求：
```swift
// 不使用 JSON，改用 form-data
var body = "action=login&phone=\(phone)&password=\(password)&login_type=\(loginType)"
request.httpBody = body.data(using: .utf8)
request.setValue("application/x-www-form-urlencoded", forHTTPHeaderField: "Content-Type")
```

---

**最后更新**: 2026-03-17 12:30
