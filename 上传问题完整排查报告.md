# 上传问题完整排查报告

**日期**: 2026-03-19 15:25  
**版本**: v2.0  
**状态**: ✅ 已完成全面加固

---

## 一、问题排查清单

### 1.1 Token 相关问题 ✅ 已修复

| 问题 | 检查方法 | 状态 | 修复方案 |
|------|---------|------|---------|
| Token 未保存 | 登录后检查 UserDefaults | ✅ 正常 | `UserDefaults.standard.set(token, forKey: "userToken")` |
| Token 未使用 | 检查请求头 | ✅ 正常 | `request.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")` |
| **Token 过期未检查** | JWT payload 解析 | ❌ **缺失** → ✅ **已修复** | 添加 `isTokenExpired()` 函数 |
| Token 格式错误 | JWT 格式验证 | ❌ **缺失** → ✅ **已修复** | 检查 token 是否有 3 个部分 |

**修复代码**:
```swift
func isTokenExpired(_ token: String) -> Bool {
    // JWT: header.payload.signature
    let components = token.split(separator: ".")
    guard components.count == 3 else { return true }
    
    // 解析 payload
    let payloadString = String(components[1])
    var base64 = payloadString
        .replacingOccurrences(of: "-", with: "+")
        .replacingOccurrences(of: "_", with: "/")
    
    while base64.count % 4 != 0 { base64 += "=" }
    
    guard let payloadData = Data(base64Encoded: base64),
          let payload = try? JSONSerialization.jsonObject(with: payloadData) as? [String: Any],
          let exp = payload["exp"] as? TimeInterval else {
        return true
    }
    
    return Date().timeIntervalSince1970 > exp
}
```

### 1.2 网络相关问题 ✅ 已修复

| 问题 | 检查方法 | 状态 | 修复方案 |
|------|---------|------|---------|
| 无网络连接 | 检查网络状态 | ❌ **缺失** → ✅ **已修复** | 添加 `checkNetworkConnectivity()` |
| API URL 错误 | 检查 URL 格式 | ✅ 正常 | `DataManager.apiURL = "http://8.136.41.211:3395/api"` |
| 服务器无响应 | 检查超时 | ✅ 正常 | 默认 30 秒超时 |
| DNS 解析失败 | 检查错误类型 | ❌ **缺失** → ✅ **已修复** | URLError 详细处理 |

**修复代码**:
```swift
func checkNetworkConnectivity() async -> Bool {
    guard let url = URL(string: "http://8.136.41.211:3395/api/check-config.php") else {
        return false
    }
    
    do {
        let (_, response) = try await URLSession.shared.data(from: url)
        guard let httpResponse = response as? HTTPURLResponse else {
            return false
        }
        return (200...299).contains(httpResponse.statusCode)
    } catch {
        print("❌ 网络检查失败：\(error)")
        return false
    }
}
```

### 1.3 权限相关问题 ✅ 已修复

| 问题 | 检查方法 | 状态 | 修复方案 |
|------|---------|------|---------|
| 401 未授权 | 检查状态码 | ❌ **缺失** → ✅ **已修复** | 添加 401 处理，清除 Token |
| 403 权限不足 | 检查状态码 | ❌ **缺失** → ✅ **已修复** | 添加 403 处理 |
| 404 资源不存在 | 检查状态码 | ❌ **缺失** → ✅ **已修复** | 添加 404 处理 |
| 500 服务器错误 | 检查状态码 | ❌ **缺失** → ✅ **已修复** | 添加 500-599 处理 |

**修复代码**:
```swift
switch httpResponse.statusCode {
case 200...299:
    break  // 成功
case 401:
    print("❌ 同步失败：401 未授权（Token 无效或过期）")
    UserDefaults.standard.removeObject(forKey: "userToken")
    UserDefaults.standard.removeObject(forKey: "isLoggedIn")
    return nil
case 403:
    print("❌ 同步失败：403 权限不足")
    return nil
case 404:
    print("❌ 同步失败：404 API 不存在")
    return nil
case 500...599:
    print("❌ 同步失败：\(httpResponse.statusCode) 服务器错误")
    return nil
default:
    print("❌ 同步失败：未知状态码 \(httpResponse.statusCode)")
    return nil
}
```

### 1.4 数据格式问题 ✅ 已修复

| 问题 | 检查方法 | 状态 | 修复方案 |
|------|---------|------|---------|
| JSON 解析失败 | 检查响应格式 | ❌ **缺失** → ✅ **已修复** | 添加 JSON 解析错误处理 |
| 字段缺失 | 检查 response 结构 | ❌ **缺失** → ✅ **已修复** | 检查 success 字段 |
| 数据类型错误 | 类型转换检查 | ✅ 正常 | 使用 `as?` 安全转换 |

**修复代码**:
```swift
guard let result = try JSONSerialization.jsonObject(with: data) as? [String: Any] else {
    print("❌ 同步失败：无法解析响应 JSON")
    return nil
}

if let success = result["success"] as? Bool, success {
    if let data = result["data"] as? [String: Any] {
        let total = data["synced"] as? Int ?? data["total"] as? Int ?? 0
        let created = data["created"] as? Int ?? 0
        let updated = data["updated"] as? Int ?? 0
        print("✅ 同步成功：总计 \(total), 新增 \(created), 更新 \(updated)")
        return (total, created, updated)
    }
} else {
    // 处理错误响应
    if let error = result["error"] as? [String: Any] {
        let code = error["code"] as? String ?? "UNKNOWN"
        let message = error["message"] as? String ?? "未知错误"
        print("❌ 同步失败：[\(code)] \(message)")
    }
    return nil
}
```

---

## 二、完整错误处理流程

### 2.1 网络错误处理

```swift
catch let urlError as URLError {
    print("❌ 同步失败：网络错误 - \(urlError.localizedDescription)")
    switch urlError.code {
    case .notConnectedToInternet:
        print("💡 建议：检查网络连接")
    case .timedOut:
        print("💡 建议：检查服务器是否响应")
    case .cannotFindHost:
        print("💡 建议：检查 API URL 是否正确")
    case .cannotConnectToHost:
        print("💡 建议：检查服务器是否运行")
    default:
        break
    }
    return nil
}
```

### 2.2 通用错误处理

```swift
catch {
    print("❌ 同步失败：\(error)")
    print("📋 错误类型：\(type(of: error))")
    print("📋 错误域：\((error as NSError).domain)")
    print("📋 错误码：\((error as NSError).code)")
    return nil
}
```

---

## 三、诊断日志输出

### 3.1 同步前检查

```
📦 ====== batchSyncCapsules 开始 ======
   - API URL: http://8.136.41.211:3395/api
   - Token: eyJhbGciOiJIUzI1NiIsInR5cCI6...
   - Token 长度：156
   - 本地胶囊数：5
✅ Token 有效，过期时间：2026-03-26 15:00:00
```

### 3.2 请求详情

```
🔄 开始同步胶囊：共 5 个
🌐 API URL: http://8.136.41.211:3395/api
📍 请求 URL: http://8.136.41.211:3395/api/capsules.php?action=batch_sync
🔑 Token 长度：156
📋 Headers: Content-Type=application/json, Authorization=Bearer eyJhbG...
📤 胶囊同步请求：5 个胶囊
📦 请求体：{"capsules":[{"id":"xxx","title":"测试","content":"内容"}],"token":"eyJhbG..."}
```

### 3.3 响应详情

```
📡 胶囊同步响应状态码：200
📄 胶囊同步响应：{"success":true,"message":"同步成功","data":{"total":5,"created":5,"updated":0}}
✅ 胶囊同步成功：总计 5, 新增 5, 更新 0
```

### 3.4 错误日志

```
❌ 胶囊同步失败：401 未授权（Token 无效或过期）
💡 已清除过期 Token，请重新登录

或

❌ 胶囊同步失败：网络错误 - 无法连接到服务器
💡 建议：检查服务器是否运行

或

❌ 胶囊同步失败：[INVALID_TOKEN] Token 格式错误
💡 建议：重新登录获取新 Token
```

---

## 四、测试步骤

### 4.1 Token 过期测试

1. **修改后端 Token 过期时间**（临时改为 1 分钟）
2. **登录 App** → 等待 1 分钟
3. **添加胶囊** → 查看日志
4. **预期结果**:
   ```
   ⚠️ 胶囊同步失败：Token 已过期，请重新登录
   ```

### 4.2 网络断开测试

1. **关闭模拟器网络**（飞行模式）
2. **添加胶囊** → 查看日志
3. **预期结果**:
   ```
   ❌ 胶囊同步失败：网络错误 - 无网络连接
   💡 建议：检查网络连接
   ```

### 4.3 服务器错误测试

1. **停止后端服务** (`systemctl stop php-fpm`)
2. **添加胶囊** → 查看日志
3. **预期结果**:
   ```
   ❌ 胶囊同步失败：网络错误 - 无法连接到服务器
   💡 建议：检查服务器是否运行
   ```

### 4.4 权限不足测试

1. **使用无权限的 Token**
2. **添加胶囊** → 查看日志
3. **预期结果**:
   ```
   ❌ 胶囊同步失败：403 权限不足
   ```

---

## 五、已修复函数列表

| 函数 | 修复内容 | 文件 |
|------|---------|------|
| `batchSyncCapsules()` | Token 检查、网络检查、状态码处理、错误日志 | DataManager.swift |
| `batchSyncWills()` | Token 检查、网络检查、状态码处理、错误日志 | DataManager.swift |
| `batchSyncEmergencyContacts()` | Token 检查、网络检查、状态码处理、错误日志 | DataManager.swift |
| `batchSyncWitnesses()` | Token 检查、网络检查、状态码处理、错误日志 | DataManager.swift |
| `isTokenExpired()` | **新增** JWT Token 过期检查 | DataManager.swift |
| `checkNetworkConnectivity()` | **新增** 网络连通性检查 | DataManager.swift |

---

## 六、下一步优化建议

### 6.1 短期优化（1-2 天）

- [ ] **使用 Keychain 存储 Token**（更安全）
- [ ] **添加 Token 刷新机制**（过期前自动刷新）
- [ ] **添加请求重试逻辑**（失败后自动重试 3 次）
- [ ] **添加离线队列**（无网络时暂存，有网络时同步）

### 6.2 中期优化（1 周）

- [ ] **部署 HTTPS**（获取 SSL 证书）
- [ ] **实现分片上传**（支持大文件）
- [ ] **实现断点续传**（上传失败后继续）
- [ ] **实现后台上传**（App 进入后台后继续）

### 6.3 长期优化（1 月）

- [ ] **添加 CDN 加速**（图片/视频上传）
- [ ] **添加压缩**（减少流量消耗）
- [ ] **添加加密**（敏感数据加密传输）
- [ ] **添加监控**（上传成功率监控）

---

## 七、关键日志截图指南

**测试时请截图以下日志**:

1. **登录成功后的 Token 日志**:
   ```
   ✅ Token 有效，过期时间：2026-03-26 15:00:00
   ```

2. **添加胶囊时的同步日志**:
   ```
   📦 ====== batchSyncCapsules 开始 ======
   📤 胶囊同步请求：1 个胶囊
   📡 胶囊同步响应状态码：200
   ✅ 胶囊同步成功：总计 1, 新增 1, 更新 0
   ```

3. **后端诊断页面**:
   ```
   http://8.136.41.211:3395/api/diagnose-all.php
   capsules: 1 条
   will_modules: 10 条
   ```

---

## 八、常见错误速查表

| 错误日志 | 原因 | 解决方案 |
|---------|------|---------|
| `⚠️ 胶囊同步失败：无 token（用户未登录）` | 用户未登录 | 重新登录 |
| `⚠️ 胶囊同步失败：Token 已过期` | Token 过期 | 重新登录 |
| `❌ 胶囊同步失败：401 未授权` | Token 无效 | 清除缓存，重新登录 |
| `❌ 胶囊同步失败：403 权限不足` | 权限不足 | 联系管理员 |
| `❌ 胶囊同步失败：404 API 不存在` | API URL 错误 | 检查 API URL |
| `❌ 胶囊同步失败：500 服务器错误` | 后端错误 | 检查后端日志 |
| `❌ 胶囊同步失败：网络错误 - 无网络连接` | 无网络 | 检查网络设置 |
| `❌ 胶囊同步失败：网络错误 - 请求超时` | 服务器无响应 | 检查服务器状态 |
| `❌ 胶囊同步失败：无法解析响应 JSON` | 响应格式错误 | 检查后端返回 |

---

**状态**: ✅ 所有关键问题已修复  
**下一步**: 用户测试并提供日志截图 🔍
