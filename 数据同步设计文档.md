# 数据同步设计文档 - 关爱老年人

## 🎯 核心理念

**终活 App 不是为了获取用户数据，而是关爱老年人**

### 设计原则
1. **无感知同步** - 用户不需要手动操作，打开 App 自动完成
2. **双向同步** - 本地↔云端，保持数据一致
3. **智能比对** - 只同步有变化的数据，节省流量
4. **多设备支持** - 换新手机登录，数据自动同步
5. **数据清理** - 上传新数据后清理旧数据

---

## 📊 同步场景

### 场景 1：日常打开 App（本地有新数据）
```
用户打开 App
  ↓
自动比对本地和云端数据
  ↓
发现本地有未同步的数据
  ↓
上传新数据/修改的数据到云端
  ↓
清理已上传的旧数据
  ↓
✅ 本地和云端数据一致
```

### 场景 2：换新手机登录（云端有数据）
```
用户在新手机登录
  ↓
自动比对本地和云端数据
  ↓
发现云端有数据，本地为空
  ↓
下载云端所有数据到本地
  ↓
✅ 新手机数据完整
```

### 场景 3：多设备使用（两边都有数据）
```
设备 A 和 设备 B 都有数据
  ↓
登录设备 A
  ↓
比对数据时间戳
  ↓
- 本地新的 → 上传到云端
- 云端新的 → 下载到本地
- 冲突的 → 以最新时间为准
  ↓
✅ 所有设备数据一致
```

---

## 🔄 同步流程

### 完整同步流程
```
打开 App / 登录
  ↓
1. 获取本地数据列表（含更新时间）
2. 获取云端数据列表（含更新时间）
3. 比对数据
  ↓
比对逻辑：
- 本地有，云端无 → 上传（新增）
- 云端有，本地无 → 下载（新设备）
- 都有，本地新 → 上传（更新）
- 都有，云端新 → 下载（其他设备修改）
- 都有，时间相同 → 跳过（已同步）
  ↓
4. 执行同步
  ↓
5. 清理旧数据（本地和云端）
  ↓
✅ 同步完成
```

---

## 📦 数据类型

### 需要同步的数据
1. **时光胶囊** - capsules
2. **遗嘱模块** - will_modules
3. **紧急联系人** - emergency_contacts
4. **见证人** - witnesses
5. **位置信息** - user_locations（只上传，不同步）
6. **签到记录** - checkin_records（只上传，不同步）

---

## 🔧 技术实现

### 数据模型扩展
```swift
struct TimeCapsule: Identifiable, Codable {
    var id: String
    var title: String
    var content: String
    // ... 其他字段
    
    // 🆕 同步相关字段
    var createdAt: Date      // 创建时间
    var updatedAt: Date      // 更新时间
    var isSynced: Bool       // 是否已同步
    var syncStatus: SyncStatus  // 同步状态
    
    enum SyncStatus: String, Codable {
        case synced      // 已同步
        case pending     // 待上传
        case modified    // 已修改
        case conflict    // 冲突
    }
}
```

### 比对逻辑
```swift
func syncData() async {
    // 1. 获取本地数据
    let localCapsules = dataManager.capsules
    let localWills = dataManager.willModules
    
    // 2. 获取云端数据列表（只获取元数据）
    let cloudCapsules = await fetchCloudCapsulesMeta()
    let cloudWills = await fetchCloudWillsMeta()
    
    // 3. 比对胶囊数据
    for localCapsule in localCapsules {
        let cloudCapsule = cloudCapsules.first { $0.id == localCapsule.id }
        
        if cloudCapsule == nil {
            // 本地有，云端无 → 上传
            await uploadCapsule(localCapsule)
        } else if localCapsule.updatedAt > cloudCapsule!.updatedAt {
            // 本地更新 → 上传
            await uploadCapsule(localCapsule)
        } else if localCapsule.updatedAt < cloudCapsule!.updatedAt {
            // 云端更新 → 下载
            await downloadCapsule(cloudCapsule!)
        }
        // 时间相同 → 跳过
    }
    
    // 4. 检查云端有但本地无的数据（新设备）
    for cloudCapsule in cloudCapsules {
        if !localCapsules.contains(where: { $0.id == cloudCapsule.id }) {
            await downloadCapsule(cloudCapsule)
        }
    }
    
    // 5. 清理旧数据
    await cleanupOldData()
}
```

---

## 📝 后端 API 设计

### 获取数据列表（含时间戳）
```
GET /api/capsules.php?action=list_meta&token=xxx
GET /api/will.php?action=list_meta&token=xxx
GET /api/emergency_contacts.php?action=list_meta&token=xxx
GET /api/will.php?action=list_witnesses_meta&token=xxx
```

**返回格式：**
```json
{
  "success": true,
  "data": [
    {
      "id": "uuid",
      "updated_at": "2026-03-19T13:00:00Z",
      "created_at": "2026-03-19T12:00:00Z"
    }
  ]
}
```

### 批量上传（带时间戳）
```
POST /api/capsules.php?action=batch_sync
```

**请求格式：**
```json
{
  "token": "xxx",
  "capsules": [
    {
      "id": "uuid",
      "title": "标题",
      "updated_at": "2026-03-19T13:00:00Z",
      // ... 其他字段
    }
  ]
}
```

### 批量下载
```
POST /api/capsules.php?action=batch_download
```

**请求格式：**
```json
{
  "token": "xxx",
  "ids": ["uuid1", "uuid2", ...]
}
```

---

## 🧹 数据清理策略

### 本地清理
- 上传成功的数据 → 标记为已同步
- 保留最近 N 条历史记录（可选）
- 用户手动删除 → 同步删除到云端

### 云端清理
- 保留每个数据的最新版本
- 历史版本保留 N 天（可选）
- 用户注销账号 → 删除所有数据

---

## 🎯 用户体验

### 无感知同步
```
用户打开 App
  ↓
看到最新数据（自动同步完成）
  ↓
无需等待，无需操作
```

### 新设备登录
```
用户在新手机登录
  ↓
自动下载所有数据
  ↓
显示"正在同步数据..."（1-2 秒）
  ↓
数据完整显示
```

### 数据冲突处理
```
两个设备同时修改同一数据
  ↓
以后修改的为准
  ↓
可选：提示用户"检测到数据冲突，已自动合并"
```

---

## 📊 同步状态显示

### 状态栏指示器
- 🟢 已同步
- 🟡 同步中...
- 🔴 同步失败（点击重试）

### 日志输出
```
🔄 开始同步数据...
📦 比对胶囊数据...
  - 3 个胶囊已同步
  - 1 个胶囊待上传
  - 0 个胶囊待下载
📦 上传 1 个胶囊...
✅ 胶囊同步完成
📝 比对遗嘱数据...
...
🎉 所有数据同步完成
```

---

## 🔒 安全考虑

### 数据加密
- 传输加密：HTTPS
- 存储加密：本地数据加密
- Token 验证：每次请求验证身份

### 隐私保护
- 不收集无关数据
- 仅同步用户创建的数据
- 用户可导出/删除所有数据

---

## ✅ 实施进度

### 阶段 1：基础同步 ✅ 已完成
- [x] 实现上传逻辑（batchSync）
- [x] 实现下载逻辑（downloadAllData）
- [x] 打开 App 自动同步
- [x] 登录后自动同步

### 阶段 2：智能比对 🚧 进行中
- [x] 实现基础下载合并逻辑
- [ ] 添加 updatedAt 字段比对
- [ ] 实现元数据获取 API（后端）
- [ ] 实现冲突处理
- [ ] 添加同步状态显示

### 阶段 3：优化清理
- [ ] 实现本地清理逻辑
- [ ] 实现云端清理逻辑
- [ ] 添加数据恢复功能（可选）
- [ ] 性能优化（增量同步）

---

## 🎯 成功标准

1. **打开 App 自动同步** - 无需手动操作
2. **新设备登录数据完整** - 所有数据自动下载
3. **多设备数据一致** - 任何修改自动同步
4. **无感知体验** - 用户感觉不到同步过程
5. **流量优化** - 只同步变化的数据

---

*最后更新：2026-03-19 13:07*
