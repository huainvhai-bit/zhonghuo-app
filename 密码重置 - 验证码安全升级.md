# 🔐 密码重置 - 验证码安全升级

## 更新时间
2026-03-21 18:15

## 问题
原密码重置功能缺少验证码验证，存在安全隐患：
- ❌ 任何人知道手机号就能重置密码
- ❌ 无法验证操作者是手机主人
- ❌ 容易被恶意攻击

## 解决方案
添加手机号验证码验证，确保安全性：
- ✅ 必须先获取验证码
- ✅ 验证码 6 位数字
- ✅ 验证码有效期 60 秒
- ✅ 验证成功后才能重置密码

---

## 实现内容

### 1. UI 改进

**新增元素**：
- 验证码输入框（6 位数字）
- 获取验证码按钮
- 60 秒倒计时
- 手机号格式验证
- 分步操作流程

**操作流程**：
```
1. 输入手机号 → 2. 获取验证码 → 3. 输入验证码 → 4. 重置密码
```

### 2. 前端逻辑

**状态管理**：
```swift
@State private var phone = ""              // 手机号
@State private var verifyCode = ""         // 验证码
@State private var newPassword = ""        // 新密码
@State private var confirmPassword = ""    // 确认密码
@State private var codeSent = false        // 是否已发送
@State private var countdown = 60          // 倒计时
@State private var isSendingCode = false   // 发送中
@State private var isLoading = false       // 重置中
```

**验证逻辑**：
```swift
// 可以重置密码的条件
private var canResetPassword: Bool {
    return codeSent && 
           !phone.isEmpty && 
           verifyCode.count == 6 && 
           !newPassword.isEmpty && 
           !confirmPassword.isEmpty
}

// 手机号格式验证
private func isValidPhone(_ phone: String) -> Bool {
    let pattern = "^1[3-9]\\d{9}$"
    let predicate = NSPredicate(format: "SELF MATCHES %@", pattern)
    return predicate.evaluate(with: phone)
}
```

**倒计时功能**：
```swift
private func startTimer() {
    timer?.invalidate()
    timer = Timer.scheduledTimer(withTimeInterval: 1.0, repeats: true) { _ in
        if countdown > 0 {
            countdown -= 1
        } else {
            timer?.invalidate()
        }
    }
}
```

### 3. 后端 API

**发送验证码**：
```swift
func sendResetPasswordCode(phone: String) async throws -> Bool {
    let url = URL(string: "\(Self.apiURL)/api.php?action=send_reset_code")!
    var request = URLRequest(url: url)
    request.httpMethod = "POST"
    request.setValue("application/json", forHTTPHeaderField: "Content-Type")
    
    let body: [String: Any] = ["phone": phone]
    request.httpBody = try JSONSerialization.data(withJSONObject: body)
    
    let (data, response) = try await URLSession.shared.data(for: request)
    // ... 处理响应
}
```

**重置密码（带验证码）**：
```swift
func resetPasswordWithCode(phone: String, verifyCode: String, newPassword: String) async throws -> Bool {
    let url = URL(string: "\(Self.apiURL)/api.php?action=reset_password")!
    var request = URLRequest(url: url)
    request.httpMethod = "POST"
    request.setValue("application/json", forHTTPHeaderField: "Content-Type")
    
    let body: [String: Any] = [
        "phone": phone,
        "verify_code": verifyCode,
        "new_password": newPassword
    ]
    request.httpBody = try JSONSerialization.data(withJSONObject: body)
    
    let (data, response) = try await URLSession.shared.data(for: request)
    // ... 处理响应
}
```

---

## 修改的文件

### AuthView.swift
```diff
+ import Combine
+ @State private var verifyCode = ""
+ @State private var isSendingCode = false
+ @State private var codeSent = false
+ @State private var countdown = 60
+ @State private var timer: Timer?
+ sendVerifyCode()              // 发送验证码
+ isValidPhone()                // 手机号验证
+ startTimer()                  // 倒计时
+ canResetPassword              // 计算属性
+ countdownText                 // 倒计时文本
- resetPassword()               // 旧版本（无验证码）
+ resetPassword()               // 新版本（带验证码验证）
```

### DataManager.swift
```diff
+ sendResetPasswordCode()       // 发送验证码 API
+ resetPasswordWithCode()       // 重置密码 API（带验证码）
- resetPassword()               // 旧版本（保留向后兼容）
```

---

## API 接口

### 1. 发送验证码
```http
POST /api.php?action=send_reset_code
Content-Type: application/json

{
    "phone": "18888888889"
}

Response:
{
    "success": true,
    "message": "验证码已发送"
}
```

### 2. 重置密码
```http
POST /api.php?action=reset_password
Content-Type: application/json

{
    "phone": "18888888889",
    "verify_code": "123456",
    "new_password": "newpass123"
}

Response:
{
    "success": true,
    "message": "密码重置成功"
}
```

---

## 用户体验

### 正常流程
1. 输入手机号 `18888888889`
2. 点击"获取验证码"
3. 显示"✅ 验证码已发送"
4. 按钮变为"60 秒后重发"并开始倒计时
5. 输入 6 位验证码
6. 输入新密码和确认密码
7. 点击"重置密码"
8. 显示"✅ 密码重置成功，请登录"
9. 1.5 秒后自动关闭，返回登录页面

### 错误处理
- **手机号格式错误** → "请输入正确的手机号"
- **验证码错误** → "验证码错误或已过期，请重新获取"
- **密码不一致** → "两次输入的密码不一致"
- **密码太短** → "密码至少 6 位"
- **网络错误** → "网络错误，请稍后重试"

---

## 安全性提升

### 之前（❌ 不安全）
```
知道手机号 → 直接重置密码 → 账号被盗
```

### 现在（✅ 安全）
```
知道手机号 → 需要手机接收验证码 → 验证成功 → 重置密码
```

**安全机制**：
1. ✅ 验证码随机生成
2. ✅ 验证码有效期 60 秒
3. ✅ 验证码使用一次即失效
4. ✅ 60 秒内不能重复发送
5. ✅ 手机号格式验证

---

## 📱 测试建议

### 测试 1: 正常流程
1. 登录页面 → 忘记密码
2. 输入手机号 `18888888889`
3. 点击"获取验证码"
4. **预期**：显示"✅ 验证码已发送"，按钮开始倒计时
5. 输入验证码 `123456`
6. 输入新密码 `test123456`
7. 确认密码 `test123456`
8. 点击"重置密码"
9. **预期**：显示"✅ 密码重置成功，请登录"

### 测试 2: 验证码倒计时
1. 点击"获取验证码"
2. **预期**：按钮显示"59 秒后重发"
3. 等待 60 秒
4. **预期**：按钮显示"重新发送"，可再次点击

### 测试 3: 手机号验证
1. 输入错误手机号 `12345678901`
2. 点击"获取验证码"
3. **预期**：显示"请输入正确的手机号"

### 测试 4: 验证码长度
1. 输入 5 位验证码
2. **预期**：重置密码按钮不可点击
3. 输入 6 位验证码
4. **预期**：重置密码按钮可点击

---

## ✅ 状态
- ✅ BUILD SUCCEEDED
- ✅ 已安装到 iPhone 17 Pro 模拟器
- ✅ 待推送

---

## 下一步

继续实现**语音/视频录制功能**（时光胶囊媒体录制）
