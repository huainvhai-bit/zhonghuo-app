# 🔧 依赖修复和功能恢复方案

## 当前状态

### ✅ 已正常工作的依赖
- SwiftUI
- Foundation
- UIKit
- CoreMotion
- CloudKit
- AVFoundation/AVKit
- PDFKit
- MapKit
- CoreLocation
- UserNotifications
- Combine
- Network
- CoreImage

### ⏸️ 待恢复的功能
- CodeScanner（扫码绑定家人）

---

## 方案：恢复 CodeScanner 功能

### 步骤 1：确认 Package 已正确添加

检查项目文件：
```bash
grep -A 5 "CodeScanner" 终活.xcodeproj/project.pbxproj
```

应该看到：
- XCRemoteSwiftPackageReference "CodeScanner"
- XCSwiftPackageProductDependency
- CodeScanner in Frameworks

### 步骤 2：恢复 CodeScanner 代码

**BindFamilyView.swift**:
```swift
// 取消注释
import CodeScanner

// 恢复扫码按钮
Button(action: { showingScanner = true }) {
    Image(systemName: "qrcode")
        .font(.system(size: 20))
        .foregroundColor(.indigo)
        .padding(10)
        .background(Color.indigo.opacity(0.1))
        .cornerRadius(8)
}

// 恢复 sheet
.sheet(isPresented: $showingScanner) {
    CodeScannerView(codeTypes: [.qr], simulatedData: "", completion: handleScan)
}

// 恢复 handleScan 函数
private func handleScan(result: Result<ScanResult, ScanError>) {
    switch result {
    case .success(let result):
        let code = extractInviteCode(from: result.string)
        if !code.isEmpty {
            inviteCode = code
            showingScanner = false
            bindFamily()
        } else {
            errorMessage = "无效的二维码"
            showingError = true
        }
    case .failure(let error):
        errorMessage = error.localizedDescription
        showingError = true
        showingScanner = false
    }
}
```

**FamilyGuardView.swift**:
```swift
// 取消注释
import CodeScanner
```

### 步骤 3：测试构建

**模拟器**:
```bash
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
```

**真机**:
```bash
xcodebuild -scheme 终活 -destination 'generic/platform=iOS' build
```

---

## 替代方案：如果 CodeScanner 仍有问题

### 方案 A：使用系统相机 API

创建自定义二维码扫描器，使用 Vision 框架：

```swift
import Vision
import AVFoundation

class QRCodeScanner: NSObject, ObservableObject {
    // 使用 Vision 框架识别二维码
}
```

### 方案 B：保持现状

继续使用输入邀请码方式，扫码功能作为可选增强。

---

## 其他依赖检查

### 系统框架（无需 Package）
- ✅ CloudKit - 系统自带
- ✅ CoreMotion - 系统自带
- ✅ MapKit - 系统自带
- ✅ AVFoundation - 系统自带
- ✅ PDFKit - 系统自带

### 第三方依赖
- ⏸️ CodeScanner - 需要 Swift Package

---

## 功能清单

### 核心功能（100% 可用）
1. ✅ 用户认证（登录/注册）
2. ✅ 首页签到
3. ✅ 时光胶囊（文字）
4. ✅ 遗嘱与资产管理
5. ✅ 见证人管理
6. ✅ 家人守护
7. ✅ 设备信息监控
8. ✅ 设置页面
9. ✅ 通知管理
10. ✅ PDF 导出

### 增强功能（90% 可用）
- ⏸️ 扫码绑定家人（需要 CodeScanner）
- ✅ 输入邀请码绑定家人

### 未来功能
- 语音/视频时光胶囊
- iCloud 云同步
- 成就系统

---

*更新时间：2026-03-20 18:45*
