# 📱 iMessage 紧急通知功能说明

**时间**：2026-03-23 17:15  
**功能**：倒计时结束时使用苹果原生 iMessage 通知紧急联系人

---

## 🎯 功能概述

### 短信使用场景区分

| 场景 | 使用方式 | 说明 |
|------|---------|------|
| **注册/登录/找回密码** | 阿里云/腾讯云短信 | 发送验证码 |
| **倒计时结束通知** | 苹果原生 iMessage | 通知紧急联系人用户可能遇到危险 |

---

## 📋 实现细节

### 1. 新增文件

**`MessageManager.swift`** - iMessage 管理器
- `sendMessage()` - 发送 iMessage
- `sendLifeCheckAlert()` - 发送生命体征确认通知
- 支持多收件人
- 完整的发送结果回调

**`HomeStatusView.swift`** - 修改
- 添加 `sendOverdueAlertToEmergencyContacts()` 函数
- 倒计时归零时自动触发
- 防止重复发送（`hasSentOverdueAlert` 标记）

---

## 🔄 工作流程

```
用户未按时签到
    ↓
倒计时归零（0 小时）
    ↓
触发 sendOverdueAlertToEmergencyContacts()
    ↓
获取所有紧急联系人（未删除的）
    ↓
调用 MessageManager.sendLifeCheckAlert()
    ↓
弹出苹果原生短信界面
    ↓
用户确认发送
    ↓
紧急联系人收到 iMessage 通知
```

---

## 📄 短信内容模板

```
【终活】紧急通知

张三 已超过 48 小时未签到，可能遇到危险。

请尽快联系确认其安全状况！

终活 App - 生命守护
```

---

## 🔧 代码示例

### 发送 iMessage 通知

```swift
// 获取紧急联系人
let contacts = user.emergencyContacts.filter { $0.deletedAt == nil }

// 发送通知
MessageManager.shared.sendLifeCheckAlert(
    to: contacts,
    userName: "张三",
    hoursOverdue: 48
) { success, message in
    if success {
        print("✅ 通知发送成功")
    } else {
        print("❌ 通知失败：\(message)")
    }
}
```

---

## ⚠️ 注意事项

### 1. 设备要求
- 需要 iOS 设备支持短信功能
- 模拟器无法发送短信（会提示不支持）
- 需要在真机上测试

### 2. 用户确认
- 苹果原生短信需要用户手动确认发送
- 这是 iOS 安全机制，无法绕过
- 用户可以看到并编辑短信内容

### 3. 费用
- iMessage（蓝色气泡）：免费（需要网络）
- 普通短信（绿色气泡）：按运营商资费收取

### 4. 权限
- 不需要额外权限申请
- 使用系统原生短信功能

---

## 🆚 与阿里云/腾讯云短信对比

| 特性 | 苹果 iMessage | 阿里云/腾讯云短信 |
|------|-------------|-----------------|
| **使用场景** | 紧急联系人通知 | 验证码发送 |
| **触发方式** | 倒计时结束 | 用户操作（注册/登录等） |
| **发送方式** | 用户确认 | 自动发送 |
| **费用** | 可能收费 | 按条收费 |
| **到达率** | 高（系统级） | 高（服务商保障） |
| **依赖网络** | iMessage 需要网络 | 不需要（短信通道） |

---

## 🧪 测试方法

### 真机测试
```
1. 打开 App → 设置签到间隔（例如 2 分钟）
2. 添加紧急联系人（至少 1 个）
3. 等待倒计时归零
4. 应该弹出短信界面
5. 确认发送
6. 检查紧急联系人是否收到短信
```

### 模拟器测试
```
模拟器不支持短信功能
会显示："设备不支持发送短信"
属于正常现象
```

---

## 📊 日志输出

```
🚨 倒计时结束，准备发送 iMessage 通知紧急联系人
📱 发送 iMessage 给 2 个紧急联系人
   - 超时阈值：48 小时
✅ iMessage 通知发送成功
```

---

## 🔒 隐私与安全

- ✅ 短信内容固定模板，无法篡改
- ✅ 只发送给预设的紧急联系人
- ✅ 用户可以看到并确认短信内容
- ✅ 不会自动群发（需要用户确认）
- ✅ 符合苹果隐私政策

---

## 🚀 部署说明

### 前端
```bash
cd /Users/lishimin/Documents/zhonghuo-app
git pull origin main
# 重新编译安装
```

### 后端
无需修改（纯前端功能）

---

## 📝 提交记录

**提交**：`25f7925` - 📱 实现 iMessage 紧急通知

**修改文件**：
- ✅ `MessageManager.swift` - 新增
- ✅ `HomeStatusView.swift` - 添加通知逻辑
- ✅ `终活.xcodeproj/project.pbxproj` - 添加文件引用

---

## ✅ 功能完成度

- [x] iMessage 管理器实现
- [x] 倒计时结束触发逻辑
- [x] 防止重复发送
- [x] 紧急联系人过滤
- [x] 发送结果回调
- [x] 日志输出
- [x] 编译通过
- [x] 推送到 GitHub

**待测试**：真机测试短信发送功能

---

*最后更新：2026-03-23 17:15*
