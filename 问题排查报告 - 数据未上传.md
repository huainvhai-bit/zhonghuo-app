# 问题排查报告 - 数据未上传

## 📊 当前状态

**时间：** 2026-03-19 13:12  
**问题：** 后端数据库所有表都是 0 条记录

### 后端状态 ✅
```bash
# 服务器配置
http://8.136.41.211:3395/api/check-config.php
✅ 配置加载成功
✅ 数据库连接成功
✅ API 端点正常

# 数据库状态
users: 1 条 ✅
capsules: 0 条 ❌
will_modules: 0 条 ❌
user_locations: 0 条 ❌
emergency_contacts: 0 条 ❌
witnesses: 0 条 ❌
```

### 前端状态 ✅
```bash
# 编译状态
✅ BUILD SUCCEEDED

# 安装状态
✅ 已安装到 iPhone 17 Pro 模拟器

# 代码状态
✅ HomeStatusView.onAppear 调用 forceUploadDataOnAppOpen()
✅ AuthView 登录后调用同步
✅ DataManager 实现 batchSync 和 downloadAllData
```

---

## 🔍 可能原因分析

### 原因 1：用户未登录 ❓
**检查：** 用户是否成功登录？
- 如果未登录，不会触发同步
- Token 为空，API 调用会失败

**验证方法：**
```
打开 App → 查看是否自动跳转到首页
如果停留在登录页 → 用户未登录
```

### 原因 2：网络请求失败 ❓
**检查：** 前端能否访问后端 API？

**验证方法：**
```bash
# 在模拟器 Safari 中访问
http://8.136.41.211:3395/api/check-config.php

# 如果无法访问 → 网络问题
# 如果可以访问 → 代码问题
```

### 原因 3：Token 无效 ❓
**检查：** 登录后是否保存了 token？

**验证方法：**
```swift
// 检查 UserDefaults
let token = UserDefaults.standard.string(forKey: "userToken")
print("Token: \(token ?? "nil")")
```

### 原因 4：API 调用失败但无日志 ❓
**检查：** 是否看到同步日志？

**预期日志：**
```
🟢 首页 onAppear - 触发自动签到
📤 打开 App 强制上传数据（胶囊、遗嘱、位置等）
🔄 ====== 打开 App 智能同步数据 ======
📥 1. 从云端下载数据...
📤 2. 上传本地新数据到云端...
📍 上传位置信息...
✅ 位置上传成功
📦 同步胶囊数据...
✅ 胶囊同步完成
```

**如果没有看到日志：**
- onAppear 没有被调用
- 用户未登录，提前返回
- 代码没有正确编译

### 原因 5：后端 API 接口问题 ❓
**检查：** 后端 API 是否正确处理请求？

**验证方法：**
```bash
# 测试胶囊列表 API
curl "http://8.136.41.211:3395/api/capsules.php?action=list&token=test"

# 测试位置上传 API
curl -X POST "http://8.136.41.211:3395/api/location.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "upload",
    "token": "test",
    "user_id": "test",
    "latitude": 39.9042,
    "longitude": 116.4074
  }'
```

---

## 🛠️ 排查步骤

### 步骤 1：检查用户登录状态

**操作：**
1. 打开模拟器上的终活 App
2. 查看当前页面
   - 如果是登录页 → 需要登录
   - 如果是首页 → 已登录

**登录测试：**
- 手机号：`13800138006`
- 密码：`test123456`

### 步骤 2：查看 Xcode 控制台日志

**操作：**
1. 打开 Xcode
2. 打开项目：`/Users/lishimin/Documents/zhonghuo-app`
3. 选择方案：终活
4. 选择设备：iPhone 17 Pro
5. 点击运行（Cmd+R）
6. 打开调试控制台（Cmd+Shift+Y）

**预期日志：**
```
🔵 登录成功
📥 开始从服务器下载数据...
📤 登录后强制上传数据...
📍 上传位置...
📦 同步胶囊...
```

### 步骤 3：测试后端 API

**测试胶囊下载：**
```bash
curl -s "http://8.136.41.211:3395/api/capsules.php?action=list&token=test" | jq .
```

**预期响应：**
```json
{
  "success": true,
  "data": []
}
```

**测试位置上传：**
```bash
curl -s -X POST "http://8.136.41.211:3395/api/location.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "upload",
    "token": "test",
    "user_id": "test",
    "latitude": 39.9042,
    "longitude": 116.4074,
    "address": "北京市"
  }' | jq .
```

**预期响应：**
```json
{
  "success": true,
  "message": "位置上传成功"
}
```

### 步骤 4：检查数据库

**SSH 登录服务器：**
```bash
ssh root@8.136.41.211
```

**查看数据库：**
```bash
mysql -u zhonghuo -p zhonghuo

# 查看所有表
SHOW TABLES;

# 查看用户数
SELECT COUNT(*) FROM users;

# 查看位置数据
SELECT * FROM user_locations ORDER BY created_at DESC LIMIT 5;

# 查看胶囊数据
SELECT * FROM capsules ORDER BY created_at DESC LIMIT 5;
```

### 步骤 5：添加详细日志

**修改 UserManager.swift：**
```swift
func uploadLocation() {
    print("📍 [位置上传] 开始...")
    print("📍 [位置上传] currentUser: \(currentUser?.name ?? "nil")")
    print("📍 [位置上传] API URL: \(DataManager.apiURL)")
    
    // ... 现有代码
    
    print("📍 [位置上传] 请求 URL: \(url)")
    print("📍 [位置上传] 请求体：\(String(data: jsonData, encoding: .utf8) ?? "nil")")
}
```

**修改 DataManager.swift：**
```swift
func batchSyncCapsules() async -> (total: Int, created: Int, updated: Int)? {
    print("📦 [胶囊同步] 开始...")
    print("📦 [胶囊同步] API URL: \(DataManager.apiURL)")
    print("📦 [胶囊同步] 本地胶囊数：\(capsules.count)")
    
    // ... 现有代码
}
```

---

## 🎯 快速诊断脚本

### 前端诊断
```bash
# 1. 检查 App 是否安装
xcrun simctl list apps "iPhone 17 Pro" | grep zhonghuo

# 2. 查看 App 沙盒文件
xcrun simctl get_app_container "iPhone 17 Pro" com.zhonghuo.app

# 3. 打开 App
xcrun simctl launch "iPhone 17 Pro" com.zhonghuo.app
```

### 后端诊断
```bash
# 1. 检查服务器状态
curl http://8.136.41.211:3395/api/check-config.php

# 2. 检查诊断页面
curl http://8.136.41.211:3395/api/diagnose-all.php | grep -o "[0-9] 条" | head -7

# 3. 查看后端日志
ssh root@8.136.41.211
tail -f /www/wwwroot/zhonghuo.cn/api/error.log
```

---

## ✅ 解决方案

### 方案 1：确保用户登录
- 打开 App → 登录账号
- 检查是否保存 token
- 查看是否跳转到首页

### 方案 2：检查网络连接
- 模拟器能否访问外网？
- 服务器端口 3395 是否开放？
- 防火墙是否阻止？

### 方案 3：查看详细日志
- 打开 Xcode 控制台
- 查看是否有错误信息
- 根据错误信息修复

### 方案 4：手动触发同步
```swift
// 在 Xcode 控制台执行
po DataManager.shared.batchSyncCapsules()
```

### 方案 5：重置应用
```bash
# 卸载 App
xcrun simctl uninstall "iPhone 17 Pro" com.zhonghuo.app

# 重新安装
xcrun simctl install "iPhone 17 Pro" $(find ~/Library/Developer/Xcode/DerivedData -name "终活.app" -type d | head -1)

# 打开 App
xcrun simctl launch "iPhone 17 Pro" com.zhonghuo.app
```

---

## 📋 检查清单

- [ ] 用户已登录
- [ ] Token 已保存
- [ ] 网络连接正常
- [ ] 后端 API 可访问
- [ ] Xcode 控制台有日志
- [ ] 看到"打开 App 智能同步数据"日志
- [ ] 看到"上传位置"日志
- [ ] 看到"胶囊同步完成"日志
- [ ] 后端数据库有数据

---

## 🚨 紧急联系

如果以上方法都无效：
1. 截图 Xcode 控制台日志
2. 截图 App 当前页面
3. 提供后端诊断页面输出
4. 检查服务器日志

---

*最后更新：2026-03-19 13:12*
