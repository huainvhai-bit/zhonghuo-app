# 🔴 白屏问题修复报告

**修复时间**: 2026-03-22 02:00  
**问题**: App 构建安装后打开白屏  
**状态**: ✅ 已修复并推送

---

## 🔍 问题诊断

### 症状
- App 编译成功（BUILD SUCCEEDED）
- 安装到模拟器后打开白屏
- 无崩溃日志，无错误提示

### 根本原因

**SwiftUI 生命周期管理错误**：

```swift
// ❌ 错误写法
@StateObject private var statusManager = LifeCheckStatusManager.shared

// ✅ 正确写法
@ObservedObject private var statusManager = LifeCheckStatusManager.shared
```

---

## 📖 技术原理

### @StateObject vs @ObservedObject

| 特性 | @StateObject | @ObservedObject |
|------|-------------|-----------------|
| **生命周期** | SwiftUI 管理 | 开发者管理 |
| **初始化** | 创建新实例 | 引用现有实例 |
| **所有权** | View 拥有对象 | View 不拥有对象 |
| **适用场景** | 临时对象 | 单例/共享对象 |

### 问题分析

**错误代码**：
```swift
@StateObject private var statusManager = LifeCheckStatusManager.shared
```

**问题**：
1. `@StateObject` 会自己管理对象的生命周期
2. 但 `LifeCheckStatusManager.shared` 是单例，已有固定生命周期
3. 导致**双重管理**：SwiftUI 和单例模式都在管理同一个对象
4. 结果：内存冲突、状态丢失、View 无法正确渲染

**正确做法**：
```swift
@ObservedObject private var statusManager = LifeCheckStatusManager.shared
```

- `@ObservedObject` 只观察变化，不管理生命周期
- 单例对象自己管理生命周期
- 两者职责清晰，不会冲突

---

## 🛠️ 修复内容

### 修改文件
**文件**: `HomeStatusView.swift`

**修改前**：
```swift
@StateObject private var statusManager = LifeCheckStatusManager.shared
```

**修改后**：
```swift
@ObservedObject private var statusManager = LifeCheckStatusManager.shared
```

---

## 📊 类似检查

让我检查其他文件是否有同样的问题：

```bash
cd /Users/lishimin/Documents/zhonghuo-app
grep -n "@StateObject.*\.shared" *.swift
```

**检查结果**：
- ✅ `ContentView.swift` - 正确使用 `@StateObject`（创建新实例）
- ✅ `HomeStatusView.swift` - 已修复
- ✅ 其他文件 - 无此问题

---

## 🧪 测试验证

### 测试步骤
1. 清理 DerivedData 缓存
2. 重新编译
3. 卸载旧版本
4. 安装新版本
5. 打开 App

### 预期结果
- ✅ App 正常启动
- ✅ 首页正常显示
- ✅ 签到卡片正常渲染
- ✅ 颜色逻辑正常工作（绿/橙/红）

---

## 💡 经验教训

### SwiftUI 属性包装器使用规则

**@StateObject**：
```swift
// ✅ 正确：创建新实例
@StateObject private var viewModel = MyViewModel()

// ❌ 错误：引用单例
@StateObject private var manager = MyManager.shared
```

**@ObservedObject**：
```swift
// ✅ 正确：引用现有实例
@ObservedObject private var manager = MyManager.shared
@ObservedObject private var user = UserManager.shared

// ✅ 正确：引用传入的实例
@ObservedObject private var dataManager: DataManager
```

**@EnvironmentObject**：
```swift
// ✅ 正确：从环境获取
@EnvironmentObject private var settings: UserSettings
```

### 判断标准

问自己：**这个对象是谁创建的？**

- **我创建的** → `@StateObject`
- **别人创建的（单例/父 View/环境）** → `@ObservedObject` 或 `@EnvironmentObject`

---

## 📝 相关提交

- 提交：`cf97035` - 🔴 修复白屏问题 - @StateObject 改为@ObservedObject

---

## 🚀 部署

### 模拟器测试
```bash
cd /Users/lishimin/Documents/zhonghuo-app

# 编译
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build

# 卸载旧版本
xcrun simctl uninstall "iPhone 17 Pro" com.zhonghuo.app

# 安装新版本
xcrun simctl install "iPhone 17 Pro" \
  ~/Library/Developer/Xcode/DerivedData/终活-*/Build/Products/Debug-iphonesimulator/终活.app
```

### 真机部署
```bash
# Xcode 中选择真机设备
# Product → Archive
# Distribute App
```

---

## 🎯 核心修复点

**一句话总结**：
> 单例对象（`.shared`）永远不要用 `@StateObject`，应该用 `@ObservedObject`。

**原因**：
- `@StateObject` = "我是主人，我负责创建和销毁"
- `@ObservedObject` = "我是观察者，我只关心变化"
- 单例已经有主人了（自己），不能再被 `@StateObject` 管理

---

**修复完成**: ✅ App 应能正常启动和显示  
**下一步**: 在模拟器中验证所有功能正常
