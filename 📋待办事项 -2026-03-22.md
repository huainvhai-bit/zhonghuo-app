# 📋 待办事项 - 终活 App Bug 修复

**创建时间**: 2026-03-22 02:40  
**优先级**: 高 → 低

---

## 🔴 高优先级（明天必须修复）

### 1️⃣ 后端 Token 同步问题
**现象**: 
```
📡 胶囊同步响应状态码：401
📄 胶囊同步响应：{"error":"Token 无效","code":"INVALID_TOKEN"}
```

**日志**:
```
🔵 ====== recordCheckIn 开始 ======
- Token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
- Token 长度：274
✅ Token 有效，过期时间：2026-03-28 17:10:10 +0000

但同步时：
⚠️ 遗嘱同步失败：无 token（用户未登录）
⚠️ 紧急联系人同步失败：无 token（用户未登录）
```

**问题**: Token 在登录时有效，但同步时丢失或未传递

**排查方向**:
- `RealTimeSyncManager` 中 Token 获取逻辑
- `batchSyncCapsules` 等方法的 Token 传递
- 用户登录成功后 Token 是否正确保存

**修复步骤**:
1. 检查 `UserManager.currentUser?.token`
2. 检查 `RealTimeSyncManager` 中 Token 获取
3. 添加详细日志定位 Token 丢失位置

---

### 2️⃣ API 配置加载失败
**现象**:
```
❌ 获取 API 配置失败：keyNotFound(CodingKeys(stringValue: "success", intValue: nil), ...)
📄 系统配置响应：{"status":"success","data":{...}}
```

**问题**: 响应格式不匹配
- 代码期望：`{"success": true, "data": {...}}`
- 实际返回：`{"status":"success","data":{...}}`

**修复**:
```swift
// 修改 JSON 解码逻辑
// 将 "success" 改为 "status"
// 或者修改后端返回格式
```

**相关文件**:
- `DataManager.swift` - `loadSystemConfig()`
- 后端 `api/config_get.php`

---

### 3️⃣ 位置上传跳过
**现象**:
```
⚠️ 无 token，跳过位置上传
📍 2. 上传位置...
⚠️ 无 token，跳过位置上传
```

**问题**: 位置上传时 Token 为空

**排查**:
- `uploadLocation()` 方法中 Token 获取
- 是否在用户登录完成后才调用

---

## 🟡 中优先级（本周内修复）

### 4️⃣ 后台任务重新实现
**当前状态**: ⚠️ 临时禁用

**问题**:
- 后台短信通知无法自动触发
- 后台通知刷新无法自动执行

**替代方案**（当前）:
- 用户打开 App 时触发通知检查
- 签到提醒通过本地通知实现

**长期方案**:
```swift
// 在 AppDelegate 中注册（最可靠）
class AppDelegate: NSObject, UIApplicationDelegate {
    func application(_ application: UIApplication,
                     didFinishLaunchingWithOptions ...) -> Bool {
        BGTaskScheduler.shared.register(
            forTaskWithIdentifier: "com.zhonghuo.app.sms_notify",
            using: nil
        ) { task in
            self.handleBackgroundSmsTask(task: task as! BGAppRefreshTask)
        }
        return true
    }
}
```

**前提条件**:
1. 恢复 `Info.plist` 中的后台任务声明
2. 确保真机签名配置正确
3. 测试后台任务触发

---

### 5️⃣ 紧急联系人不足提示
**现象**:
```
👥 检查紧急联系人数量：0 人（要求：2 人）
⚠️ 紧急联系人不足 2 人，显示提示
```

**问题**: 用户未设置紧急联系人

**优化**:
- 在首页添加明显提示
- 引导用户设置紧急联系人
- 可能需要在首次登录时强制设置

---

### 6️⃣ 签到倒计时显示问题
**现象**:
```
🔄 updateStatus: secondsRemaining=60.88, isSafe=true
⚠️ 倒计时紧急：只剩 1 分钟，立即提醒
```

**问题**: 自动签到后倒计时应该重置为 48 小时

**排查**:
- `recordCheckIn` 成功后是否更新 `lastCheckInDate`
- `updateStatus` 是否正确读取最新数据

---

## 🟢 低优先级（有空时优化）

### 7️⃣ 日志输出优化
**问题**: 日志太多，难以定位关键问题

**优化**:
- 使用 `DebugConfig.swift` 控制日志级别
- 生产环境减少日志输出
- 关键错误使用 `os_log`

---

### 8️⃣ 错误处理增强
**问题**: 部分错误没有用户提示

**优化**:
- 网络错误显示友好提示
- Token 过期自动刷新或重新登录
- 数据同步失败显示重试按钮

---

### 9️⃣ 性能优化
**问题**: 启动时大量同步任务

**优化**:
- 延迟非关键同步任务
- 使用后台队列处理
- 添加进度提示

---

## 📊 修复进度

| 优先级 | 数量 | 已完成 | 待修复 |
|--------|------|--------|--------|
| 🔴 高 | 3 | 0 | 3 |
| 🟡 中 | 3 | 0 | 3 |
| 🟢 低 | 3 | 0 | 3 |
| **总计** | **9** | **0** | **9** |

---

## 🎯 明天计划

### 上午（高优先级）
1. 修复 Token 同步问题（401 错误）
2. 修复 API 配置加载失败
3. 修复位置上传跳过问题

### 下午（中优先级）
4. 重新实现后台任务
5. 优化紧急联系人提示
6. 修复签到倒计时显示

### 晚上（低优先级）
7. 日志输出优化
8. 错误处理增强
9. 性能优化

---

## 📝 测试清单

### 核心功能
- [ ] App 启动正常
- [ ] 用户登录成功
- [ ] 自动签到成功
- [ ] 数据同步成功（胶囊、遗嘱、资产等）
- [ ] 位置上传成功
- [ ] 通知提醒正常

### 后台功能
- [ ] 后台任务注册成功
- [ ] 后台短信通知触发
- [ ] 后台通知刷新触发

### 用户体验
- [ ] 无白屏
- [ ] 无卡顿
- [ ] 错误提示友好
- [ ] 倒计时显示正确

---

## 🔗 相关文档

- `/Users/lishimin/Documents/zhonghuo-app/🔴真机白屏修复报告.md`
- `/Users/lishimin/Documents/zhonghuo-app/🔴终极修复报告 - 简化启动逻辑.md`
- `/Users/lishimin/Documents/zhonghuo-app/📋待办事项 -2026-03-22.md`

---

**最后更新**: 2026-03-22 02:40  
**明天继续**! 💪
