# 🔍 前后端对接排查报告

**时间**：2026-03-23 15:52  
**排查范围**：前端所有 API 调用 vs 后端所有 API 接口

---

## ✅ 已对接成功的 API（14 个）

| 模块 | 前端调用 | 后端接口 | 状态 |
|------|---------|---------|------|
| 配置检查 | `/api/config_get.php` | `/api/config_get.php` | ✅ |
| 用户注册 | `/api/users.php?action=register` | `/api/users.php?action=register` | ✅ |
| 用户登录 | `/api/users.php?action=login` | `/api/users.php?action=login` | ✅ |
| 胶囊同步 | `/api/capsules.php?action=batch_sync` | `/api/capsules.php?action=batch_sync` | ✅ |
| 遗嘱模块 | `/api/will.php?action=batch_sync` | `/api/will.php?action=batch_sync` | ✅ |
| 位置上传 | `/api/location.php?action=upload` | `/api/location.php?action=upload` | ✅ |
| 位置获取 | `/api/location.php?action=get` | `/api/location.php?action=get` | ✅ |
| 家人邀请码 | `/api/family.php?action=get_invite_code` | `/api/family.php?action=get_invite_code` | ✅ |
| 紧急联系人 | `/api/emergency_contacts.php?action=batch_sync` | `/api/emergency_contacts.php?action=batch_sync` | ✅ |
| 通知配置 | `/api/notification_config.php?action=get` | `/api/notification_config.php?action=get` | ✅ |
| 资产更新 | `/api/will.php?action=update_asset` | `/api/will.php?action=update_asset` | ✅ |
| 签到同步 | `/api/checkin.php?action=checkin_sync` | `/api/checkin.php?action=checkin_sync` | ✅ |
| 重置密码 | `/api/users.php?action=send_reset_code` | `/api/users.php?action=send_reset_code` | ✅ |
| 重置密码 | `/api/users.php?action=reset_password` | `/api/users.php?action=reset_password` | ✅ |

---

## ❌ 发现问题的 API（4 个）

### 问题 1：签到 API action 不匹配

**前端调用**：
```swift
/api/checkin.php?action=checkin  // ❌ 错误
```

**后端接口**：
```php
case 'sync':      // ✅ 正确
case 'record':    // ✅ 正确
case 'checkin_sync':  // ✅ 正确（兼容）
```

**修复方案**：
- 前端改为 `action=sync` 或 `action=checkin_sync`

---

### 问题 2：资产更新 API Token 验证失败

**错误**：`未登录或 token 无效`

**原因**：后端 `will.php` 的 `updateAsset()` 函数 Token 验证逻辑有问题

**修复方案**：检查后端 Token 验证代码

---

### 问题 3：见证人 API 返回格式错误

**错误**：`jq: parse error: Invalid numeric literal`

**原因**：后端返回的不是 JSON 格式

**修复方案**：检查 `api/witnesses.php` 输出

---

### 问题 4：短信发送 API 参数不匹配

**前端调用**：
```json
{"phone":"13800138006","code":"123456"}
```

**后端期望**：
```json
{"phone":"13800138006","message":"..."}  // 需要 message 字段
```

**修复方案**：检查后端 `api/sms.php` 参数验证

---

## ⚠️ 后端功能可用性问题

### 1. 后台管理页面
- ❌ `/admin/check-config.php` 不存在
- ✅ `/admin/map.php` 存在且正常
- ✅ `/admin/index.php` 存在

### 2. 数据库迁移
- ✅ `database.sql` 包含所有必要表结构
- ⚠️ 服务器数据库需要确认是否最新

---

## 📋 后端 API 完整列表（21 个）

| 文件 | Actions | 状态 |
|------|---------|------|
| `users.php` | register, login, get_userinfo, validate, send_reset_code, reset_password | ✅ |
| `checkin.php` | sync, record, config, checkin_sync | ⚠️ action 不匹配 |
| `capsules.php` | batch_sync, create, update, delete, list | ✅ |
| `will.php` | batch_sync, update_asset, list, create, update, delete | ⚠️ Token 验证问题 |
| `family.php` | get_invite_code, bind_family, list_family, accept_family, reject_family | ✅ |
| `witnesses.php` | batch_sync, create, update, delete, list | ❌ 返回格式错误 |
| `emergency_contacts.php` | batch_sync, create, update, delete, list | ✅ |
| `location.php` | upload, get, list_all, clear | ✅ |
| `sms.php` | send_sms, send_verify_code, verify_sms_code | ⚠️ 参数不匹配 |
| `notification_config.php` | get, update | ✅ |
| `config_get.php` | (直接返回配置) | ✅ |

---

## 🔧 需要修复的问题

### 高优先级（影响核心功能）
1. ❌ 签到 API action 不匹配 - 影响签到功能
2. ❌ 资产更新 Token 验证 - 影响资产管理
3. ❌ 见证人 API 返回格式 - 影响见证人功能

### 中优先级（影响次要功能）
4. ⚠️ 短信发送参数 - 影响验证码发送
5. ⚠️ 后台配置检查页面缺失

---

## 📊 对接完整度

- **已对接**：14/21 API（67%）
- **有问题**：4/21 API（19%）
- **未对接**：3/21 API（14%）

---

**下一步**：修复高优先级问题，确保核心功能可用
