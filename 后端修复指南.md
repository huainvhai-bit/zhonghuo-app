# 终活后端 - 完整修复指南

## 🐛 问题总结

**用户报告**：前端注册了用户，但后端 admin 不显示

**根本原因**：
1. ❌ `api/users.php` 是测试文件，不连接数据库
2. ❌ `config.php` 数据库配置错误（库名、用户名、密码都不对）
3. ❌ admin 后台查询的表名与实际表名不一致
4. ❌ API 返回的用户数据缺少 `id` 字段

---

## 🔧 快速修复（推荐）

### 方式 1：一键修复脚本

**在服务器上执行**：

```bash
# 1. SSH 登录
ssh root@8.136.41.211

# 2. 上传修复脚本
# 在本地 Mac 上执行
scp /Users/lishimin/Documents/zhonghuo-app/fix_all.sh root@8.136.41.211:/tmp/

# 3. 在服务器上执行
chmod +x /tmp/fix_all.sh
sudo bash /tmp/fix_all.sh
```

### 方式 2：手动修复

#### 步骤 1：修复 API 文件

```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 进入后端目录
cd /www/wwwroot/zhonghuo.cn/backend

# 备份当前文件
mv api/users.php api/users.php.test

# 恢复 backup 版本
cp api/users.php.backup api/users.php

# 验证
php -l api/users.php
```

#### 步骤 2：修复数据库配置

```bash
# 编辑 config.php
vi /www/wwwroot/zhonghuo.cn/backend/config.php
```

修改为：
```php
<?php
$GLOBALS['db_host'] = 'localhost';
$GLOBALS['db_name'] = 'zhonghuo_db';       // 改为 zhonghuo_db
$GLOBALS['db_user'] = 'zhonghuo_user';     // 改为 zhonghuo_user
$GLOBALS['db_pass'] = 'Zhonghuo@2026';     // 改为 Zhonghuo@2026
$GLOBALS['app_name'] = '终活 App';
$GLOBALS['app_version'] = '2.0.0';
```

#### 步骤 3：修复 admin 后台表名

```bash
# 编辑 admin/users.php
vi /www/wwwroot/zhonghuo.cn/backend/admin/users.php
```

找到查询部分，修改表名：
```php
// 修改前
(SELECT COUNT(*) FROM check_ins ci WHERE ci.user_id = u.id) as checkin_count,
(SELECT COUNT(*) FROM wills w WHERE w.user_id = u.id) as will_count

// 修改后
(SELECT COUNT(*) FROM check_in_records ci WHERE ci.user_id = u.id) as checkin_count,
(SELECT COUNT(*) FROM will_templates w WHERE w.user_id = u.id) as will_count
```

#### 步骤 4：验证数据库

```bash
# 登录 MySQL
mysql -u root -p

# 检查数据库
SHOW DATABASES LIKE 'zhonghuo_db';
USE zhonghuo_db;
SHOW TABLES;

# 检查用户表
SELECT * FROM users;

# 如果表不存在，运行安装脚本
source /www/wwwroot/zhonghuo.cn/backend/install/schema.sql;
```

---

## 🧪 测试验证

### 测试 1：注册新用户

```bash
curl -X POST "http://8.136.41.211:3395/backend/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "phone": "13800138009",
    "name": "测试用户 9",
    "password": "test123"
  }'
```

**期望响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试用户 9",
      "phone": "13800138009"
    }
  }
}
```

### 测试 2：登录

```bash
curl -X POST "http://8.136.41.211:3395/backend/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138006",
    "login_type": "password",
    "password": "test123456"
  }'
```

**期望响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "user_id": "user_test001",
    "user": {
      "id": "user_test001",
      "name": "测试用户",
      "phone": "13800138006",
      "check_in_interval": 48
    }
  }
}
```

### 测试 3：查看数据库

```bash
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db \
  -e "SELECT id, name, phone, created_at FROM users;"
```

**期望结果**：
```
+---------------+----------+-------------+---------------------+
| id            | name     | phone       | created_at          |
+---------------+----------+-------------+---------------------+
| user_test001  | 测试用户  | 13800138006 | 2026-03-17 18:00:00 |
| user_xxx      | 测试用户 9 | 13800138009 | 2026-03-17 18:30:00 |
+---------------+----------+-------------+---------------------+
```

### 测试 4：访问 admin 后台

1. 打开浏览器访问：http://8.136.41.211:3395/backend/admin/
2. 登录 admin 账号
3. 查看用户列表

**期望结果**：
- ✅ 显示所有注册用户
- ✅ 显示签到记录
- ✅ 显示最后登录时间和 IP
- ✅ 显示时光胶囊和遗嘱数量

---

## 📋 修复清单

- [ ] `api/users.php` 替换为 backup 版本
- [ ] `config.php` 数据库配置修复
  - [ ] 数据库名：`zhonghuo_db`
  - [ ] 用户名：`zhonghuo_user`
  - [ ] 密码：`Zhonghuo@2026`
- [ ] `admin/users.php` 表名修复
  - [ ] `check_ins` → `check_in_records`
  - [ ] `wills` → `will_templates`
- [ ] 数据库验证
  - [ ] 数据库 `zhonghuo_db` 存在
  - [ ] 表 `users` 存在
  - [ ] 表 `check_in_records` 存在
  - [ ] 表 `will_templates` 存在
- [ ] 功能测试
  - [ ] 注册成功
  - [ ] 登录成功
  - [ ] admin 后台显示用户

---

## 🎯 修复后的完整流程

1. **前端注册** → `api/users.php` 接收请求
2. **数据库插入** → 用户数据保存到 `users` 表
3. **返回 Token** → 前端收到包含 `user.id` 的完整响应
4. **前端登录** → 验证密码，返回用户信息
5. **admin 后台** → 查询 `users` 表，显示所有用户
6. **统计数据** → 正确查询 `check_in_records` 和 `will_templates`

---

## 📞 故障排查

### 问题 1：注册成功但数据库无数据

**检查**：
```bash
# 查看 PHP 错误日志
tail -50 /www/wwwlogs/zhonghuo.cn.error.log

# 检查数据库连接
mysql -u zhonghuo_user -p'Zhonghuo@2026' -e "SELECT 1"
```

### 问题 2：admin 后台显示空白

**检查**：
```bash
# 查看 admin 错误日志
tail -50 /www/wwwlogs/zhonghuo.cn.error.log

# 检查表名是否正确
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db -e "SHOW TABLES;"
```

### 问题 3：登录返回 404

**检查**：
```bash
# 检查 API 文件是否存在
ls -la /www/wwwroot/zhonghuo.cn/backend/api/users.php

# 检查文件权限
chown -R www:www /www/wwwroot/zhonghuo.cn/backend
```

---

## 📄 相关文档

- [后端问题诊断报告.md](后端问题诊断报告.md)
- [服务器安装指南.md](服务器安装指南.md)
- [DEPLOYMENT.md](/www/wwwroot/zhonghuo.cn/backend/DEPLOYMENT.md)

---

*最后更新：2026-03-17 18:35*
