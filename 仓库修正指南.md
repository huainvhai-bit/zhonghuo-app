# ⚠️ 仓库修正指南

**问题**：后端文件错误推送到前端仓库（zhonghuo-app），应该推送到后端仓库（zhonghuo-backend-php）

---

## ✅ 已完成的本地修正

### 1. 本地已回退

```bash
cd /Users/lishimin/Documents/zhonghuo-app
git reset --hard 8d70878
```

本地已回到正确的提交（8d70878），删除了错误提交（638c526 和 86034c0）。

### 2. 需要清理远程仓库

远程仓库（zhonghuo-app）还有 2 个错误提交，需要强制推送覆盖。

---

## 📋 手动执行步骤

### 步骤 1：清理前端仓库（zhonghuo-app）

```bash
# 1. 进入前端目录
cd /Users/lishimin/Documents/zhonghuo-app

# 2. 确认当前状态
git log --oneline -3
# 应该看到：
# 8d70878 🔧 修复自动签到功能...
# 91e2be7 🔧 修复签到间隔更新...
# 7d70011 🔧 修复：移除登录调试弹窗...

# 3. 强制推送（覆盖远程的错误提交）
git push origin main --force

# 4. 验证
git status
# 应该显示：Your branch is up to date with 'origin/main'
```

### 步骤 2：推送到后端仓库（zhonghuo-backend-php）

#### 方案 A：临时添加远程仓库

```bash
# 1. 在后端目录操作（如果没有，先克隆）
cd /Users/lishimin/Documents
git clone https://github.com/huainvhai-bit/zhonghuo-backend-php.git
cd zhonghuo-backend-php

# 2. 复制后端文件
cp -r ../zhonghuo-app/install.php .
cp -r ../zhonghuo-app/config.php .
cp -r ../zhonghuo-app/database.sql .
cp -r ../zhonghuo-app/api/ .

# 3. 添加并提交
git add -A
git commit -m "🔧 修复配置保存问题：install.php 正确保存到 config.php"

# 4. 推送
git push origin main
```

#### 方案 B：在当前目录添加远程

```bash
# 1. 在前端目录添加后端远程
cd /Users/lishimin/Documents/zhonghuo-app

# 2. 添加后端远程仓库
git remote add backend https://github.com/huainvhai-bit/zhonghuo-backend-php.git

# 3. 只推送后端相关文件
git add install.php config.php database.sql api/
git commit -m "🔧 修复配置保存问题：install.php 正确保存到 config.php"

# 4. 推送到后端仓库
git push backend main
```

---

## 🔍 验证

### 前端仓库（zhonghuo-app）

```bash
# 查看提交历史
https://github.com/huainvhai-bit/zhonghuo-app/commits/main

# 应该只看到前端相关的提交，没有后端文件
```

### 后端仓库（zhonghuo-backend-php）

```bash
# 查看提交历史
https://github.com/huainvhai-bit/zhonghuo-backend-php/commits/main

# 应该看到最新的后端修复提交
```

---

## 📊 仓库分工

| 仓库 | URL | 内容 |
|------|-----|------|
| **前端** | https://github.com/huainvhai-bit/zhonghuo-app | Swift/SwiftUI 代码（.swift 文件） |
| **后端** | https://github.com/huainvhai-bit/zhonghuo-backend-php | PHP 后端代码（.php 文件） |

---

## ⚠️ 注意事项

### 不要推送的文件

**前端仓库不应该有**：
- ❌ install.php
- ❌ config.php
- ❌ database.sql
- ❌ api/ 目录

**后端仓库不应该有**：
- ❌ .swift 文件
- ❌ xcodeproj 文件
- ❌ Assets.xcassets

---

## 🎯 快速执行命令

```bash
# ========== 清理前端仓库 ==========
cd /Users/lishimin/Documents/zhonghuo-app
git reset --hard 8d70878
git push origin main --force

# ========== 推送到后端仓库 ==========
cd /Users/lishimin/Documents
git clone https://github.com/huainvhai-bit/zhonghuo-backend-php.git
cd zhonghuo-backend-php

cp -r ../zhonghuo-app/install.php .
cp -r ../zhonghuo-app/config.php .
cp -r ../zhonghuo-app/database.sql .
cp -r ../zhonghuo-app/api/ .

git add -A
git commit -m "🔧 修复配置保存问题"
git push origin main
```

---

## 📝 后续改进

### 建议添加 .gitignore

**前端仓库**（zhonghuo-app/.gitignore）：
```gitignore
# 后端文件
*.php
api/
config/database.php
install.php
database.sql
```

**后端仓库**（zhonghuo-backend-php/.gitignore）：
```gitignore
# 前端文件
*.swift
*.xcodeproj/
DerivedData/
```

---

**请在网络恢复后执行上述命令！** 🚀
