# 终活 App 后端 - 服务器安装指南

## 🚀 快速安装（推荐）

### 步骤 1：SSH 登录服务器
```bash
ssh root@8.136.41.211
```

### 步骤 2：进入网站目录
```bash
cd /www/wwwroot/zhonghuo.cn
```

### 步骤 3：克隆后端代码
```bash
# 如果还没有 backend 目录
git clone https://github.com/huainvhai-bit/zhonghuo-backend-php.git backend

# 如果已有 backend 目录，更新代码
cd backend
git pull origin main
```

### 步骤 4：初始化数据库
```bash
mysql -u root -p < backend/init_zhonghuo_db.sql
```
输入 MySQL root 密码后按回车

### 步骤 5：部署 API 文件
```bash
# 复制 API 文件到网站 api 目录
cp backend/api/users.php api/
mkdir -p api/config
cp backend/config/*.php api/config/
```

### 步骤 6：设置文件权限
```bash
chown -R www:www api/
chmod 644 api/*.php
chmod 644 api/config/*.php
```

### 步骤 7：验证部署
```bash
# 检查 PHP 语法
php -l api/users.php

# 测试登录 API
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13800138006","login_type":"password","password":"test123456"}'
```

---

## 📋 完整安装脚本

复制以下内容到服务器执行：

```bash
#!/bin/bash
set -e

echo "🚀 开始安装终活后端..."

# 进入网站目录
cd /www/wwwroot/zhonghuo.cn

# 克隆或更新代码
if [ -d "backend" ]; then
    echo "📦 更新现有代码..."
    cd backend && git pull origin main && cd ..
else
    echo "📦 克隆新代码..."
    git clone https://github.com/huainvhai-bit/zhonghuo-backend-php.git backend
fi

# 初始化数据库
echo "📊 初始化数据库..."
mysql -u root -p < backend/init_zhonghuo_db.sql

# 部署 API
echo "🔧 部署 API 文件..."
cp backend/api/users.php api/
mkdir -p api/config
cp backend/config/*.php api/config/

# 设置权限
echo "🔐 设置权限..."
chown -R www:www api/
chmod 644 api/*.php
chmod 644 api/config/*.php

# 验证
echo "✅ 验证部署..."
php -l api/users.php

echo ""
echo "🎉 安装完成！"
echo ""
echo "测试命令："
echo "curl -X POST 'http://8.136.41.211:3395/api/users.php' \\"
echo "  -H 'Content-Type: application/json' \\"
echo "  -d '{\"action\":\"login\",\"phone\":\"13800138006\",\"login_type\":\"password\",\"password\":\"test123456\"}'"
```

---

## 🧪 测试验证

### 测试 1：登录测试
```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13800138006","login_type":"password","password":"test123456"}'
```

**期望响应**：
```json
{
  "success": true,
  "message": "登录成功",
  "data": {
    "token": "真实的 token...",
    "user_id": "user_test001",
    "user": {
      "id": "user_test001",
      "name": "测试用户",
      "phone": "13800138006"
    }
  }
}
```

### 测试 2：查看数据库
```bash
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db \
  -e "SELECT id, name, phone, created_at FROM users;"
```

**期望结果**：
```
+---------------+----------+-------------+---------------------+
| id            | name     | phone       | created_at          |
+---------------+----------+-------------+---------------------+
| user_test001  | 测试用户  | 13800138006 | 2026-03-17 18:00:00 |
| user_test002  | 测试用户 8 | 13800138008 | 2026-03-17 18:00:00 |
+---------------+----------+-------------+---------------------+
```

---

## ⚠️ 常见问题

### 问题 1：Git 命令不存在
```bash
# 安装 Git
yum install git -y  # CentOS
apt install git -y  # Ubuntu
```

### 问题 2：MySQL 密码错误
```bash
# 重置 MySQL root 密码
# 或联系服务器管理员获取密码
```

### 问题 3：权限不足
```bash
# 确保使用 root 或 sudo 执行
sudo bash 安装脚本
```

### 问题 4：PHP 版本过低
```bash
# 检查 PHP 版本
php -v

# 需要 PHP 7.4+
# 升级 PHP（根据系统选择）
```

---

## 📞 需要帮助？

如果安装过程中遇到问题，请提供：
1. 错误信息截图
2. 服务器系统版本（`cat /etc/os-release`）
3. PHP 版本（`php -v`）
4. MySQL 版本（`mysql -V`）

---

*最后更新：2026-03-17*
