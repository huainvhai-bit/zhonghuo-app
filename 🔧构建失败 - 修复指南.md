# 🔧 前端构建失败 - 修复指南

**错误**: `cannot find 'DeviceMonitor' in scope`

**原因**: 新创建的 Swift 文件没有被添加到 Xcode 项目中

---

## ✅ 解决方案（2 选 1）

### 方案 1：在 Xcode 中手动添加（推荐）

**步骤**:

1. **打开 Xcode 项目**
   ```bash
   cd /Users/lishimin/Documents/zhonghuo-app
   open 终活.xcodeproj
   ```

2. **添加文件**
   - 右键点击左侧项目导航中的 `终活/` 文件夹
   - 选择 **"Add Files to '终活'..."**
   
3. **选择文件**
   
   按住 **Command** 键，同时选择以下 6 个文件：
   - ✅ DeviceMonitor.swift
   - ✅ FamilyMember.swift
   - ✅ FamilyGuardView.swift
   - ✅ InviteCodeView.swift
   - ✅ BindFamilyView.swift
   - ✅ FamilyMemberDetailView.swift
   
4. **确认选项**
   
   在弹出的对话框中，确保勾选：
   - ✅ **Copy items if needed**
   - ✅ **Add to targets: 终活**
   
5. **点击 "Add"**

6. **清理并重新编译**
   ```bash
   # 清理 DerivedData
   rm -rf ~/Library/Developer/Xcode/DerivedData/终活-*
   
   # 重新编译
   cd /Users/lishimin/Documents/zhonghuo-app
   xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
   ```

---

### 方案 2：使用脚本自动添加

**步骤**:

1. **安装 pbxproj 工具**
   ```bash
   pip3 install pbxproj
   ```

2. **运行添加脚本**
   ```bash
   cd /Users/lishimin/Documents/zhonghuo-app
   chmod +x add-files-to-project.sh
   ./add-files-to-project.sh
   ```

3. **清理并重新编译**
   ```bash
   rm -rf ~/Library/Developer/Xcode/DerivedData/终活-*
   xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
   ```

---

## 🔍 验证是否成功

编译成功后，应该看到：
```
** BUILD SUCCEEDED **
```

如果没有错误，说明文件已成功添加。

---

## 📋 需要添加的文件列表

| 文件名 | 说明 | 行数 |
|--------|------|------|
| DeviceMonitor.swift | 设备监控（步数、电量） | ~200 |
| FamilyMember.swift | 家人关系数据模型 | ~90 |
| FamilyGuardView.swift | 家人守护主页 | ~320 |
| InviteCodeView.swift | 邀请码页面 | ~280 |
| BindFamilyView.swift | 绑定家人页面 | ~280 |
| FamilyMemberDetailView.swift | 家人详情页 | ~400 |

**总计**: ~1570 行代码

---

## ⚠️ 常见问题

### Q1: 添加文件后仍然报错？
**A**: 清理 DerivedData 缓存：
```bash
rm -rf ~/Library/Developer/Xcode/DerivedData/终活-*
```

### Q2: Xcode 中看不到新文件？
**A**: 
1. 关闭 Xcode
2. 重新打开项目
3. 检查文件是否在项目中（显示为黑色，不是红色）

### Q3: 提示缺少 CodeScanner？
**A**: 需要添加 CodeScanner 依赖：
```bash
# 在 Xcode 中
# File → Add Packages...
# 输入：https://github.com/CameraKit/CodeScanner.git
```

或者使用 SPM：
```swift
// 在 Package.swift 中添加
dependencies: [
    .package(url: "https://github.com/CameraKit/CodeScanner.git", from: "2.0.0")
]
```

### Q4: 编译时提示缺少权限描述？
**A**: 在 Info.plist 中添加：
```xml
<key>NSMotionUsageDescription</key>
<string>我们需要访问您的运动数据来统计每日步数</string>
```

---

## 🚀 快速编译命令

```bash
cd /Users/lishimin/Documents/zhonghuo-app

# 1. 清理缓存
rm -rf ~/Library/Developer/Xcode/DerivedData/终活-*

# 2. 清理项目
xcodebuild clean -scheme 终活

# 3. 重新编译
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build

# 4. 查看结果
# 如果看到 ** BUILD SUCCEEDED ** 说明成功
```

---

## 📞 仍然无法解决？

查看完整错误日志：
```bash
cd /Users/lishimin/Documents/zhonghuo-app
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build 2>&1 | tee build.log
```

然后查看 `build.log` 文件中的详细错误信息。

---

*更新时间：2026-03-20 17:40*
