# 终活 App 压力测试与性能优化报告

**测试时间**: 2026-03-18  
**目标用户群**: 老年人（需要简洁、稳定、快速响应）  
**预期并发**: 初期 1000+ 日活，后期 10000+ 日活

---

## 📊 压力测试结果

### 1. API 并发测试

**测试工具**: `ab` (Apache Bench)  
**测试场景**: 登录 API (`/api/users.php`)

```bash
# 测试命令
ab -n 1000 -c 100 -p login.json -T 'application/json' http://8.136.41.211:3395/api/users.php
```

**性能指标**:
- 并发请求数：100
- 总请求数：1000
- 目标响应时间：< 500ms（老年人需要更快响应）

---

## 🔧 后端优化清单

### ✅ 已完成优化

1. **数据库连接池**
   - 使用 PDO 持久连接
   - 连接复用，减少连接开销

2. **密码哈希优化**
   - 使用 `password_hash()` / `password_verify()`
   - 避免明文存储，安全性高

3. **错误处理**
   - 统一错误码返回（200 + error code）
   - 避免 404/500 等 HTTP 错误影响客户端解析

### ⏳ 待优化项目

1. **数据库索引优化**
   ```sql
   -- 手机号查询索引（登录高频）
   ALTER TABLE users ADD INDEX idx_phone (phone);
   
   -- 用户 ID 索引（外键查询）
   ALTER TABLE capsules ADD INDEX idx_user_id (user_id);
   ALTER TABLE will_modules ADD INDEX idx_user_id (user_id);
   ALTER TABLE assets ADD INDEX idx_user_id (user_id);
   ```

2. **查询缓存**
   - 用户信息缓存（Redis/Memcached）
   - 配置信息缓存（config.php 结果缓存 24 小时）

3. **API 限流**
   - 登录接口：同一 IP 1 分钟内最多 10 次
   - 注册接口：同一手机号 1 小时内最多 3 次
   - 验证码接口：同一手机号 1 分钟内最多 1 次

4. **慢查询日志**
   ```php
   // 记录执行时间 > 1s 的查询
   if ($executionTime > 1.0) {
       error_log("Slow query: {$sql} - {$executionTime}s");
   }
   ```

5. **数据库连接数限制**
   ```php
   // 防止连接数过多
   $maxConnections = 50;
   $currentConnections = $db->query('SELECT COUNT(*) FROM information_schema.processlist')->fetchColumn();
   if ($currentConnections >= $maxConnections) {
       error('服务器繁忙，请稍后再试', 'SERVER_BUSY', 503);
   }
   ```

---

## 📱 前端优化清单

### ✅ 已完成优化

1. **登录前零 API 调用**
   - 避免并发冲突
   - 减少不必要的网络请求

2. **错误提示友好化**
   - 清晰的中文提示
   - 根据错误码显示具体原因

### ⏳ 待优化项目

1. **加载状态优化**
   ```swift
   // 添加加载动画，避免用户重复点击
   @State private var isLoading = false
   
   Button(action: handleSubmit) {
       HStack {
           if isLoading {
               ProgressView()
                   .scaleEffect(0.8)
           }
           Text(isLoading ? "登录中..." : "登录")
       }
       .disabled(isLoading)  // 防止重复提交
   }
   ```

2. **网络超时处理**
   ```swift
   // 超时时间设置
   config.timeoutIntervalForRequest = 15  // 请求超时 15s
   config.timeoutIntervalForResource = 30 // 资源超时 30s
   ```

3. **离线模式**
   - 本地数据缓存
   - 网络恢复后自动同步

4. **大列表优化**
   - 时光胶囊列表：分页加载（每次 20 条）
   - 使用 `LazyVStack` 延迟渲染

---

## 🚨 老年人特别优化

### 1. 字体大小
- 默认字体：18pt（比标准大 2pt）
- 最小字体：16pt（确保可读性）

### 2. 按钮尺寸
- 最小高度：50pt（方便点击）
- 点击区域：按钮周围扩展 10pt

### 3. 响应时间
- 目标：< 300ms（老年人耐心有限）
- 超时提示：> 5s 显示"网络较慢，请耐心等待"

### 4. 错误提示
- 避免技术术语（如"404"、"JSON 解析失败"）
- 使用简单直白的语言（如"账号不存在"、"密码错误"）

---

## 📈 监控与告警

### 1. 性能监控
```bash
# 服务器监控
- CPU 使用率 > 80% → 告警
- 内存使用率 > 85% → 告警
- 磁盘使用率 > 90% → 告警
- 数据库连接数 > 80% → 告警
```

### 2. 错误监控
- 记录所有 API 错误到日志
- 每日错误统计 > 100 次 → 告警
- 单接口错误率 > 5% → 告警

### 3. 慢查询监控
- 记录执行时间 > 1s 的查询
- 每日慢查询 > 50 次 → 告警

---

## 🧪 压力测试脚本

### 1. 登录接口压力测试
```bash
#!/bin/bash
# test-login-pressure.sh

echo "📊 登录接口压力测试"
echo "并发数：100, 总请求：1000"

ab -n 1000 -c 100 \
   -p login.json \
   -T 'application/json' \
   http://8.136.41.211:3395/api/users.php | \
   grep -E "Requests per second|Time per request|Failed requests"
```

### 2. 注册接口压力测试
```bash
#!/bin/bash
# test-register-pressure.sh

echo "📊 注册接口压力测试"
echo "并发数：50, 总请求：500"

for i in {1..500}; do
    phone="1380013$(printf "%04d" $i)"
    curl -X POST "http://8.136.41.211:3395/api/users.php" \
         -H "Content-Type: application/json" \
         -d "{\"action\":\"register\",\"name\":\"测试用户\",\"phone\":\"$phone\",\"password\":\"test123\"}" &
done

wait
echo "✅ 测试完成"
```

### 3. 数据库连接测试
```bash
#!/bin/bash
# test-db-connections.sh

echo "📊 数据库连接测试"
mysql -h localhost -u zhonghuo -pzhonghuo -e "
SHOW STATUS LIKE 'Threads_connected';
SHOW STATUS LIKE 'Max_used_connections';
SHOW VARIABLES LIKE 'max_connections';
"
```

---

## ✅ 优化验收标准

### 1. 性能指标
- [ ] 登录 API 响应时间 < 300ms（95% 请求）
- [ ] 注册 API 响应时间 < 500ms（95% 请求）
- [ ] 并发 100 用户时，错误率 < 1%
- [ ] 数据库连接数 < 50（正常负载）

### 2. 用户体验
- [ ] 加载动画清晰可见
- [ ] 错误提示简单易懂
- [ ] 按钮尺寸适合老年人点击
- [ ] 字体大小适合老年人阅读

### 3. 系统稳定性
- [ ] 连续运行 24 小时无崩溃
- [ ] 内存泄漏检测通过
- [ ] 数据库连接无泄漏
- [ ] 日志文件自动轮转

---

## 📝 下一步行动

1. **立即执行**:
   - [ ] 添加数据库索引
   - [ ] 实现 API 限流
   - [ ] 添加慢查询日志

2. **本周内**:
   - [ ] 部署 Redis 缓存
   - [ ] 前端添加加载动画
   - [ ] 完成压力测试报告

3. **下周内**:
   - [ ] 实现离线模式
   - [ ] 添加性能监控面板
   - [ ] 配置告警通知

---

**最后更新**: 2026-03-18  
**负责人**: 终活开发团队
