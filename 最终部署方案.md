# 🎯 终活 App - 最终部署方案

**当前位置**：后端代码已部署，数据库未创建  
**下一步**：在宝塔面板创建数据库（3 分钟完成）

---

## 📊 诊断结果

**检查地址**：
```
http://8.136.41.211:3395/api/check-config.php
```

**当前状态**：
- ✅ 代码已部署
- ✅ PHP 环境正常
- ✅ 配置文件正确
- ❌ 数据库未创建（需要手动创建）

---

## 🚀 创建数据库（3 步完成）

### 步骤 1：登录宝塔面板

**访问**：
```
http://8.136.41.211:8888
```

**输入**：
- 用户名：（你的宝塔用户名）
- 密码：（你的宝塔密码）

---

### 步骤 2：添加数据库

1. 点击左侧菜单 **"数据库"**
2. 点击右上角 **"添加数据库"** 按钮
3. 填写以下信息：

| 字段 | 填写内容 | 说明 |
|------|----------|------|
| 数据库名 | `zhonghuo_db` | 必须一致 |
| 用户名 | `zhonghuo_user` | 创建新用户 |
| 密码 | `Zhonghuo@2026` | 必须一致 |
| 权限 | 所有人 | 或"本地服务器" |

4. 点击 **"提交"**

---

### 步骤 3：验证

**刷新诊断页面**：
```
http://8.136.41.211:3395/api/check-config.php
```

**应该显示**：
- ✅ 数据库连接成功
- ✅ users 表存在
- ✅ sms_codes 表存在

---

## 🧪 测试 API

### 测试 1：发送验证码

```bash
curl -X POST "http://8.136.41.211:3395/api/sms.php" \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138099","action":"register"}'
```

**预期响应**：
```json
{
  "success": true,
  "message": "验证码已发送",
  "code": "123456",
  "expiresIn": 300
}
```

---

### 测试 2：注册

```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "name": "测试用户",
    "phone": "13800138099",
    "password": "test123456",
    "verify_code": "123456"
  }'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099"
    }
  }
}
```

---

### 测试 3：登录

```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138099",
    "login_type": "password",
    "password": "test123456"
  }'
```

**预期响应**：同上（`is_new: false`）

---

## 📱 测试 App

### 1. 在 Xcode 中运行

1. 打开 Xcode
2. 打开终活 App 项目
3. 选择 iPhone 17 Pro 模拟器
4. 点击运行（▶️）

### 2. 测试注册

1. 输入姓名：`测试用户`
2. 输入手机号：`13800138099`
3. 输入密码：`test123456`
4. 点击"获取验证码"
5. 输入验证码：`123456`（测试模式）
6. 点击"注册"

**预期**：注册成功，自动跳转到首页

### 3. 测试登录

1. 切换到"登录"
2. 输入手机号：`13800138099`
3. 输入密码：`test123456`
4. 点击"登录"

**预期**：登录成功，显示首页

---

## 🔍 完整诊断

**访问完整诊断页面**：
```
http://8.136.41.211:3395/diagnostic.php
```

**检查所有项目**：
- ✅ PHP 环境
- ✅ 配置文件
- ✅ 数据库连接（绿色）
- ✅ 数据表（users、sms_codes）
- ✅ API 文件
- ✅ API 功能测试

---

## ❓ 常见问题

### Q1: 宝塔面板登录不上？

**解决**：
```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 查看宝塔面板信息
bt default

# 如果忘记密码，重置密码
bt
# 选择选项：5 修改面板密码
```

---

### Q2: 创建数据库失败？

**可能原因**：
- 数据库名已存在
- 用户名已存在

**解决**：
1. 在宝塔面板删除已存在的数据库
2. 重新创建

---

### Q3: 诊断页面还是显示数据库连接失败？

**解决**：
1. 确认数据库名、用户名、密码完全一致
2. 刷新诊断页面（Ctrl+F5 强制刷新）
3. 检查宝塔面板数据库用户权限

---

### Q4: API 测试失败？

**检查**：
```bash
# 测试 config.php
curl http://8.136.41.211:3395/api/config.php

# 应该返回 JSON
{
  "success": true,
  "data": {
    "endpoints": {
      "base": "http://8.136.41.211:3395",
      "api": "http://8.136.41.211:3395/api"
    }
  }
}
```

---

## 📝 总结

**部署流程**：
1. ✅ 上传代码（已完成）
2. ⏳ 在宝塔面板创建数据库（3 分钟）
3. ✅ 测试 API
4. ✅ 测试 App

**不需要**：
- ❌ MySQL root 密码
- ❌ 执行任何命令
- ❌ 复杂的配置

**只需要**：
- ✅ 在宝塔面板点击几下

---

*创建数据库后，刷新诊断页面验证！*
