# 🔧 前端注册网络错误 - 紧急修复指南

## 🚨 问题诊断

**症状**：前端注册提示"网络错误"

**原因**：服务器上的 `api/users.php` 是**测试版本**，返回模拟数据，没有真正连接数据库

**验证**：
```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138009","name":"测试 9","password":"test123"}'
```

**当前返回**（错误）：
```json
{
  "success": true,
  "data": {
    "token": "test_token_xxx",
    "user_id": "test_xxx",
    "is_new": true
  }
}
```

**应该返回**（正确）：
```json
{
  "success": true,
  "data": {
    "token": "真实的 JWT Token",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试 9",
      "phone": "13800138009"
    }
  }
}
```

---

## 🔧 快速修复（3 种方式）

### 方式 1：从 GitHub 拉取（推荐）

```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 进入网站根目录（根据实际部署位置）
cd /www/wwwroot/zhonghuo.cn

# 如果是 backend 子目录部署
cd backend

# 拉取最新代码
git pull origin main

# 验证
php -l api/users.php
```

### 方式 2：手动修复（最快）

```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 进入网站根目录
cd /www/wwwroot/zhonghuo.cn

# 1. 备份当前 users.php
cp api/users.php api/users.php.test

# 2. 恢复 backup 版本（如果存在）
if [ -f "api/users.php.backup" ]; then
    cp api/users.php.backup api/users.php
    echo "✅ 已恢复 users.php"
else
    echo "❌ 没有找到 backup 文件"
fi

# 3. 修复 config.php
cat > config.php << 'EOF'
<?php
$GLOBALS['db_host'] = 'localhost';
$GLOBALS['db_name'] = 'zhonghuo_db';
$GLOBALS['db_user'] = 'zhonghuo_user';
$GLOBALS['db_pass'] = 'Zhonghuo@2026';
$GLOBALS['app_name'] = '终活 App';
$GLOBALS['app_version'] = '2.0.0';
EOF

echo "✅ 已修复 config.php"

# 4. 设置权限
chown -R www:www .
chmod 644 api/users.php
chmod 644 config.php

# 5. 验证
php -l api/users.php
```

### 方式 3：使用修复脚本

```bash
# 1. SSH 登录
ssh root@8.136.41.211

# 2. 上传修复脚本（在本地 Mac 上执行）
scp /Users/lishimin/Documents/zhonghuo-app/紧急修复.sh root@8.136.41.211:/tmp/

# 3. 在服务器上执行
chmod +x /tmp/紧急修复.sh
bash /tmp/紧急修复.sh
```

---

## 🧪 测试验证

### 测试 1：注册新用户

```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "phone": "13800138009",
    "name": "新用户",
    "password": "test123"
  }'
```

**✅ 正确响应**（包含 user 对象）：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "eyJ0eXAiOiJKV1QiLCJhbGc...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "新用户",
      "phone": "13800138009"
    }
  }
}
```

**❌ 错误响应**（缺少 user 对象）：
```json
{
  "success": true,
  "data": {
    "token": "test_token_xxx",
    "user_id": "test_xxx",
    "is_new": true
  }
}
```

### 测试 2：登录

```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138006",
    "login_type": "password",
    "password": "test123456"
  }'
```

### 测试 3：查看数据库

```bash
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db \
  -e "SELECT id, name, phone, created_at FROM users;"
```

### 测试 4：访问 admin 后台

浏览器访问：http://8.136.41.211:3395/admin/

---

## 📋 检查清单

修复后确认：

- [ ] `api/users.php` 文件大小约 7KB（不是 1.8KB 的测试版）
- [ ] `config.php` 数据库配置正确
  - [ ] 数据库名：`zhonghuo_db`
  - [ ] 用户名：`zhonghuo_user`
  - [ ] 密码：`Zhonghuo@2026`
- [ ] 注册 API 返回包含 `user.id` 字段
- [ ] 登录 API 返回包含 `user.id` 字段
- [ ] 数据库中可以查询到用户
- [ ] admin 后台可以显示用户列表

---

## 🔍 故障排查

### 问题 1：修复后仍然返回测试数据

**检查**：
```bash
# 查看 users.php 文件大小
ls -lh /www/wwwroot/zhonghuo.cn/api/users.php

# 如果是 1-2KB，说明还是测试版
# 如果是 7-8KB，说明已修复
```

**解决**：
```bash
# 确认 backup 文件位置
find /www/wwwroot/zhonghuo.cn -name "users.php.backup" -type f

# 手动复制
cp /path/to/users.php.backup /www/wwwroot/zhonghuo.cn/api/users.php
```

### 问题 2：数据库连接失败

**检查**：
```bash
# 测试数据库连接
mysql -u zhonghuo_user -p'Zhonghuo@2026' -e "SELECT 1"

# 如果失败，检查数据库是否存在
mysql -u root -p -e "SHOW DATABASES LIKE 'zhonghuo_db';"
```

**解决**：
```bash
# 如果数据库不存在，运行安装脚本
mysql -u root -p < /www/wwwroot/zhonghuo.cn/install/schema.sql
```

### 问题 3：权限错误

**检查**：
```bash
# 查看文件权限
ls -la /www/wwwroot/zhonghuo.cn/api/

# 所有者应该是 www:www
```

**解决**：
```bash
chown -R www:www /www/wwwroot/zhonghuo.cn
chmod 644 /www/wwwroot/zhonghuo.cn/api/*.php
```

---

## 📞 需要帮助？

如果以上方法都无法解决，请提供：

1. **服务器目录结构**：
   ```bash
   ls -la /www/wwwroot/zhonghuo.cn/
   ```

2. **API 文件内容**：
   ```bash
   head -20 /www/wwwroot/zhonghuo.cn/api/users.php
   ```

3. **错误日志**：
   ```bash
   tail -50 /www/wwwlogs/zhonghuo.cn.error.log
   ```

---

*最后更新：2026-03-17 18:52*
