# 🔍 终活 App 前后端全面代码审查报告

**审查时间**：2026-03-17 18:55  
**审查范围**：前端 (iOS SwiftUI) + 后端 (PHP)  
**GitHub**：
- 前端：https://github.com/huainvhai-bit/zhonghuo-app
- 后端：https://github.com/huainvhai-bit/zhonghuo-backend-php

---

## 📊 审查结果总览

| 类别 | 问题数 | 严重 | 已修复 | 待修复 |
|------|--------|------|--------|--------|
| API 对接 | 3 | 🔴 2 | 0 | 3 |
| 数据库配置 | 2 | 🔴 2 | 0 | 2 |
| 表名不一致 | 2 | 🟡 2 | 0 | 2 |
| 代码冲突 | 1 | 🟡 1 | 0 | 1 |
| **总计** | **8** | **7** | **0** | **8** |

---

## 🔴 严重问题（必须修复）

### 问题 1：api/users.php 是测试版本 ⚠️ **最严重**

**文件**：`backend/api/users.php` (1.8KB, 71 行)

**问题**：
- ❌ 这是**测试文件**，不连接数据库
- ❌ 所有响应都是硬编码的模拟数据
- ❌ 注册和登录都不会保存真实数据

**当前代码**：
```php
// 简化的 users.php - 用于测试
header('Content-Type: application/json');

// 模拟 core.php 的函数
function getParam($name, $default = null) { ... }
function postParam($name, $default = null) { ... }

// 模拟登录成功
success([
    'token' => 'test_token_' . time(),
    'user_id' => 'test_' . md5($phone),
    'user' => [
        'name' => '测试用户',
        'phone' => $phone
    ]
]);
```

**正确版本**：`api/users.php.backup` (6.8KB, 260 行)
- ✅ 完整数据库操作
- ✅ JWT Token 生成
- ✅ 密码哈希
- ✅ 真实的用户注册和登录

**修复方法**：
```bash
cd /www/wwwroot/zhonghuo.cn
cp api/users.php.backup api/users.php
```

**影响**：
- 🔴 前端注册提示"网络错误"（因为返回数据格式不对）
- 🔴 前端登录无法获取真实用户信息
- 🔴 admin 后台无法显示注册用户

---

### 问题 2：数据库配置不一致

**文件**：`backend/config.php`

**当前配置**：
```php
$GLOBALS['db_host'] = 'localhost';
$GLOBALS['db_name'] = 'zhonghuo_db';       // ✅ 正确
$GLOBALS['db_user'] = 'zhonghuo_user';     // ✅ 正确
$GLOBALS['db_pass'] = 'Zhonghuo@2026';     // ✅ 正确
```

**问题**：
- ✅ GitHub 上的 config.php 已修复
- ⚠️ **服务器上可能还是旧配置**

**验证方法**：
```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 查看服务器上的配置
cat /www/wwwroot/zhonghuo.cn/config.php
```

---

### 问题 3：API 返回缺少 user.id 字段

**文件**：`backend/api/users.php` (测试版本)

**当前返回**：
```json
{
  "success": true,
  "data": {
    "token": "test_token_xxx",
    "user_id": "test_xxx",
    "user": {
      "name": "测试用户",    // ❌ 缺少 id 字段
      "phone": "13800138006"
    }
  }
}
```

**前端期望** (`AuthView.swift`):
```swift
let userData = data["user"] as? [String: Any] ?? [:]
let userId = userData["id"] as? String ?? data["user_id"] as? String ?? ""
```

**正确返回** (users.php.backup):
```json
{
  "success": true,
  "data": {
    "token": "真实 token",
    "user_id": "user_xxx",
    "user": {
      "id": "user_xxx",      // ✅ 包含 id
      "name": "测试用户",
      "phone": "13800138006"
    }
  }
}
```

**修复**：替换为 users.php.backup 后自动解决

---

## 🟡 中等问题（需要修复）

### 问题 4：admin 后台表名错误

**文件**：`backend/admin/users.php`

**当前查询**：
```sql
SELECT 
    u.*,
    (SELECT COUNT(*) FROM check_in_records ci WHERE ci.user_id = u.id) as checkin_count,
    (SELECT COUNT(*) FROM will_templates w WHERE w.user_id = u.id) as will_count
FROM users u
```

**数据库实际表名** (`install/schema.sql`):
```sql
CREATE TABLE IF NOT EXISTS `check_ins` (...);
CREATE TABLE IF NOT EXISTS `wills` (...);
CREATE TABLE IF NOT EXISTS `will_templates` (...);
```

**问题**：
- ❌ 查询使用 `check_in_records`，实际表名是 `check_ins`
- ⚠️ 查询使用 `will_templates`，但遗嘱表是 `wills`

**修复**：
```bash
cd /www/wwwroot/zhonghuo.cn
sed -i 's/check_in_records/check_ins/g' admin/users.php
sed -i 's/will_templates/wills/g' admin/users.php
```

---

### 问题 5：前端 API 地址硬编码

**文件**：`frontend/DataManager.swift`

**代码**：
```swift
// 第 90 行
static func initializeDefaultConfig() {
    DataManager.baseURL = "http://8.136.41.211:3395"
    DataManager.apiURL = "http://8.136.41.211:3395/api"
}
```

**问题**：
- ⚠️ API 地址硬编码，不灵活
- ✅ 但有动态获取配置的机制（fetchServerConfig）

**建议**：
- 保持现状（开发阶段硬编码可以接受）
- 生产环境使用动态配置

---

### 问题 6：数据库表结构不一致

**审查**：`install/schema.sql` vs 后端代码

**表名对照**：
| 代码期望 | 实际表名 | 状态 |
|---------|---------|------|
| `users` | `users` | ✅ |
| `capsules` | `capsules` | ✅ |
| `wills` | `wills` | ✅ |
| `check_ins` | `check_ins` | ✅ |
| `sms_codes` | `sms_codes` | ✅ |
| `will_templates` | `will_templates` | ✅ |
| `assets` | `assets` | ✅ |
| `witnesses` | `witnesses` | ✅ |

**结论**：表结构正确，但 admin 代码查询用错了表名

---

## 🟢 轻微问题（可选优化）

### 问题 7：测试文件过多

**文件**：`backend/api/` 目录

**当前文件**：
```
api/users.php           # ❌ 测试版（需要删除）
api/users.php.backup    # ✅ 正式版
api/users.php.broken    # ⚠️ 损坏版本
api/users_simple.php    # ⚠️ 简化版本
api/test.php            # ⚠️ 测试文件
api/test_direct.php     # ⚠️ 测试文件
api/test_post.php       # ⚠️ 测试文件
```

**建议**：
```bash
# 清理测试文件
rm api/users.php.test*
rm api/users.php.broken
rm api/users_simple.php
rm api/test*.php
```

---

### 问题 8：前端错误处理不够完善

**文件**：`frontend/AuthView.swift`

**代码** (第 600-750 行):
```swift
// 错误处理逻辑分散
// 缺少统一的错误码映射
```

**建议**：
- 创建统一的错误码映射表
- 添加错误重试机制
- 优化错误提示信息

---

## 🔧 完整修复方案

### 立即修复（生产环境必须）

```bash
# === 1. SSH 登录服务器 ===
ssh root@8.136.41.211

# === 2. 进入后端目录 ===
cd /www/wwwroot/zhonghuo.cn

# === 3. 修复 users.php ===
cp api/users.php api/users.php.test.$(date +%Y%m%d_%H%M%S)
cp api/users.php.backup api/users.php

# === 4. 验证 config.php ===
cat config.php
# 确认数据库配置正确

# === 5. 修复 admin 表名 ===
sed -i 's/check_in_records/check_ins/g' admin/users.php
sed -i 's/will_templates/wills/g' admin/users.php

# === 6. 设置权限 ===
chown -R www:www .
chmod 644 api/users.php
chmod 644 config.php
chmod 644 admin/users.php

# === 7. 测试 API ===
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138009","name":"测试 9","password":"test123"}'

# 应该返回包含 "user" 对象的 JSON
```

### 使用自动修复脚本

```bash
# 在本地 Mac 上
scp /Users/lishimin/Documents/zhonghuo-app/全面修复后端.sh root@8.136.41.211:/tmp/

# 在服务器上
chmod +x /tmp/全面修复后端.sh
bash /tmp/全面修复后端.sh
```

---

## ✅ 修复验证清单

修复后请确认：

### API 测试
- [ ] 注册 API 返回包含 `user.id` 字段
- [ ] 登录 API 返回包含 `user.id` 字段
- [ ] Token 是真实的 JWT（不是 test_token_xxx）

### 数据库测试
- [ ] 数据库中可以看到新用户
- [ ] 用户密码是哈希过的（不是明文）
- [ ] 用户 created_at 时间正确

### Admin 后台测试
- [ ] 可以访问 http://8.136.41.211:3395/admin/
- [ ] 可以看到用户列表
- [ ] 可以看到签到统计
- [ ] 最后登录时间正确

### 前端测试
- [ ] App 注册成功
- [ ] App 登录成功
- [ ] 首页显示用户信息
- [ ] 签到功能正常

---

## 📋 文件清单

### 后端关键文件
```
backend/
├── api/
│   ├── users.php           # 🔴 需要替换
│   ├── users.php.backup    # ✅ 正确版本
│   ├── capsules.php        # ✅ 时光胶囊 API
│   ├── checkin.php         # ✅ 签到 API
│   ├── will.php            # ✅ 遗嘱 API
│   ├── sms.php             # ✅ 短信 API
│   └── config.php          # ✅ 配置 API
├── admin/
│   └── users.php           # 🟡 需要修复表名
├── config.php              # ✅ 数据库配置
└── install/
    └── schema.sql          # ✅ 数据库结构
```

### 前端关键文件
```
frontend/
├── AuthView.swift          # ✅ 登录注册界面
├── DataManager.swift       # ✅ 数据管理
├── Models.swift            # ✅ 数据模型
├── HomeStatusView.swift    # ✅ 首页
└── SettingsView.swift      # ✅ 设置
```

---

## 🎯 优先级排序

1. **P0 - 立即修复**：
   - 替换 `api/users.php` 为 backup 版本
   - 验证 `config.php` 数据库配置

2. **P1 - 今天修复**：
   - 修复 `admin/users.php` 表名
   - 测试注册和登录流程

3. **P2 - 本周优化**：
   - 清理测试文件
   - 完善错误处理

---

*报告生成时间：2026-03-17 18:55*  
*下次审查：修复后重新审查*
