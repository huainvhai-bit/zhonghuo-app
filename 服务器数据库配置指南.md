# 🚨 服务器数据库配置指南

**问题**：注册时提示"数据库连接失败，请稍后重试"  
**原因**：服务器数据库用户不存在或密码错误  
**时间**：2026-03-17 21:12

---

## 🔍 错误诊断

### 当前状态

**前端错误提示**：
```
数据库连接失败，请稍后重试
```

**后端实际错误**：
```
SQLSTATE[HY000] [1045] Access denied for user 'zhonghuo_user'@localhost
```

**API 测试结果**：
```bash
# 验证码 API - ✅ 成功
curl -X POST "http://8.136.41.211:3395/api/sms.php" \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138099","action":"register"}'
# 返回：{"success":true,"code":"123456"}

# 注册 API - ❌ 失败
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试","password":"test123","verify_code":"123456"}'
# 返回：{"error":"数据库连接失败，请检查配置","code":"DB_ERROR"}
```

---

## ✅ 解决方案

### 步骤 1：SSH 登录服务器

```bash
ssh root@8.136.41.211
```

---

### 步骤 2：创建数据库和用户

**复制以下命令到服务器执行**：

```bash
mysql -u root -p << 'EOF'
-- 创建数据库
CREATE DATABASE IF NOT EXISTS zhonghuo_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户并设置密码
CREATE USER IF NOT EXISTS 'zhonghuo_user'@'localhost' IDENTIFIED BY 'Zhonghuo@2026';

-- 授权
GRANT ALL PRIVILEGES ON zhonghuo_db.* TO 'zhonghuo_user'@'localhost';

-- 刷新权限
FLUSH PRIVILEGES;

-- 确认创建成功
SELECT User, Host FROM mysql.user WHERE User = 'zhonghuo_user';

-- 退出
EXIT;
EOF
```

**预期输出**：
```
+---------------+-----------+
| User          | Host      |
+---------------+-----------+
| zhonghuo_user | localhost |
+---------------+-----------+
```

---

### 步骤 3：创建数据表

**复制以下命令到服务器执行**：

```bash
mysql -u root -p << 'EOF'
USE zhonghuo_db;

-- 创建 users 表
CREATE TABLE IF NOT EXISTS users (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    last_login_at DATETIME NULL,
    INDEX idx_phone (phone),
    INDEX idx_created_at (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 创建 sms_codes 表
CREATE TABLE IF NOT EXISTS sms_codes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    phone VARCHAR(20) NOT NULL,
    code VARCHAR(10) NOT NULL,
    action VARCHAR(20) NOT NULL,
    expires_at DATETIME NOT NULL,
    is_used TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_phone_action (phone, action),
    INDEX idx_expires (expires_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- 确认表创建成功
SHOW TABLES;

-- 退出
EXIT;
EOF
```

**预期输出**：
```
+----------------------+
| Tables_in_zhonghuo_db |
+----------------------+
| sms_codes            |
| users                |
+----------------------+
```

---

### 步骤 4：更新后端代码

```bash
cd /www/wwwroot/zhonghuo.cn
git pull origin main
ls -lh api/users.php api/sms.php
chown -R www:www .
```

**预期输出**：
```
-rwxr-xr-x 1 www www 5.2K Mar 17 21:04 api/users.php
-rwxr-xr-x 1 www www 2.9K Mar 17 20:03 api/sms.php
```

---

### 步骤 5：测试 API

#### 测试 1：验证码 API

```bash
curl -X POST "http://localhost:3395/api/sms.php" \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138099","action":"register"}'
```

**预期响应**：
```json
{
  "success": true,
  "message": "验证码发送成功",
  "code": "123456"
}
```

#### 测试 2：注册 API

```bash
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "phone": "13800138099",
    "name": "测试用户",
    "password": "test123",
    "verify_code": "123456"
  }'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "abc123...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099"
    }
  }
}
```

#### 测试 3：登录 API

```bash
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138099",
    "login_type": "password",
    "password": "test123"
  }'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "abc123...",
    "user_id": "user_xxx",
    "is_new": false,
    "user": {
      "id": "user_xxx",
      "name": "测试用户",
      "phone": "13800138099"
    }
  }
}
```

---

## 🔧 故障排查

### 问题 1：数据库连接仍然失败

**检查数据库用户权限**：
```bash
mysql -u root -p -e "SHOW GRANTS FOR 'zhonghuo_user'@'localhost';"
```

**预期输出**：
```
+--------------------------------------------------------------------------------+
| Grants for zhonghuo_user@localhost                                             |
+--------------------------------------------------------------------------------+
| GRANT USAGE ON *.* TO 'zhonghuo_user'@'localhost'                              |
| GRANT ALL PRIVILEGES ON `zhonghuo_db`.* TO 'zhonghuo_user'@'localhost'         |
+--------------------------------------------------------------------------------+
```

### 问题 2：API 返回 404 错误

**检查文件权限**：
```bash
ls -lh /www/wwwroot/zhonghuo.cn/api/users.php
```

**修复权限**：
```bash
chown -R www:www /www/wwwroot/zhonghuo.cn
chmod -R 755 /www/wwwroot/zhonghuo.cn/api
```

### 问题 3：数据库表不存在

**检查表是否存在**：
```bash
mysql -u root -p -e "USE zhonghuo_db; SHOW TABLES;"
```

**如果表不存在，重新执行步骤 3**

---

## 📊 配置信息

### 数据库配置（config.php）

```php
$GLOBALS['db_host'] = 'localhost';
$GLOBALS['db_name'] = 'zhonghuo_db';
$GLOBALS['db_user'] = 'zhonghuo_user';
$GLOBALS['db_pass'] = 'Zhonghuo@2026';
```

### 数据库连接信息

- **主机**：localhost
- **数据库名**：zhonghuo_db
- **用户名**：zhonghuo_user
- **密码**：Zhonghuo@2026
- **字符集**：utf8mb4
- **排序规则**：utf8mb4_unicode_ci

### 数据表结构

#### users 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(64) | 用户 ID（主键） |
| name | VARCHAR(100) | 用户名 |
| phone | VARCHAR(20) | 手机号（唯一） |
| password_hash | VARCHAR(255) | 密码哈希 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |
| last_login_at | DATETIME | 最后登录时间 |

#### sms_codes 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 验证码 ID（主键） |
| phone | VARCHAR(20) | 手机号 |
| code | VARCHAR(10) | 验证码 |
| action | VARCHAR(20) | 操作类型（register/login） |
| expires_at | DATETIME | 过期时间 |
| is_used | TINYINT | 是否已使用 |
| created_at | DATETIME | 创建时间 |

---

## ✅ 验证清单

- [ ] SSH 登录服务器
- [ ] 创建数据库 zhonghuo_db
- [ ] 创建用户 zhonghuo_user
- [ ] 授权用户权限
- [ ] 创建 users 表
- [ ] 创建 sms_codes 表
- [ ] 拉取最新后端代码
- [ ] 设置文件权限
- [ ] 测试验证码 API
- [ ] 测试注册 API
- [ ] 测试登录 API
- [ ] 前端测试注册功能
- [ ] 前端测试登录功能

---

## 🎯 完成后预期效果

1. ✅ 获取验证码成功（显示验证码：123456）
2. ✅ 注册成功（显示：注册成功！）
3. ✅ 登录成功（显示：登录成功！）
4. ✅ 用户数据保存到数据库
5. ✅ 前端自动跳转到首页

---

*指南生成时间：2026-03-17 21:12*
