# ✅ 数据同步修复报告 - 2026-03-26

## 📊 同步状态

### ✅ 已修复的功能

| 功能 | 状态 | 说明 |
|------|------|------|
| JWT 验证 | ✅ 成功 | 不再报签名错误 |
| 遗嘱同步 | ✅ 成功 | 5 个遗嘱已同步 |
| 位置上传 | ✅ 成功 | 位置正常上传 |
| 签到同步 | ✅ 成功 | 签到正常 |
| 邀请码生成 | ✅ 成功 | 邀请码：25DB66 |

### ⚠️ 部分成功

| 功能 | 状态 | 问题 |
|------|------|------|
| 见证人同步 | ⚠️ 1/3 | 2 个见证人缺少手机号，被跳过 |

### ❌ 待修复

| 功能 | 状态 | 问题 |
|------|------|------|
| 胶囊同步 | ❌ 失败 | httpError(500) |

---

## 🔍 问题分析

### 1. 见证人同步（部分成功）

**前端发送**：
```json
[
  {"id": "4FA7CC6F...", "name": "王宇", "phone": "15566888555888", "relationship": "朋友"},
  {"id": "7812C023...", "name": "具体 ", "phone": "", "relationship": "量一下"},
  {"id": "96B945B6...", "name": "你也一样", "phone": "", "relationship": "你小子"}
]
```

**后端要求**：
```php
if (empty($name) || empty($phone)) continue;  // 跳过
```

**结果**：只有第 1 个见证人（有手机号）同步成功，后 2 个被跳过。

**解决方案**：
- 前端验证：添加见证人时必须填写手机号
- 或者后端放宽要求：允许 phone 为空

---

### 2. 胶囊同步失败（500 错误）

**前端发送**：
```json
{
  "id": "573185AF-9C80-4D51-BE87-716F4A814575",
  "title": "同组",
  "type": "text",
  "content": "弥陀",
  "openAt": "2026-03-26T15:01:00.000Z"
}
```

**错误日志**：
```
❌ 胶囊同步失败：httpError(500)
```

**可能原因**：
1. 数据库表缺少 `type` 字段
2. GraphQL 解析失败
3. 云存储库文件缺失

---

## 🔧 修复方案

### 方案 1：见证人 - 前端验证（推荐）

修改 `WitnessesView.swift`，添加手机号验证：

```swift
// 添加见证人时
if phone.isEmpty {
    showError("请填写见证人手机号")
    return
}
```

### 方案 2：见证人 - 后端放宽

修改 `api/graphql.php`：

```php
// 修改前
if (empty($name) || empty($phone)) continue;

// 修改后（允许 phone 为空）
if (empty($name)) continue;
```

### 方案 3：胶囊 - 检查数据库字段

```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 检查 capsules 表结构
mysql -u zhonghuo -pzhonghuo zhonghuo -e "DESCRIBE capsules;"

# 检查是否有 type 字段
# 应该看到：type VARCHAR(50) NOT NULL
```

### 方案 4：胶囊 - 检查云存储库

```bash
# 检查 lib 目录
ls -la /www/wwwroot/zhonghuo.cn/lib/

# 应该看到：
# CloudStorage.php
# CloudStorageRetry.php
```

---

## 📝 待办事项

### 立即可做（前端）

1. **见证人添加验证**：要求必须填写手机号
2. **胶囊同步调试**：添加更详细的错误日志

### 需要服务器操作

1. **检查数据库表结构**：确认 `capsules.type` 字段存在
2. **检查云存储库文件**：确认 `lib/CloudStorage.php` 存在
3. **查看 PHP 错误日志**：找到 500 错误的具体原因

---

## 🚀 服务器排查命令

```bash
# 1. SSH 登录
ssh root@8.136.41.211

# 2. 检查胶囊表结构
cd /www/wwwroot/zhonghuo.cn
mysql -u zhonghuo -pzhonghuo zhonghuo -e "DESCRIBE capsules;"

# 3. 检查云存储库
ls -la lib/CloudStorage*.php

# 4. 查看 PHP 错误日志
tail -100 /www/wwwlogs/zhonghuo.cn_error.log | grep -E "capsule|500"

# 5. 检查 witnesses 表数据
mysql -u zhonghuo -pzhonghuo zhonghuo -e "SELECT * FROM witnesses WHERE user_id='user_69c52a225db66';"

# 6. 检查 capsules 表数据
mysql -u zhonghuo -pzhonghuo zhonghuo -e "SELECT * FROM capsules WHERE user_id='user_69c52a225db66';"
```

---

## 📊 当前数据状态

**用户 ID**：`user_69c52a225db66`

| 数据类型 | 前端 | 后端 | 状态 |
|----------|------|------|------|
| 见证人 | 3 个 | 1 个 | ⚠️ 2 个缺少手机号 |
| 胶囊 | 1 个 | 0 个 | ❌ 同步失败 |
| 遗嘱 | 5 个 | 5 个 | ✅ 已同步 |
| 紧急联系人 | 0 个 | 0 个 | ✅ 一致 |

---

**下一步**：执行服务器排查命令，找到胶囊同步失败的具体原因。
