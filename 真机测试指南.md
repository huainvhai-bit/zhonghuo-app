# 📱 终活 App - 真机测试指南

## ✅ App 已安装

**设备**: iPhone 6s (吊打华为 mate80pro 的 iPhone6s)
**Bundle ID**: com.zhonghuo.app
**安装时间**: 2026-03-16 16:15

---

## ⚠️ 重要：配置后端 API 地址

### 当前配置
App 中的后端 API 地址配置在 `DataManager.swift` 中：

```swift
static let baseURL = "http://192.168.1.100"  // ⚠️ 需要修改为你的服务器 IP
static let apiURL = "\(baseURL)/api"
```

### 修改步骤

1. **打开 Xcode 项目**
   ```bash
   cd /Users/lishimin/Documents/zhonghuo-app
   open 终活.xcodeproj
   ```

2. **编辑 DataManager.swift**
   - 找到第 14-15 行
   - 修改 `baseURL` 为你的服务器 IP 地址

3. **示例配置**
   ```swift
   // 本地测试
   static let baseURL = "http://192.168.1.100"
   
   // 服务器部署
   static let baseURL = "http://your-server-ip"
   
   // 或域名
   static let baseURL = "https://zhonghuo.yourdomain.com"
   ```

4. **重新编译安装**
   ```bash
   xcodebuild -scheme 终活 -destination 'id=00008140-001449A13640801C' build
   xcrun devicectl device install app --device 00008140-001449A13640801C \
     /Users/lishimin/Library/Developer/Xcode/DerivedData/终活-cqrdtepyatgbutagxrvrefgapusm/Build/Products/Debug-iphoneos/终活.app
   ```

---

## 🧪 测试流程

### 1. 测试用户注册/登录

1. 打开 App
2. 进入"我的"页面
3. 点击"登录/注册"
4. 输入手机号和姓名
5. 点击"注册/登录"

**预期结果**:
- ✅ 如果后端 API 配置正确且服务器在线 → 注册成功
- ✅ 如果 API 未配置或服务器离线 → 使用本地存储（当前模式）

---

### 2. 测试时光胶囊

1. 进入"时光胶囊"页面
2. 点击"+"添加胶囊
3. 输入标题和内容
4. 设置开启时间
5. 点击"保存"

**预期结果**:
- ✅ 胶囊成功添加到列表
- ✅ 数据保存在本地或同步到后端

---

### 3. 测试签到功能

1. 进入首页
2. 点击"签到"按钮
3. 查看签到记录

**预期结果**:
- ✅ 签到成功
- ✅ 显示倒计时（根据设置的间隔）

---

### 4. 测试后端连接状态

#### 方法 1: 查看控制台日志

1. 打开 Xcode
2. 连接设备
3. 打开 **Window → Devices and Simulators**
4. 选择你的设备
5. 点击 **View Device Logs**
6. 查找 "终活" 的日志

**关键日志**:
```
🔵 ====== 用户状态 ======
📁 文档路径：/var/.../Documents
👤 登录状态：true/false
⏰ 签到间隔：60
```

#### 方法 2: 添加网络请求测试

在 `ContentView.swift` 中添加测试代码：

```swift
.onAppear {
    // 测试后端连接
    Task {
        do {
            let url = URL(string: "\(DataManager.apiURL)/users.php?action=test")!
            let (data, response) = try await URLSession.shared.data(from: url)
            print("🌐 后端连接状态：\(response)")
            print("📄 响应数据：\(String(data: data, encoding: .utf8) ?? "")")
        } catch {
            print("❌ 后端连接失败：\(error)")
        }
    }
}
```

---

## 📊 当前功能状态

### ✅ 已实现（本地模式）
- 用户注册/登录（本地存储）
- 时光胶囊（增删改查）
- 遗嘱与资产（模板 + 管理）
- 见证人管理
- 签到功能

### ⏳ 待实现（后端连接）
- 用户数据云同步
- 胶囊数据云同步
- 遗嘱数据云同步
- 见证人云同步
- 签到记录云同步

---

## 🔧 后端 API 测试

### 测试后端是否在线

```bash
# 1. 测试安装页面
curl http://your-server-ip/install/

# 2. 测试 API 接口
curl http://your-server-ip/api/users.php?action=test

# 3. 测试管理后台
curl http://your-server-ip/admin/
```

### 预期响应

**安装页面**: 显示 HTML 安装向导
**API 测试**: 返回 JSON
```json
{"success": true, "message": "API is working"}
```
**管理后台**: 显示登录页面或 dashboard

---

## 📝 测试检查清单

### 基础功能
- [ ] App 正常启动
- [ ] 导航栏显示正常
- [ ] 5 个 Tab 都可以切换
- [ ] 新手引导正常显示（首次使用）

### 用户功能
- [ ] 可以注册/登录
- [ ] 用户信息保存成功
- [ ] 退出登录正常

### 时光胶囊
- [ ] 可以添加胶囊
- [ ] 可以编辑胶囊
- [ ] 可以删除胶囊
- [ ] 列表显示正常

### 遗嘱与资产
- [ ] 可以添加遗嘱模块
- [ ] 可以添加资产
- [ ] PDF 导出正常

### 签到功能
- [ ] 签到按钮正常
- [ ] 倒计时显示正确
- [ ] 签到记录保存

### 后端连接（如果已配置）
- [ ] 用户数据同步成功
- [ ] 胶囊数据同步成功
- [ ] 无网络错误提示

---

## 🐛 常见问题

### 问题 1: App 无法连接后端
**症状**: 网络请求超时或失败

**解决**:
1. 检查 `DataManager.baseURL` 配置
2. 确保服务器在线
3. 确保手机和服务器在同一网络（或服务器有公网 IP）
4. 检查防火墙设置

### 问题 2: 证书签名问题
**症状**: 安装失败

**解决**:
```bash
# 清理旧证书
xcrun simctl erase all

# 重新编译
xcodebuild clean build
```

### 问题 3: 数据不同步
**症状**: 本地数据无法同步到后端

**解决**:
1. 检查 API 配置
2. 查看后端日志
3. 检查网络请求是否被拦截

---

## 📞 联系支持

如有问题，请查看：
- App 日志（Xcode → Devices and Simulators）
- 后端日志（服务器 data/error.log）
- Nginx 日志（/var/log/nginx/error.log）

---

**最后更新**: 2026-03-16 16:15
**版本**: v2.0 ✅
