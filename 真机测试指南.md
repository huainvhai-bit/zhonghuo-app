# 真机测试指南

## 1. Xcode 配置

### 签名设置
1. 打开 `终活.xcodeproj`
2. 选择左侧项目文件（蓝色图标）
3. 选择 `终活` Target
4. 点击 **Signing & Capabilities** 标签页
5. 配置以下项：
   - **Automatically manage signing**: ✅ 勾选
   - **Team**: 选择你的 Apple Developer Team
   - **Bundle Identifier**: `com.zhonghuo.app`（必须唯一）

### 如果 Bundle ID 冲突
如果提示 "Bundle ID 已被使用"，修改为：
- `com.zhonghuo.app.你的姓名缩写`
- 例如：`com.zhonghuo.app.lsm`

修改方法：
1. 在 Xcode 中修改 Bundle Identifier
2. 或编辑 `Info.plist`：
```xml
<key>CFBundleIdentifier</key>
<string>com.zhonghuo.app.lsm</string>
```

---

## 2. 连接 iPhone

1. 用数据线连接 iPhone 到 Mac
2. 在 iPhone 上信任此电脑
3. 在 Xcode 顶部设备选择器中选择你的 iPhone

---

## 3. 运行应用

1. 点击运行按钮（▶️）或按 `Cmd + R`
2. 首次运行需要在 iPhone 上信任开发者：
   - 设置 → 通用 → VPN 与设备管理
   - 找到你的开发者证书
   - 点击"信任"

---

## 4. 测试功能

### 注册登录
- [ ] 打开 App 显示注册/登录界面
- [ ] 输入姓名和手机号（11 位）
- [ ] 点击注册，成功登录
- [ ] 退出后重新登录，使用手机号即可

### 定位功能
- [ ] 注册后请求定位权限
- [ ] 选择"使用 App 期间允许"或"始终允许"
- [ ] 在"我的"页面查看定位状态
- [ ] 修改签到间隔时提示后台定位

### 签到功能
- [ ] 打开 App 自动检查签到
- [ ] 首次打开自动签到成功
- [ ] 查看签到倒计时

### 后台定位（需要真机）
1. 修改签到间隔为 >1 分钟
2. 系统提示开启后台定位
3. 去设置中开启"始终允许"
4. 关闭 App 后仍能获取位置

---

## 5. 常见问题

### Q: "No signing certificate found"
**解决：**
1. Xcode → Preferences → Accounts
2. 选择你的 Apple ID
3. 点击 "Manage Certificates"
4. 添加 iOS Distribution 证书

### Q: "Device not found"
**解决：**
1. 重新插拔数据线
2. 在 iPhone 上信任此电脑
3. 重启 Xcode

### Q: 定位不工作
**解决：**
1. 检查 iPhone 设置 → 隐私 → 定位服务 → 终活
2. 确保已开启定位
3. 真机才能测试后台定位

---

## 6. 推送测试

测试完成后，如果需要推送更新：

```bash
cd /Users/lishimin/Documents/zhonghuo-app
git add -A
git commit -m "更新描述"
git push origin main
```

---

**最后更新：** 2026-03-15 20:30
