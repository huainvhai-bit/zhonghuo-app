# 终活 App 后端部署说明

## 📋 问题诊断

**当前问题**：App 注册登录成功，但后端数据库没有保存用户数据

**原因**：后端 API 使用的是**测试模式硬编码响应**，没有真正操作数据库

---

## 🔧 解决方案

### 方案 1：在服务器上手动部署（推荐）

#### 步骤 1：SSH 登录服务器
```bash
ssh root@8.136.41.211
```

#### 步骤 2：上传文件
```bash
# 在本地执行
scp /tmp/init_zhonghuo_db.sql root@8.136.41.211:/tmp/
scp /tmp/users_production.php root@8.136.41.211:/tmp/
scp /tmp/deploy_backend.sh root@8.136.41.211:/tmp/
```

#### 步骤 3：执行部署
```bash
# 在服务器上执行
chmod +x /tmp/deploy_backend.sh
/tmp/deploy_backend.sh
```

#### 步骤 4：验证部署
```bash
# 测试注册
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "register",
    "phone": "13800138009",
    "name": "真实用户",
    "password": "test123",
    "verify_code": "123456"
  }'

# 测试登录
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{
    "action": "login",
    "phone": "13800138006",
    "login_type": "password",
    "password": "test123456"
  }'

# 查看数据库
mysql -u zhonghuo_user -p'Zhonghuo@2026' zhonghuo_db -e "SELECT * FROM users;"
```

---

### 方案 2：修改现有代码（快速修复）

如果不想完全替换后端，可以只修改关键的数据库连接部分。

#### 1. 检查当前数据库配置
```bash
# 在服务器上查看
cat /www/wwwroot/zhonghuo.cn/api/users.php | head -50
```

#### 2. 确认数据库是否存在
```bash
mysql -u root -p -e "SHOW DATABASES LIKE 'zhonghuo_db';"
```

#### 3. 如果数据库不存在，创建它
```bash
mysql -u root -p < /tmp/init_zhonghuo_db.sql
```

#### 4. 修改 users.php
编辑 `/www/wwwroot/zhonghuo.cn/api/users.php`，确保：
- ✅ 数据库连接配置正确
- ✅ 注册功能真正执行 INSERT
- ✅ 登录功能真正执行 SELECT

---

## 📊 数据库表结构

### users 表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(64) | 用户 ID |
| phone | VARCHAR(20) | 手机号（唯一） |
| name | VARCHAR(100) | 姓名 |
| password_hash | VARCHAR(255) | 密码哈希 |
| created_at | DATETIME | 创建时间 |
| last_login_at | DATETIME | 最后登录时间 |
| last_check_in_date | DATETIME | 最后签到时间 |
| check_in_interval_hours | INT | 签到间隔（小时） |

### 其他表
- `capsules` - 时光胶囊
- `will_templates` - 遗嘱模板
- `assets` - 资产
- `witnesses` - 见证人
- `emergency_contacts` - 紧急联系人
- `check_in_records` - 签到记录

---

## 🧪 测试账号

部署完成后，以下账号应该可以正常使用：

| 手机号 | 密码 | 说明 |
|--------|------|------|
| 13800138006 | test123456 | 主测试账号 |
| 13800138008 | test123 | 备用测试账号 |

---

## ⚠️ 注意事项

1. **安全性**：
   - 部署后应关闭 `DEBUG_MODE`
   - 修改默认密码
   - 启用验证码验证

2. **备份**：
   - 部署前自动备份旧代码
   - 定期备份数据库

3. **日志**：
   - 检查 `/www/wwwlogs/zhonghuo.cn.log`
   - 监控错误日志

---

## 📞 问题排查

如果部署后仍有问题：

1. **检查 PHP 错误日志**
   ```bash
   tail -50 /www/wwwlogs/zhonghuo.cn.error.log
   ```

2. **检查数据库连接**
   ```bash
   mysql -u zhonghuo_user -p'Zhonghuo@2026' -e "SELECT 1"
   ```

3. **测试 API 响应**
   ```bash
   curl -v "http://8.136.41.211:3395/api/users.php"
   ```

---

*最后更新：2026-03-17 17:45*
