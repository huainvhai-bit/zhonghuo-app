# 前后端数据同步流程

## 📊 数据流向图

```
用户登录 → 下载数据 ← 服务器
    ↓
完成签到 → 上传数据 → 服务器
```

---

## 🔄 完整流程

### 1️⃣ 用户登录（AuthView.swift）

```swift
// 登录成功后
Task.detached {
    await DataManager.shared.downloadAllData()  // 从服务器下载所有数据
}
```

**下载的数据包括：**
- ✅ 时光胶囊 (`downloadCapsules()`)
- ✅ 遗嘱模块 (`downloadWills()`)
- ✅ 紧急联系人 (`downloadEmergencyContacts()`)
- ✅ 见证人 (`downloadWitnesses()`)

**后端接口：**
- `GET /api/capsules.php?action=list&token=xxx`
- `GET /api/will.php?action=list&token=xxx`
- `GET /api/emergency_contacts.php?action=list&token=xxx`
- `GET /api/will.php?action=list_witnesses&token=xxx`

---

### 2️⃣ 用户签到（UserManager.swift）

**触发时机：**
- 手动点击"立即签到"
- 自动签到（距离上次签到≥48 小时）

**签到后自动同步：**
```swift
// 1. 同步签到记录
await syncCheckInToServer(isAuto: isAuto)

// 2. 上传位置
self.uploadLocation()

// 3. 同步胶囊数据
await DataManager.shared.batchSyncCapsules()

// 4. 同步遗嘱数据
await DataManager.shared.batchSyncWills()

// 5. 同步紧急联系人
await DataManager.shared.batchSyncEmergencyContacts()

// 6. 同步见证人
await DataManager.shared.batchSyncWitnesses()
```

**后端接口：**
- `POST /api/checkin.php?action=record` - 签到记录
- `POST /api/location.php?action=upload` - 位置上传
- `POST /api/capsules.php?action=batch_sync` - 胶囊批量同步
- `POST /api/will.php?action=batch_sync` - 遗嘱批量同步
- `POST /api/emergency_contacts.php?action=batch_sync` - 紧急联系人同步
- `POST /api/will.php?action=sync_witnesses` - 见证人同步

---

## 📦 数据结构

### 胶囊同步（前端 → 后端）

```json
{
  "token": "xxx",
  "capsules": [
    {
      "id": "uuid",
      "title": "标题",
      "content": "内容",
      "type": "文字/语音/视频",
      "media_url": "",
      "send_date": "2026-03-20T10:00:00Z",
      "is_sent": false
    }
  ]
}
```

### 遗嘱同步（前端 → 后端）

```json
{
  "token": "xxx",
  "modules": [
    {
      "id": "uuid",
      "type": "财产分配",
      "title": "标题",
      "subtitle": "副标题",
      "content": "内容",
      "is_completed": true
    }
  ]
}
```

### 数据下载（后端 → 前端）

```json
{
  "success": true,
  "message": "获取成功",
  "data": [
    {
      "id": "uuid",
      "title": "标题",
      ...
    }
  ]
}
```

---

## ✅ 验证步骤

### 1. 验证登录下载

```bash
# 1. 登录 App
# 2. 查看 Xcode 控制台日志：
📥 ====== 开始从服务器下载数据 ======
📦 下载胶囊数据...
✅ 胶囊下载成功：X 个
📝 下载遗嘱数据...
✅ 遗嘱下载成功：X 个
🎉 所有数据下载完成！
```

### 2. 验证签到上传

```bash
# 1. 点击"立即签到"
# 2. 查看 Xcode 控制台日志：
📝 1. 同步签到记录...
✅ 签到记录同步成功
📍 2. 上传位置...
📍 准备上传位置：39.9042, 116.4074
📡 位置上传响应状态码：200
✅ 位置上传成功
📦 3. 同步胶囊数据...
✅ 胶囊同步成功：总计 X, 新增 X, 更新 X
...
🎉 所有同步任务完成！
```

### 3. 验证服务器数据

```bash
# 访问诊断页面
http://8.136.41.211:3395/api/diagnose-all.php

# 查看数据库
mysql -u zhonghuo -p zhonghuo
SELECT COUNT(*) FROM users;           # 应该 ≥ 1
SELECT COUNT(*) FROM capsules;        # 签到后应该有数据
SELECT COUNT(*) FROM will_modules;    # 签到后应该有数据
SELECT COUNT(*) FROM user_locations;  # 签到后应该有数据
```

---

## 🔧 常见问题排查

### Q1: 首页不显示胶囊/遗嘱数量

**可能原因：**
1. 登录失败，token 无效
2. 后端接口未更新
3. 下载失败（网络问题）

**排查步骤：**
```bash
# 1. 检查登录日志
🔵 登录成功，开始处理用户数据...
✅ 用户数据已保存
📥 开始从服务器下载数据...

# 2. 检查下载日志
📦 下载胶囊数据...
✅ 胶囊下载成功：X 个

# 3. 测试后端接口
curl "http://8.136.41.211:3395/api/capsules.php?action=list&token=xxx"
```

### Q2: 签到后数据未上传

**可能原因：**
1. 签到未完成（倒计时未结束）
2. 网络问题
3. Token 过期

**排查步骤：**
```bash
# 1. 检查签到日志
⏰ 用户点击签到
✅ 签到成功

# 2. 检查同步日志
🔄 开始异步同步任务...
📝 1. 同步签到记录...
📍 2. 上传位置...
📦 3. 同步胶囊数据...

# 3. 检查服务器数据
curl "http://8.136.41.211:3395/api/diagnose-all.php"
```

### Q3: 地图不显示用户

**可能原因：**
1. 位置权限未授权
2. 签到未完成
3. 位置上传失败

**排查步骤：**
```bash
# 1. 检查定位权限
# iOS 设置 → 终活 → 位置 → 使用 App 期间

# 2. 检查位置上传日志
📍 准备上传位置：39.9042, 116.4074
📡 位置上传响应状态码：200
✅ 位置上传成功

# 3. 检查服务器数据
curl "http://8.136.41.211:3395/api/location.php?action=list&token=xxx"
```

---

## 📝 部署清单

### 前端部署
- [x] 编译成功
- [x] 安装到模拟器
- [ ] 推送到 GitHub（网络问题，稍后推送）

### 后端部署
- [x] 添加 `list` 接口（遗嘱、见证人）
- [x] 添加 `list_witnesses` 接口
- [ ] 推送到 GitHub（网络问题，稍后推送）
- [ ] SSH 登录服务器拉取最新代码

### 服务器更新命令
```bash
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn
git pull origin main
```

---

## 🎯 测试账号

- **手机号：** `13800138006`
- **密码：** `test123456`
- **验证码：** `123456`（开发模式）

---

*最后更新：2026-03-19 12:40*
