# 🔄 云端优先架构改造方案

## 当前架构（本地优先）❌

```
登录流程:
1. 登录成功 → 保存 user.json 本地文件
2. App 重启 → 从 user.json 加载
3. 数据变更 → 先写本地，再同步到服务器

问题:
- 本地文件可能过期
- 多设备数据不一致
- 登录状态依赖本地文件
```

## 目标架构（云端优先）✅

```
登录流程:
1. 登录成功 → Token 保存 UserDefaults
2. App 重启 → 从 Token 恢复登录状态
3. 数据变更 → 直接写服务器，本地仅缓存

优势:
- 服务器是唯一数据源
- 多设备实时同步
- 登录状态可靠
```

---

## 📋 改造清单

### 1. 登录状态管理 ✅

**当前**:
```swift
// UserManager
func loadUser() {
    if let user = loadUserFromFile() {
        self.isLoggedIn = true  // 依赖本地文件
    }
}
```

**修改为**:
```swift
// UserManager
func loadUser() {
    // 优先从 Token 恢复登录状态
    if let token = UserDefaults.standard.string(forKey: "userToken"),
       !token.isEmpty {
        self.isLoggedIn = true
        print("✅ 从 Token 恢复登录状态")
        // 异步从服务器拉取用户数据
        Task {
            await fetchUserData()
        }
    }
}
```

---

### 2. 数据同步策略 ✅

#### 2.1 时光胶囊
**修改**: `TimeCapsuleView` 的增删改查
```swift
// 创建胶囊
func createCapsule() async {
    // 1. 先上传到服务器
    let success = await DataManager.shared.uploadCapsule(capsule)
    
    // 2. 成功后更新本地
    if success {
        capsules.append(capsule)
    }
}

// 删除胶囊
func deleteCapsule(_ capsule: Capsule) async {
    // 1. 先删除服务器
    let success = await DataManager.shared.deleteCapsule(capsule.id)
    
    // 2. 成功后删除本地
    if success {
        capsules.removeAll { $0.id == capsule.id }
    }
}
```

#### 2.2 遗嘱与资产
**修改**: `WillAssetsView` 的增删改查
```swift
// 更新资产
func updateAsset(_ asset: Asset) async {
    // 实时上传
    await DataManager.shared.syncAssetToServer(asset)
}
```

#### 2.3 紧急联系人
**修改**: `EmergencyContactsView`
```swift
// 添加联系人
func addContact(_ contact: EmergencyContact) async {
    // 实时上传
    await DataManager.shared.addEmergencyContact(contact)
}
```

#### 2.4 家人守护
**修改**: `FamilyGuardView`
```swift
// 绑定家人
func bindFamily(code: String) async {
    // 实时上传
    await DataManager.shared.bindFamily(code: code)
}
```

#### 2.5 签到数据
**修改**: `UserManager.performAutoSignIn()`
```swift
// 签到时自动上传位置和设备信息
func performAutoSignIn() async {
    // 1. 记录签到
    await recordCheckIn()
    
    // 2. 上传位置
    await uploadLocation()
    
    // 3. 上传设备信息
    await uploadDeviceInfo()
}
```

---

### 3. 数据加载策略 ✅

#### 3.1 登录时全量拉取
```swift
// AuthView.handleAuthSuccess()
private func handleAuthSuccess(_ json: [String: Any]) async {
    // 保存 Token
    UserDefaults.standard.set(token, forKey: "userToken")
    
    // 从服务器拉取完整数据
    await DataManager.shared.downloadAllData()
}
```

#### 3.2 App 启动时恢复
```swift
// UserManager.loadUser()
func loadUser() {
    // 从 Token 恢复登录状态
    if let token = UserDefaults.standard.string(forKey: "userToken"),
       !token.isEmpty {
        self.isLoggedIn = true
        
        // 异步拉取用户数据
        Task {
            await fetchUserData()
        }
    }
}
```

---

### 4. 离线支持（可选增强）

```swift
// DataManager
class DataManager: ObservableObject {
    // 本地缓存（只读）
    private var localCache: [String: Any] = [:]
    
    // 网络状态
    var isOnline: Bool {
        // 检测网络
    }
    
    // 智能同步
    func syncData() async {
        if isOnline {
            // 在线：实时上传
            await uploadToServer()
        } else {
            // 离线：保存到本地队列
            saveToOfflineQueue()
        }
    }
}
```

---

## 📊 同步时机

| 操作 | 同步时机 | 说明 |
|------|---------|------|
| 用户登录 | 立即 | 拉取全量数据 |
| 创建胶囊 | 立即 | 上传到服务器 |
| 更新胶囊 | 立即 | 更新服务器 |
| 删除胶囊 | 立即 | 删除服务器 |
| 添加联系人 | 立即 | 上传到服务器 |
| 更新资产 | 立即 | 更新服务器 |
| 绑定家人 | 立即 | 更新服务器 |
| 签到 | 立即 | 上传签到 + 位置 |
| App 启动 | 可选 | 检查数据更新 |
| App 切后台 | 可选 | 保存草稿 |

---

## 🔧 需要修改的文件

### 前端
1. ✅ `UserManager.swift` - 登录状态从 Token 恢复
2. ✅ `AuthView.swift` - 登录后拉取全量数据
3. ✅ `TimeCapsuleView.swift` - 实时上传胶囊
4. ✅ `WillAssetsView.swift` - 实时上传资产
5. ✅ `EmergencyContactsView.swift` - 实时上传联系人
6. ✅ `FamilyGuardView.swift` - 实时上传家人
7. ✅ `DataManager.swift` - 同步策略调整

### 后端
无需修改（API 已就绪）

---

## ✅ 实施步骤

### 阶段 1：登录状态修复（立即）
1. 修改 `UserManager.loadUser()` 从 Token 恢复
2. 测试登录状态持久化

### 阶段 2：数据实时上传（今天）
1. 修改所有增删改操作
2. 先上传服务器，再更新本地

### 阶段 3：优化和增强（可选）
1. 添加离线队列
2. 添加同步状态指示器
3. 添加冲突解决策略

---

*方案制定时间：2026-03-21 02:15*
