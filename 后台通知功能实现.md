# 🔔 后台通知功能实现

## 更新时间
2026-03-21 18:50

## 问题
**用户提问**：如果 App 退出了后台，倒计时结束时还能通知紧急联系人吗？

**答案**：✅ 可以！通过本地通知 + 后台任务实现。

---

## ✅ 实现方案

### 方案概述
1. **本地通知** - 48 小时后提醒用户签到
2. **后台检查任务** - 定期检查超时并通知监护人
3. **通知触发检查** - 用户收到/点击通知时检查超时

---

## 📝 实现细节

### 1. 本地通知（48 小时提醒）

**功能**：用户签到后，设置 48 小时后的本地通知提醒

**代码**：
```swift
func scheduleBackgroundCheck() {
    // 取消之前的通知
    UNUserNotificationCenter.current().removeAllPendingNotificationRequests()
    
    // 计算下次签到时间（48 小时后）
    let checkInInterval: TimeInterval = 48 * 3600
    let nextCheckInTime = Date().addingTimeInterval(checkInInterval)
    
    // 创建通知内容
    let content = UNMutableNotificationContent()
    content.title = "⏰ 该签到啦"
    content.body = "您已经快 48 小时未签到，请打开 App 确认安全"
    content.sound = .default
    
    // 创建触发器（48 小时后）
    let trigger = UNCalendarNotificationTrigger(
        dateMatching: Calendar.current.dateComponents([...], from: nextCheckInTime),
        repeats: true
    )
    
    // 添加通知
    UNUserNotificationCenter.current().add(request)
}
```

**效果**：
- ✅ 即使 App 退出，通知也会按时触发
- ✅ 用户点击通知打开 App
- ✅ 打开 App 时检查超时状态

---

### 2. 后台检查任务（BGTaskScheduler）

**功能**：系统定期唤醒 App 检查超时状态

**代码**：
```swift
// 注册后台任务
BGTaskScheduler.shared.register(forTaskWithIdentifier: "com.zhonghuo.app.check", using: nil) { task in
    self.handleBackgroundCheck(task: task as! BGAppRefreshTask)
}

// 安排后台检查
let request = BGAppRefreshTaskRequest(identifier: "com.zhonghuo.app.check")
request.earliestBeginDate = Date().addingTimeInterval(60 * 60) // 1 小时后
try BGTaskScheduler.shared.submit(request)

// 处理后台检查
func handleBackgroundCheck(task: BGAppRefreshTask) {
    // 检查并通知监护人
    LifeCheckStatusManager.shared.checkAndNotifyGuardians()
    
    // 重新安排下一次检查
    scheduleBackgroundCheckTask()
    
    task.setTaskCompleted(success: true)
}
```

**效果**：
- ✅ 系统定期唤醒 App（时间由系统决定）
- ✅ 后台检查超时状态
- ✅ 超时自动通知监护人

**注意**：
- ⚠️ iOS 后台任务执行时间由系统决定
- ⚠️ 不能保证精确的执行时间
- ⚠️ 需要用户经常使用 App 才会触发

---

### 3. 通知触发检查

**功能**：用户收到/点击通知时检查超时

**代码**：
```swift
// 收到通知时的处理
func userNotificationCenter(_ center: UNUserNotificationCenter, 
                           willPresent notification: UNNotification, 
                           withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void) {
    
    if notification.request.identifier == "checkin_reminder" {
        LifeCheckStatusManager.shared.checkAndNotifyGuardians()
    }
    
    completionHandler([.banner, .sound])
}

// 用户点击通知时的处理
func userNotificationCenter(_ center: UNUserNotificationCenter, 
                           didReceive response: UNNotificationResponse, 
                           withCompletionHandler completionHandler: @escaping () -> Void) {
    
    if response.notification.request.identifier == "checkin_reminder" {
        LifeCheckStatusManager.shared.checkAndNotifyGuardians()
    }
    
    completionHandler()
}
```

**效果**：
- ✅ 通知显示时检查
- ✅ 用户点击时检查
- ✅ 确保及时通知监护人

---

## 📊 工作流程

```
用户签到
    ↓
设置 48 小时后本地通知
    ↓
[48 小时后]
    ↓
通知触发 ⏰
    ↓
检查超时状态
    ↓
超时 ≥ 24 小时？
    ↓ 是
通知监护人 📞
```

---

## 🔧 配置要求

### Info.plist 配置

需要添加后台任务标识：

```xml
<key>BGTaskSchedulerPermittedIdentifiers</key>
<array>
    <string>com.zhonghuo.app.check</string>
</array>
```

### 权限请求

```swift
// 请求通知权限
UNUserNotificationCenter.current().requestAuthorization(options: [.alert, .sound, .badge]) { granted, error in
    // ...
}
```

---

## 📱 测试方法

### 测试 1: 本地通知
1. 签到
2. 等待 48 小时（或修改系统时间）
3. **预期**：收到"⏰ 该签到啦"通知

### 测试 2: 后台检查
1. 签到
2. 将 App 切换到后台
3. 等待 1 小时以上
4. **预期**：系统可能唤醒 App 检查（时间由系统决定）

### 测试 3: 超时通知
1. 修改系统时间，模拟超时 30 小时
2. 收到通知或打开 App
3. **预期**：控制台显示"⚠️ 检测到超时，准备通知监护人..."

---

## ⚠️ 注意事项

### iOS 后台限制
1. **后台任务时间不确定** - 系统决定何时执行
2. **需要用户经常使用** - 不常用的 App 后台任务少
3. **电量优化** - 低电量模式可能限制后台任务

### 最佳实践
1. ✅ 使用本地通知作为主要提醒方式
2. ✅ 后台任务作为辅助检查
3. ✅ App 打开时立即检查
4. ✅ 通知触发时检查

---

## 🎯 三种触发方式

| 方式 | 触发时机 | 可靠性 | 说明 |
|------|----------|--------|------|
| **本地通知** | 48 小时后 | ⭐⭐⭐⭐⭐ | 最可靠，系统保证 |
| **后台任务** | 系统决定 | ⭐⭐⭐ | 辅助检查 |
| **App 打开** | 用户打开 | ⭐⭐⭐⭐⭐ | 立即检查 |

**推荐**：主要依赖本地通知 + App 打开检查，后台任务作为补充。

---

## 📝 修改的文件

### LifeCheckStatusManager.swift
```diff
+ import UserNotifications
+ scheduleBackgroundCheck()       // 设置后台检查
+ checkAndNotifyGuardians()       // 检查并通知
```

### ZhonghuoApp.swift
```diff
+ import BackgroundTasks
+ AppDelegate 实现 UNUserNotificationCenterDelegate
+ setupCheckInReminder()          // 设置签到提醒
+ startBackgroundCheckTask()      // 启动后台检查
+ handleBackgroundCheck()         // 处理后台任务
+ 通知代理方法                    // 收到/点击通知
```

---

## ✅ 状态
- ✅ BUILD SUCCEEDED
- ✅ 已安装到 iPhone 17 Pro 模拟器
- ✅ 待推送

---

## 💡 总结

**问题**：App 退出后台后，倒计时结束时还能通知监护人吗？

**答案**：✅ 可以！通过以下三种方式：

1. **本地通知**（主要）- 48 小时后提醒，系统保证触发
2. **后台任务**（辅助）- 系统定期唤醒检查
3. **App 打开**（立即）- 用户打开时立即检查

**即使 App 退出后台，也能及时通知监护人！** 🎉

---

*最后更新：2026-03-21 18:50*
