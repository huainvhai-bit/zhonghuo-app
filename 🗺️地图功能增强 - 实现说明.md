# 🗺️ 地图功能增强 - 实现说明

## 📅 更新时间
2026-03-21 23:00

## ✨ 新增功能

### 1. 地图类型切换
- **标准地图** - 传统街道地图
- **卫星地图** - 卫星影像图（混合模式）
- **切换按钮** - 在地图右上角

### 2. 定位精度优化
- **精度阈值** - >500 米不上传（避免错误位置）
- **精度显示** - 地图上显示定位精度（开发中）
- **精度存储** - 后端存储 accuracy 字段

### 3. 地图显示优化
- **缩放级别** - 0.005（更近的视角）
- **标记大小** - 36pt（更大的图钉）
- **地图高度** - 280pt（更高的显示区域）
- **视觉效果** - 添加阴影，更立体

---

## 🔧 技术实现

### 前端修改

#### 1. FamilyMemberDetailView.swift
```swift
// 地图类型枚举
enum MapType {
    case standard  // 标准地图
    case hybrid    // 卫星地图
}
@State private var mapType: MapType = .standard

// 地图类型切换按钮
Picker("地图类型", selection: $mapType) {
    Label("标准", systemImage: "map")
    Label("卫星", systemImage: "globe")
}

// iOS 17+ 使用新版 MapKit
if #available(iOS 17.0, *) {
    Map(initialPosition: .region(...)) {
        Marker(...)
    }
    .mapStyle(mapType == .standard ? .standard : .hybrid)
}
```

#### 2. FamilyMember.swift
```swift
struct DeviceInfo: Codable {
    var latitude: Double?
    var longitude: Double?
    var address: String?
    var accuracy: Double?  // ✅ 新增：定位精度（米）
}
```

#### 3. UserManager.swift
```swift
func locationManager(_ manager: CLLocationManager, didUpdateLocations locations: [CLLocation]) {
    let accuracy = location.horizontalAccuracy
    
    // 精度检查
    if accuracy > 500 {
        print("⚠️ 定位精度过低，暂不上传")
        return
    }
    
    // 上传精度信息
    uploadLocationToServer(..., accuracy: accuracy)
}
```

---

### 后端修改

#### 1. api/location.php
```php
// 接收精度字段
$accuracy = $data['accuracy'] ?? null;

// 存储到数据库
$stmt = $db->prepare('INSERT INTO user_locations 
    (user_id, latitude, longitude, address, accuracy, created_at) 
    VALUES (?, ?, ?, ?, ?, ?)');
$stmt->execute([$userId, $latitude, $longitude, $address, $accuracy, $now]);

// 返回精度信息
success([
    'latitude' => (float)$location['latitude'],
    'longitude' => (float)$location['longitude'],
    'accuracy' => $location['accuracy'] ? (float)$location['accuracy'] : null,
    ...
]);
```

#### 2. database_add_accuracy.sql
```sql
-- 添加 accuracy 字段
ALTER TABLE user_locations 
ADD COLUMN IF NOT EXISTS accuracy DECIMAL(10,2) DEFAULT NULL 
COMMENT '定位精度（米）';

-- 添加索引
CREATE INDEX IF NOT EXISTS idx_user_created 
ON user_locations(user_id, created_at);
```

---

## 📦 部署步骤

### 1. 前端部署
```bash
# 拉取最新代码
cd /Users/lishimin/Documents/zhonghuo-app
git pull origin main

# 编译并安装
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
xcrun simctl install "iPhone 17 Pro" <app-path>
```

### 2. 后端部署
```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 进入目录
cd /www/wwwroot/zhonghuo.cn

# 拉取代码
git pull origin main

# 执行数据库迁移
mysql -u zhonghuo -p zhonghuo < database_add_accuracy.sql

# 验证
mysql -u zhonghuo -p zhonghuo -e "DESC user_locations;"
```

---

## ⚠️ 已知限制

### MapKit 在中国的数据问题

**问题描述**：
- Apple MapKit 在中国大陆的地图数据更新较慢
- 卫星地图可能不是最新的
- 某些地区细节不够丰富

**原因**：
- 中国地图数据需要特殊资质
- MapKit 使用高德数据，但更新频率较低
- 卫星图来自第三方供应商

**解决方案**：
1. **短期** - 使用卫星地图模式（通常更新更快）
2. **中期** - 考虑集成高德地图 SDK（需要单独开发）
3. **长期** - 等待 Apple 更新中国地图数据

### 定位精度问题

**影响因素**：
- GPS 信号强度（室内弱，室外强）
- 建筑物遮挡
- 天气条件
- 设备硬件

**建议**：
- 在开阔地带使用
- 避免室内、地下室
- 等待 GPS 稳定（启动后几秒）
- 精度 < 100 米时最准确

---

## 🎯 测试建议

### 1. 地图类型切换
- [ ] 点击"标准"按钮，查看街道地图
- [ ] 点击"卫星"按钮，查看卫星影像
- [ ] 切换流畅，无卡顿

### 2. 定位精度
- [ ] 在开阔地带打开 App
- [ ] 查看日志中的精度值
- [ ] 精度 > 500 米时不上传
- [ ] 精度 < 100 米时最准确

### 3. 地图显示
- [ ] 地图加载迅速
- [ ] 标记位置准确
- [ ] 缩放级别合适
- [ ] 卫星图清晰

---

## 📊 提交统计

| 仓库 | 提交数 | 文件数 | 主要修改 |
|------|--------|--------|----------|
| zhonghuo-app | 3 | 4 | FamilyMemberDetailView, FamilyMember, UserManager |
| zhonghuo-backend-php | 2 | 2 | location.php, database_add_accuracy.sql |

---

## 🔗 相关文档

- [后台通知功能实现.md](./后台通知功能实现.md)
- [短信通知功能实现.md](./短信通知功能实现.md)
- [🚀服务器部署完整指南.md](./🚀服务器部署完整指南.md)

---

*最后更新：2026-03-21 23:00*
