# 🚨 数据库修复 - 只需 1 步！

**问题**：数据库连接失败（用户不存在）  
**解决**：只需在 config.php 中填写 MySQL root 密码

---

## ✅ 修复步骤（仅 1 步）

### 在服务器上执行：

```bash
# 1. SSH 登录
ssh root@8.136.41.211

# 2. 编辑 config.php
vi /www/wwwroot/zhonghuo.cn/config.php

# 3. 找到这一行：
$GLOBALS['mysql_root_pwd'] = getenv('MYSQL_ROOT_PASSWORD') ?: '你的 MySQL root 密码';

# 4. 改成你的实际密码（例如密码是 123456）：
$GLOBALS['mysql_root_pwd'] = '123456';

# 5. 保存退出（按 ESC，输入 :wq，回车）

# 6. 拉取最新代码
cd /www/wwwroot/zhonghuo.cn
git pull origin main

# 7. 测试 API（会自动初始化数据库）
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试","password":"test123","verify_code":"123456"}'
```

---

## 🎯 原理

**首次访问 API 时**：
1. ✅ 检测到数据库不存在
2. ✅ 自动使用 config.php 中的 root 密码连接 MySQL
3. ✅ 创建数据库 `zhonghuo_db`
4. ✅ 创建用户 `zhonghuo_user`
5. ✅ 创建表 `users` 和 `sms_codes`
6. ✅ 返回 API 响应

**完全自动化！**

---

## ✅ 预期响应

```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试",
      "phone": "13800138099"
    }
  }
}
```

---

## 🔍 验证

**访问诊断页面**：
```
http://8.136.41.211:3395/api/check-config.php
```

**应该显示**：
- ✅ 数据库连接成功
- ✅ users 表存在
- ✅ sms_codes 表存在

---

## 🔐 安全建议

**初始化完成后**，可以删除 config.php 中的 root 密码：

```php
// 初始化前
$GLOBALS['mysql_root_pwd'] = '你的密码';

// 初始化后（删除或注释掉）
// $GLOBALS['mysql_root_pwd'] = '你的密码';
```

---

## 📝 总结

**只需 1 步**：
1. ✅ 编辑 config.php，填写 MySQL root 密码

**然后**：
- ✅ git pull 拉取代码
- ✅ 访问 API（自动初始化）

**不需要执行任何 SQL 命令！**

---

*修改后访问 API 即可自动完成数据库初始化！*
