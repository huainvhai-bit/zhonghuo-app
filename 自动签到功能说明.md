# 自动签到功能说明

## 🎯 功能变更

**之前：** 用户需要手动点击"立即签到"按钮  
**现在：** 打开 App 自动签到，无需手动操作

---

## 📱 自动签到触发时机

### 1️⃣ 打开 App 时
```swift
// HomeStatusView.onAppear
.onAppear {
    handleAutoCheckIn()  // 自动签到
    updateStatus()       // 更新倒计时
}
```

### 2️⃣ 从后台进入前台时
```swift
// ContentView.onChange(of: scenePhase)
if newPhase == .active {
    if selectedTab == 0 {
        // 在首页，触发 HomeStatusView 的自动签到
        NotificationCenter.post("TriggerAutoCheckIn")
    } else {
        // 在其他页面，调用 UserManager 签到
        userManager.performAutoSignIn()
    }
}
```

---

## 🔄 自动签到流程

```
打开 App / 进入前台
    ↓
handleAutoCheckIn()
    ↓
userManager.recordCheckIn(isAuto: true)
    ↓
1. 保存签到记录到本地
2. 同步签到记录到服务器
3. 上传位置信息到服务器
4. 同步胶囊数据到服务器
5. 同步遗嘱数据到服务器
6. 同步紧急联系人到服务器
7. 同步见证人到服务器
    ↓
✅ 完成！倒计时重置
```

---

## 📊 数据同步

### 自动上传的数据
1. **签到记录** - `checkin.php?action=record`
2. **位置信息** - `location.php?action=upload`
3. **胶囊数据** - `capsules.php?action=batch_sync`
4. **遗嘱数据** - `will.php?action=batch_sync`
5. **紧急联系人** - `emergency_contacts.php?action=batch_sync`
6. **见证人数据** - `will.php?action=sync_witnesses`

### 上传时机
- ✅ 每次自动签到时
- ✅ 用户完成任何数据修改后（添加/编辑胶囊、遗嘱等）

---

## 📝 日志输出

### 正常签到日志
```
🟢 首页 onAppear - 触发自动签到
🔄 打开 App 自动签到（重置倒计时，证明用户安全）
   - 当前时间：2026-03-19 12:45:00
   - lastCheckIn: 2026-03-17 10:30:00
   - interval: 172800s (两天 小时)
   - 距离上次签到：50.3 小时
✅ 执行自动签到
📝 1. 同步签到记录...
✅ 签到记录同步成功
📍 2. 上传位置...
📍 准备上传位置：39.9042, 116.4074
📡 位置上传响应状态码：200
✅ 位置上传成功
📦 3. 同步胶囊数据...
✅ 胶囊同步完成：(total: 5, created: 0, updated: 5)
📦 4. 同步遗嘱数据...
✅ 遗嘱同步完成：(total: 10, created: 0, updated: 10)
...
🎉 所有同步任务完成！
📍 位置和数据已自动上传到服务器
✅ 自动签到完成！倒计时已重置为 两天 小时
```

### 从后台进入前台日志
```
🟡 scenePhase 变化：active
🟢 从后台进入前台（首页）- 触发自动签到
📍 位置和数据将自动上传到服务器
🔔 收到自动签到通知（从后台进入前台）
...（同上）
```

---

## 🔧 代码修改

### HomeStatusView.swift
**移除：**
- ❌ `handleManualCheckIn()` 函数
- ❌ "立即签到"按钮 UI

**保留：**
- ✅ `handleAutoCheckIn()` - 自动签到核心逻辑
- ✅ `updateStatus()` - 更新倒计时显示
- ✅ `getCheckInStatus()` - 计算签到状态

### ContentView.swift
**优化：**
- ✅ 完善 scenePhase 处理注释
- ✅ 添加自动上传说明

---

## ✅ 测试验证

### 1. 打开 App 测试
```bash
# 1. 打开模拟器上的终活 App
# 2. 查看 Xcode 控制台（Cmd+Shift+Y）
# 3. 确认看到以下日志：
🟢 首页 onAppear - 触发自动签到
✅ 自动签到完成！
📍 位置和数据已自动上传到服务器

# 4. 访问诊断页面验证
http://8.136.41.211:3395/api/diagnose-all.php
# 检查 user_locations 表是否有新记录
```

### 2. 后台进入前台测试
```bash
# 1. 打开 App，等待自动签到完成
# 2. Home 键返回主屏幕（App 进入后台）
# 3. 等待 5 秒
# 4. 重新打开 App
# 5. 查看日志：
🟡 scenePhase 变化：active
🟢 从后台进入前台（首页）- 触发自动签到
✅ 自动签到完成！

# 6. 验证倒计时已重置
```

### 3. 数据同步测试
```bash
# 1. 添加/修改胶囊或遗嘱
# 2. 切换到后台，再回到前台（触发自动签到）
# 3. 查看日志确认数据已同步：
📦 3. 同步胶囊数据...
✅ 胶囊同步完成：(total: X, created: Y, updated: Z)

# 4. 访问诊断页面验证
http://8.136.41.211:3395/api/diagnose-all.php
```

---

## 🎯 用户体验改进

### 之前
1. 打开 App
2. 看到倒计时
3. 点击"立即签到"按钮
4. 等待上传完成
5. 倒计时重置

### 现在
1. 打开 App
2. 自动签到完成 ✅
3. 倒计时已重置 ✅
4. 数据已同步 ✅

**优势：**
- ✅ 操作更简单（无需点击）
- ✅ 数据更及时（打开即同步）
- ✅ 体验更流畅（无感知完成）
- ✅ 证明用户安全（每次打开都签到）

---

## 📅 签到间隔设置

用户可以在设置中调整签到间隔：
- 1 天
- 2 天（默认）
- 3 天
- ...
- 7 天

**注意：** 无论设置多长间隔，每次打开 App 都会自动签到重置倒计时。

---

## 🔒 安全说明

**自动签到的意义：**
- 证明用户安全（还活着）
- 记录用户最后活跃时间
- 上传当前位置信息
- 同步所有数据到服务器

**如果用户长时间未打开 App：**
- 倒计时过期（显示红色）
- 紧急联系人可能收到通知
- 见证人可能被触发

**建议：**
- 每天打开一次 App（最简单）
- 确保允许定位权限
- 保持网络连接

---

*最后更新：2026-03-19 12:45*
