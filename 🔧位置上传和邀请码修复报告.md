# 🚀 位置上传和邀请码获取问题修复 - 部署指南

## 📅 修复时间
2026-03-23 13:04

---

## 🐛 修复的问题

### 问题 1：后端收不到前端定位 ❌
**原因**：API 路径错误
- **前端调用**：`/location.php`
- **后端实际路径**：`/api/location.php`

**修复**：
- 修改 `UserManager.swift` 中的 API URL
- 从 `/location.php` 改为 `/api/location.php`

### 问题 2：前端获取不到二维码和邀请码 ❌
**原因**：API 错误响应格式不匹配
- **后端返回**：`{"error":"...","code":"..."}` (401 状态码)
- **前端期望**：`{"success":false,"message":"..."}`

**修复**：
1. 后端 `api/core.php` - error 函数添加 `success` 和 `message` 字段
2. 前端 `InviteCodeView.swift` - 移除 HTTP 状态码检查，直接解析响应
3. 前端 `FamilyGuardView.swift` - 同样修复

---

## 📦 已推送代码

### 前端 (zhonghuo-app)
**提交**：`b461a2a` - 🐛 修复位置上传和邀请码获取问题

**修改文件**：
- `UserManager.swift` - 位置上传 API 路径
- `InviteCodeView.swift` - 邀请码获取逻辑
- `FamilyGuardView.swift` - 邀请码获取日志

### 后端 (zhonghuo-backend-php)
**提交**：`6268649` - 🐛 修复 API 错误响应格式 - 添加 success 和 message 字段

**修改文件**：
- `api/core.php` - error 函数响应格式

---

## 🔧 服务器部署命令

```bash
# 1. SSH 登录服务器
ssh root@8.136.41.211

# 2. 部署后端
cd /www/wwwroot/zhonghuo.cn
git pull origin main
systemctl restart php-fpm-81

# 3. 验证部署
curl -s 'http://8.136.41.211:3395/api/family.php?action=get_invite_code'
# 应该返回：{"success":false,"error":"未登录或 token 无效",...}

# 4. 测试位置上传 API（需要有效 token）
# 前端会自动测试
```

---

## ✅ 验证清单

### 1. 位置上传测试
1. 打开 App，登录账号
2. 进入「我的」→「家人守护」
3. 开启位置共享
4. 检查后端日志：`tail -f /www/wwwlogs/php-fpm.log`
5. 应该看到位置上传请求

### 2. 邀请码获取测试
1. 打开 App，登录账号
2. 进入「家人守护」
3. 点击「生成邀请码」或扫描二维码
4. 应该能正常显示邀请码和二维码

---

## 📊 API 测试

### 测试邀请码 API（未登录）
```bash
curl -s 'http://8.136.41.211:3395/api/family.php?action=get_invite_code'
```
**预期响应**：
```json
{
  "success": false,
  "error": "未登录或 token 无效",
  "code": "UNAUTHORIZED",
  "message": "未登录或 token 无效"
}
```

### 测试位置上传 API（需要 token）
```bash
curl -s -X POST 'http://8.136.41.211:3395/api/location.php' \
  -H 'Content-Type: application/json' \
  -d '{"action":"upload","token":"YOUR_TOKEN","user_id":"USER_ID","latitude":39.9,"longitude":116.4}'
```

---

## 🔍 故障排查

### 如果位置仍然无法上传
1. 检查前端日志（Xcode Console）
2. 查看是否有 `📍 位置上传失败` 错误
3. 检查后端日志：`tail -200 /www/wwwlogs/php-fpm.log`

### 如果邀请码仍然无法获取
1. 检查前端日志（Xcode Console）
2. 查看是否有 `errorMessage`
3. 手动测试 API：
   ```bash
   curl -s 'http://8.136.41.211:3395/api/family.php?action=get_invite_code'
   ```

---

## 📝 相关文档
- `/Users/lishimin/Documents/zhonghuo-app/🔧位置上传和邀请码修复报告.md`（待创建）

---

**修复完成！请部署后测试验证。** ✅
