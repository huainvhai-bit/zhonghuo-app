# 🔔 完整后台通知流程实现

## 更新时间
2026-03-21 19:30

---

## ✅ 完整流程

```
用户签到
    ↓
设置 48 小时（可配置）后本地通知 🔔
    ↓
[App 可退出后台]
    ↓
倒计时剩余 12 小时（可配置）
    ↓
系统显示通知 ⏰ "该签到啦"
    ↓
每 2 小时（可配置）继续系统显示通知 ⏰ "该签到啦"
    ↓
倒计时结束后 [48 小时后]
    ↓
┌─────────────────────────────────┐
│ 如果 App 在后台运行             │
│ ↓                               │
│ 自动发送短信给所有紧急联系人 📞 │
└─────────────────────────────────┘
    ↓
如果 App 完全退出
    ↓
每 1 小时（可配置）推送系统通知 ⏰
"您已超过签到时间，请打开 app 确认安全"
    ↓
用户打开 App
    ↓
自动签到 ✅
    ↓
重置倒计时 → 安全
```

---

## 📝 实现细节

### 1. 可配置的时间参数

**文件**：`NotificationConfig`（集成到 LifeCheckStatusManager.swift）

```swift
struct NotificationConfig {
    var checkInInterval: Int = 48         // 签到间隔（小时）
    var firstReminderHours: Int = 12      // 首次提醒时间（剩余小时）
    var reminderInterval: Int = 2         // 重复提醒间隔（小时）
    var overduePushInterval: Int = 1      // 超时后推送间隔（小时）
    var enableSmsNotification: Bool = true // 是否启用短信通知
}
```

**说明**：
- 所有时间参数都可从后端配置
- 默认值符合用户需求

---

### 2. 通知阶段

#### 阶段 1: 首次提醒（剩余 12 小时）
```swift
scheduleFirstReminder()
// 通知内容："您即将需要签到，请打开 App 确认安全"
```

#### 阶段 2: 重复提醒（每 2 小时）
```swift
scheduleRepeatReminders()
// 通知内容："您还有 X 小时需要签到"
// 最多设置 10 个重复提醒
```

#### 阶段 3: 超时通知（每 1 小时）
```swift
scheduleOverdueNotifications()
// 通知内容："您已超过签到时间 X 小时，请打开 App 确认安全"
// 设置 5 个超时通知
```

#### 阶段 4: 后台短信通知
```swift
scheduleBackgroundSmsTask()
// 超时后触发后台任务
// 自动发送短信给所有紧急联系人
```

---

### 3. 核心代码

#### 设置完整通知流程
```swift
func scheduleCheckInNotifications() {
    // 1. 取消之前的通知
    UNUserNotificationCenter.current().removeAllPendingNotificationRequests()
    
    // 2. 计算下次签到时间
    let nextCheckInTime = lastCheckIn.addingTimeInterval(48 * 3600)
    
    // 3. 设置首次提醒（剩余 12 小时）
    let firstReminderTime = nextCheckInTime.addingTimeInterval(-12 * 3600)
    scheduleFirstReminder()
    
    // 4. 设置重复提醒（每 2 小时）
    scheduleRepeatReminders(from: firstReminderTime, to: nextCheckInTime)
    
    // 5. 设置超时通知（每 1 小时）
    scheduleOverdueNotifications(after: nextCheckInTime)
    
    // 6. 安排后台短信任务
    scheduleBackgroundSmsTask(after: nextCheckInTime)
}
```

#### 后台短信通知任务
```swift
func handleBackgroundSmsTask(task: BGAppRefreshTask) {
    updateStatus()
    
    if !isSafe && config.enableSmsNotification {
        let hoursOverdue = -hoursRemaining
        
        // 超时即发送短信
        if hoursOverdue > 0 {
            await notifyGuardians()
        }
    }
    
    task.setTaskCompleted(success: true)
}
```

#### 通知代理（超时触发短信）
```swift
func userNotificationCenter(_ center: UNUserNotificationCenter, 
                           willPresent notification: UNNotification, 
                           withCompletionHandler completionHandler: @escaping (UNNotificationPresentationOptions) -> Void) {
    
    // 超时通知触发短信
    if notification.request.identifier.contains("checkin_overdue") {
        Task {
            await LifeCheckStatusManager.shared.notifyGuardians()
        }
    }
    
    completionHandler([.banner, .sound])
}
```

---

## 📊 通知时间线示例

假设用户 **2026-03-21 10:00** 签到：

| 时间 | 事件 | 通知内容 |
|------|------|----------|
| 03-21 10:00 | 用户签到 | - |
| 03-23 10:00 | 剩余 12 小时 | ⏰ 该签到啦 |
| 03-23 12:00 | 剩余 10 小时 | ⏰ 您还有 10 小时需要签到 |
| 03-23 14:00 | 剩余 8 小时 | ⏰ 您还有 8 小时需要签到 |
| 03-23 16:00 | 剩余 6 小时 | ⏰ 您还有 6 小时需要签到 |
| 03-23 18:00 | 剩余 4 小时 | ⏰ 您还有 4 小时需要签到 |
| 03-23 20:00 | 剩余 2 小时 | ⏰ 您还有 2 小时需要签到 |
| **03-23 22:00** | **到期** | - |
| 03-23 23:00 | 超时 1 小时 | ⚠️ 已超时 1 小时 + 📞 短信通知 |
| 03-24 00:00 | 超时 2 小时 | ⚠️ 已超时 2 小时 + 📞 短信通知 |
| 03-24 01:00 | 超时 3 小时 | ⚠️ 已超时 3 小时 + 📞 短信通知 |

---

## 🎯 三种场景处理

### 场景 1: App 在后台运行
- ✅ 超时通知触发
- ✅ 自动发送短信给监护人
- ✅ 用户打开 App → 自动签到

### 场景 2: App 完全退出
- ✅ 系统继续显示通知（每 1 小时）
- ⚠️ 无法发送短信（需要后端定时任务）
- ✅ 用户打开 App → 自动签到

### 场景 3: 用户看到通知打开 App
- ✅ 自动签到
- ✅ 重置倒计时
- ✅ 取消所有待处理通知
- ✅ 重新设置通知流程

---

## 🔧 配置方式

### 方式 1: 代码配置（当前）
```swift
private var config: NotificationConfig {
    return NotificationConfig(
        checkInInterval: 48,
        firstReminderHours: 12,
        reminderInterval: 2,
        overduePushInterval: 1,
        enableSmsNotification: true
    )
}
```

### 方式 2: 从后端获取（推荐）
```swift
// API: GET /api/notification_config
// 返回：
{
    "checkInInterval": 48,
    "firstReminderHours": 12,
    "reminderInterval": 2,
    "overduePushInterval": 1,
    "enableSmsNotification": true
}
```

---

## 📱 测试方法

### 测试 1: 完整通知流程
1. 签到
2. 修改系统时间到剩余 12 小时
3. **预期**：收到首次提醒通知
4. 继续修改时间，每 2 小时
5. **预期**：收到重复提醒通知
6. 修改时间到超时
7. **预期**：收到超时通知 + 短信通知

### 测试 2: App 在后台
1. 签到
2. 将 App 切换到后台
3. 修改系统时间到超时
4. **预期**：收到超时通知
5. **预期**：控制台显示"📱 超时通知触发，准备发送短信..."

### 测试 3: 用户打开 App
1. 收到通知
2. 点击通知打开 App
3. **预期**：自动签到
4. **预期**：倒计时重置为 48 小时
5. **预期**：通知流程重新设置

---

## 📝 修改的文件

### 新增
- **LifeCheckStatusManager.swift** - 集成 NotificationConfig

### 修改
1. **LifeCheckStatusManager.swift**
   - 添加 NotificationConfig 结构体
   - scheduleCheckInNotifications() - 完整通知流程
   - scheduleFirstReminder() - 首次提醒
   - scheduleRepeatReminders() - 重复提醒
   - scheduleOverdueNotifications() - 超时通知
   - scheduleBackgroundSmsTask() - 后台短信任务
   - handleBackgroundSmsTask() - 处理后台任务

2. **ZhonghuoApp.swift**
   - startBackgroundTasks() - 启动后台任务
   - handleBackgroundSmsTask() - 短信通知任务
   - handleNotificationRefresh() - 通知刷新任务
   - userNotificationCenter - 超时触发短信

3. **删除**
   - NotificationConfig.swift（集成到 LifeCheckStatusManager）

---

## ✅ 状态
- ✅ BUILD SUCCEEDED
- ✅ 已安装到 iPhone 17 Pro 模拟器
- ✅ 待推送

---

## 💡 总结

**实现的功能**：
1. ✅ 可配置的时间参数（签到间隔、提醒时间、推送间隔）
2. ✅ 多阶段通知（首次提醒、重复提醒、超时通知）
3. ✅ 后台短信通知（App 在后台运行时）
4. ✅ 超时自动通知监护人
5. ✅ 用户打开 App 自动签到

**优势**：
- 🎯 完整的通知流程，覆盖所有场景
- ⚙️ 可配置的时间参数，灵活适应需求
- 📱 即使用户不打开 App，也能通知监护人
- 🔔 系统通知 + 短信通知，双重保障

**下一步**：
- [ ] 从后端获取配置参数
- [ ] 测试完整流程
- [ ] 优化通知文案

---

*最后更新：2026-03-21 19:30*
