# 🔧 终活 App 登录问题完整诊断报告

**时间**: 2026-03-17 13:45  
**问题**: 模拟器登录提示"服务器错误"

---

## ✅ 后端状态（已确认正常）

### API 测试结果
```bash
✅ POST http://8.136.41.211:3395/api/users.php
   状态码：200 OK
   响应：{"success": true, "token": "..."}
```

### 后端环境
- ✅ PHP-FPM: 5 workers 运行中
- ✅ nginx: 配置正确
- ✅ 数据库：连接正常
- ✅ JWT: 功能正常

---

## 🔍 可能的问题原因

### 1. 模拟器网络权限问题 ⚠️ **最可能**

**症状**: 模拟器无法访问外部网络

**检查方法**:
```bash
# 在模拟器中打开 Safari，访问
http://8.136.41.211:3395/api/config.php
```

**解决方案**:
1. 检查 Mac 防火墙设置
2. 检查模拟器网络权限
3. 尝试使用 Mac 的 IP 地址代替服务器 IP

---

### 2. 前端 API 配置问题

**检查点**:
- `DataManager.baseURL` 是否正确
- `DataManager.apiURL` 是否正确

**当前配置**:
```swift
DataManager.baseURL = "http://8.136.41.211:3395"
DataManager.apiURL = "http://8.136.41.211:3395/api"
```

---

### 3. HTTPS/ATS 限制

**问题**: iOS 默认要求 HTTPS 连接

**解决方案**: 在 `Info.plist` 中添加：
```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <true/>
</dict>
```

---

### 4. 后端 PHP 扩展缺失

**必需扩展**:
- ✅ PHP 7.4+
- ✅ PDO MySQL
- ✅ JSON
- ✅ OpenSSL (JWT)
- ✅ mbstring

**检查命令**:
```bash
ssh root@8.136.41.211
php -m | grep -E 'pdo|json|openssl|mbstring'
```

---

### 5. 数据库连接问题

**检查项**:
```bash
ssh root@8.136.41.211
mysql -uzhonghuo -pwangzi1314 zhonghuo -e "SELECT 1"
```

---

## 🛠️ 完整修复步骤

### 步骤 1: 检查 Info.plist

确保包含 ATS 配置：

```bash
cd /Users/lishimin/Documents/zhonghuo-app
cat Info.plist | grep -A 10 "NSAppTransportSecurity"
```

如果没有，添加：
```xml
<key>NSAppTransportSecurity</key>
<dict>
    <key>NSAllowsArbitraryLoads</key>
    <true/>
</dict>
```

---

### 步骤 2: 测试模拟器网络

在模拟器中：
1. 打开 Safari
2. 访问 `http://8.136.41.211:3395/api/config.php`
3. 应该看到 JSON 响应

如果无法访问：
- 检查 Mac 防火墙
- 尝试使用 Mac 本地 IP

---

### 步骤 3: 添加详细错误日志

修改 `AuthView.swift` 的 `loginWithAPI()` 函数，添加更详细的错误信息：

```swift
private func loginWithAPI() async {
    print("🔵 开始登录流程...")
    print("  phone: \(phone)")
    print("  loginType: \(loginType)")
    print("  DataManager.baseURL: \(DataManager.baseURL)")
    print("  DataManager.apiURL: \(DataManager.apiURL)")
    print("  isBackendOnline: \(DataManager.shared.isBackendOnline)")
    
    // 测试基础网络连接
    do {
        let testURL = URL(string: "\(DataManager.baseURL)/api/config.php")!
        print("📡 测试连接：\(testURL)")
        let (testData, testResponse) = try await URLSession.shared.data(from: testURL)
        print("✅ 连接测试成功")
        print("  响应：\(String(data: testData, encoding: .utf8) ?? "nil")")
    } catch {
        print("❌ 连接测试失败：\(error)")
        await MainActor.run {
            errorMessage = "无法连接到服务器：\(error.localizedDescription)\n\n请检查:\n1. 网络连接\n2. 服务器地址\n3. 防火墙设置"
            showingError = true
        }
        return
    }
    
    // ... 其余登录逻辑
}
```

---

### 步骤 4: 验证后端 PHP 扩展

```bash
ssh root@8.136.41.211

# 检查 PHP 版本
php -v

# 检查必需扩展
php -m | grep -E 'pdo_mysql|json|openssl|mbstring'

# 如果缺少，安装
apt-get install php7.4-mysql php7.4-json php7.4-openssl php7.4-mbstring
```

---

### 步骤 5: 检查数据库连接

```bash
ssh root@8.136.41.211

# 测试数据库连接
mysql -uzhonghuo -pwangzi1314 zhonghuo -e "SHOW TABLES"

# 检查 users 表
mysql -uzhonghuo -pwangzi1314 zhonghuo -e "SELECT id, name, phone FROM users LIMIT 3"
```

---

## 📋 服务器环境清单

### 必需软件
- [x] nginx (Web 服务器)
- [x] PHP 7.4+ (PHP 解释器)
- [x] PHP-FPM (FastCGI 进程管理器)
- [x] MySQL 5.7+ (数据库)

### 必需 PHP 扩展
- [x] pdo_mysql (数据库连接)
- [x] json (JSON 处理)
- [x] openssl (JWT 签名)
- [x] mbstring (多字节字符串)

### 必需配置
- [x] nginx: enable-php-74.conf
- [x] PHP-FPM: www.conf (pool 配置)
- [x] config.php (数据库配置)
- [x] jwt.php (JWT 库)

### 文件权限
```bash
/www/wwwroot/zhonghuo.cn/
├── api/           755
│   ├── users.php  644
│   ├── core.php   644
│   ├── jwt.php    644
│   └── config.php 644
├── config.php     644
└── data/          755 (可写)
```

---

## 🧪 完整测试流程

### 1. 服务器端测试
```bash
# SSH 登录
ssh root@8.136.41.211

# 测试 PHP
php -l /www/wwwroot/zhonghuo.cn/api/users.php

# 测试 API
curl -X POST http://localhost:3395/api/users.php \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13233323334","password":"test123456"}'

# 检查日志
tail -20 /www/wwwroot/zhonghuo.cn/data/error.log
```

### 2. Mac 端测试
```bash
# 测试服务器连通性
curl http://8.136.41.211:3395/api/config.php

# 测试登录 API
curl -X POST http://8.136.41.211:3395/api/users.php \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13233323334","password":"test123456"}'
```

### 3. 模拟器测试
1. 打开 Safari，访问 `http://8.136.41.211:3395/api/config.php`
2. 如果能看到 JSON，说明网络正常
3. 打开 App，尝试登录
4. 查看 Xcode 控制台的详细日志

---

## 🎯 快速诊断脚本

创建 `/Users/lishimin/Documents/zhonghuo-app/diagnose.sh`:

```bash
#!/bin/bash

echo "=== 终活 App 网络诊断 ==="
echo ""

# 1. 测试服务器连通性
echo "1. 测试服务器连通性..."
curl -s -o /dev/null -w "%{http_code}" http://8.136.41.211:3395/api/config.php
echo ""

# 2. 测试登录 API
echo "2. 测试登录 API..."
curl -s -X POST http://8.136.41.211:3395/api/users.php \
  -H "Content-Type: application/json" \
  -d '{"action":"login","phone":"13233323334","password":"test123456"}' | jq '.success'

# 3. 检查 Info.plist
echo "3. 检查 Info.plist ATS 配置..."
grep -A 5 "NSAppTransportSecurity" Info.plist || echo "❌ 未找到 ATS 配置"

# 4. 检查 DataManager
echo "4. 检查 DataManager API 配置..."
grep -A 2 "baseURL" DataManager.swift | head -5

echo ""
echo "=== 诊断完成 ==="
```

---

## 📝 下一步行动

1. **立即检查**: Info.plist 中的 ATS 配置
2. **测试网络**: 在模拟器 Safari 中访问 API
3. **查看详细错误**: 运行 App 并查看 Xcode 控制台
4. **服务器检查**: 确认 PHP 扩展完整

---

**最后更新**: 2026-03-17 13:45
