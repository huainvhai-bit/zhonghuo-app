# 📱 前后端配置对接完成

## 更新时间
2026-03-21 20:00

---

## ✅ 完成的功能

### 1. 后端配置 API

**文件**：`api/notification_config.php`

**接口**：
```
GET /api/notification_config.php?action=get
POST /api/notification_config.php?action=update
```

**返回数据**：
```json
{
  "success": true,
  "data": {
    "checkInInterval": 48,
    "firstReminderHours": 12,
    "reminderInterval": 2,
    "overduePushInterval": 1,
    "enableSmsNotification": true,
    "overdueThreshold": 0,
    "smsProvider": "aliyun",
    "maxReminders": 10,
    "maxOverdueNotifications": 5
  }
}
```

---

### 2. 短信发送 API

**文件**：`api/sms.php`

**接口**：
```
POST /api/sms.php?action=send          # 发送短信
POST /api/sms.php?action=send_overdue  # 超时通知
POST /api/sms.php?action=send_code     # 发送验证码
```

**支持服务商**：
- ✅ 阿里云短信
- ✅ 腾讯云短信
- ✅ 可扩展其他服务商

---

### 3. 定时检查任务

**文件**：`cron/check_timeout.php`

**执行频率**：每分钟

**功能**：
1. 查询所有活跃用户
2. 计算超时时长
3. 超时用户 → 发送短信给紧急联系人
4. 记录执行日志

**Cron 配置**：
```bash
* * * * * php /www/wwwroot/zhonghuo.cn/cron/check_timeout.php
```

---

### 4. 数据库表

**文件**：`database_config.sql`

**新增表**：
1. **app_config** - 应用配置表
2. **sms_logs** - 短信发送日志
3. **verification_codes** - 验证码表
4. **cron_logs** - 定时任务日志

---

### 5. 前端配置加载

**修改文件**：
- `DataManager.swift` - 添加 `fetchNotificationConfig()`
- `LifeCheckStatusManager.swift` - 添加 `loadNotificationConfig()`
- `ZhonghuoApp.swift` - App 启动时加载配置

**工作流程**：
```
App 启动
    ↓
调用后端 API 获取配置
    ↓
缓存到 UserDefaults
    ↓
使用配置设置通知
```

---

## 🔄 前后端配置对应

| 前端字段 | 后端配置键 | 说明 | 默认值 |
|----------|------------|------|--------|
| `checkInInterval` | `notify_checkin_interval` | 签到间隔（小时） | 48 |
| `firstReminderHours` | `notify_first_reminder` | 首次提醒（剩余小时） | 12 |
| `reminderInterval` | `notify_reminder_interval` | 重复提醒间隔（小时） | 2 |
| `overduePushInterval` | `notify_overdue_interval` | 超时推送间隔（小时） | 1 |
| `enableSmsNotification` | `notify_enable_sms` | 启用短信通知 | true |
| `overdueThreshold` | `notify_overdue_threshold` | 超时阈值（小时） | 0 |
| `smsProvider` | `sms_provider` | 短信服务商 | aliyun |
| `maxReminders` | `notify_max_reminders` | 最大提醒次数 | 10 |
| `maxOverdueNotifications` | `notify_max_overdue` | 最大超时通知 | 5 |

---

## 📊 完整工作流程

### 流程 1: 配置加载
```
App 启动
    ↓
LifeCheckStatusManager.loadNotificationConfig()
    ↓
DataManager.fetchNotificationConfig()
    ↓
GET /api/notification_config.php?action=get
    ↓
返回配置 JSON
    ↓
缓存到 UserDefaults
    ↓
使用配置设置通知
```

### 流程 2: 用户签到
```
用户签到
    ↓
设置 48 小时后通知（从配置读取）
    ↓
计算首次提醒时间（剩余 12 小时）
    ↓
设置重复提醒（每 2 小时）
    ↓
设置超时通知（每 1 小时）
```

### 流程 3: 超时通知
```
用户超时
    ↓
本地通知触发（App 在后台）
    ↓
POST /api/sms.php?action=send_overdue
    ↓
发送短信给监护人

或者

定时任务检查（每分钟）
    ↓
查询超时用户
    ↓
发送短信给监护人
    ↓
记录到 sms_logs
```

---

## 🚀 部署步骤

### 1. 部署后端

```bash
# SSH 登录服务器
ssh root@8.136.41.211

# 进入应用目录
cd /www/wwwroot/zhonghuo.cn

# 拉取最新代码
git pull origin main

# 导入数据库配置
mysql -u zhonghuo -p zhonghuo < database_config.sql

# 设置文件权限
chmod 755 api/notification_config.php api/sms.php cron/check_timeout.php
chown -R www:www api/ cron/

# 配置定时任务
crontab -e
# 添加：* * * * * php /www/wwwroot/zhonghuo.cn/cron/check_timeout.php
```

### 2. 配置短信服务

```sql
-- 阿里云配置
UPDATE app_config SET config_value = '你的 AccessKey ID' WHERE config_key = 'sms_aliyun_key_id';
UPDATE app_config SET config_value = '你的 AccessKey Secret' WHERE config_key = 'sms_aliyun_key_secret';
UPDATE app_config SET config_value = '终活' WHERE config_key = 'sms_aliyun_sign';
UPDATE app_config SET config_value = '你的模板 ID' WHERE config_key = 'sms_aliyun_template';
```

### 3. 测试验证

```bash
# 测试获取配置
curl http://8.136.41.211:3395/api/notification_config.php?action=get

# 测试发送短信
curl -X POST http://8.136.41.211:3395/api/sms.php?action=send \
  -H "Content-Type: application/json" \
  -d '{"phone": "13800138000", "message": "测试"}'

# 手动执行定时任务
php /www/wwwroot/zhonghuo.cn/cron/check_timeout.php
```

---

## 📝 配置管理

### 修改签到间隔（从 48 小时改为 24 小时）

**方式 1: 数据库**
```sql
UPDATE app_config SET config_value = '24' WHERE config_key = 'notify_checkin_interval';
```

**方式 2: API**
```bash
curl -X POST http://8.136.41.211:3395/api/notification_config.php?action=update \
  -H "Content-Type: application/json" \
  -d '{"checkInInterval": 24}'
```

**效果**：
- 前端 App 下次启动时自动获取新配置
- 无需重新编译 App
- 无需用户更新

---

### 临时关闭短信通知

```sql
UPDATE app_config SET config_value = '0' WHERE config_key = 'notify_enable_sms';
```

**效果**：
- 所有短信通知暂停
- 本地通知继续工作
- 定时任务不发送短信

---

## 🎯 优势

### 1. 灵活配置
- ✅ 所有时间参数都可配置
- ✅ 无需重新编译 App
- ✅ 实时生效

### 2. 双重保障
- ✅ 本地通知（前端）
- ✅ 定时任务（后端）
- ✅ 即使 App 退出也能通知

### 3. 完整日志
- ✅ 短信发送日志
- ✅ 定时任务日志
- ✅ 便于问题排查

### 4. 可扩展
- ✅ 支持多短信服务商
- ✅ 可添加更多配置项
- ✅ 易于维护

---

## ✅ 状态

- ✅ 后端 API 已完成
- ✅ 前端配置加载已完成
- ✅ 数据库表已创建
- ✅ 定时任务已实现
- ✅ 文档已完善
- ✅ 已推送到 GitHub

---

## 📱 测试建议

### 测试 1: 配置获取
1. 打开 App
2. 查看控制台日志
3. **预期**：显示"✅ 获取通知配置成功"

### 测试 2: 配置修改
1. 修改数据库配置
2. 重启 App
3. **预期**：使用新配置

### 测试 3: 短信发送
1. 模拟超时
2. 查看短信日志
3. **预期**：收到短信

---

**🎉 前后端配置对接完成！**

所有参数都可通过后端配置，无需重新编译 App！🚀

---

*最后更新：2026-03-21 20:00*
