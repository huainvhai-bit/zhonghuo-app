# 🔧 注册网络错误修复报告

**修复时间**：2026-03-17 20:45  
**问题**：注册提示"the data couldn't be read because it is missing"（JSON 解析失败）

---

## 🔍 问题诊断

### 测试结果
```bash
curl -X POST "http://8.136.41.211:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试","password":"test123"}'
```

**服务器返回**：
```json
{"error":"数据库连接失败：SQLSTATE[HY000] [1045] Access denied for user 'zhonghuo_user'@'localhost' (using password: YES)","code":"DB_ERROR"}
```

### 根本原因

1. **数据库连接失败** - 服务器数据库配置错误或用户权限问题
2. **错误信息包含中文** - 前端 JSONDecoder 解析失败
3. **HTTP 500 错误** - 前端未正确处理错误响应

---

## ✅ 修复方案

### 前端修复（AuthView.swift）

**修改内容**：
1. 添加原始响应打印（调试用）
2. 改进错误处理逻辑
3. 添加数据库错误识别
4. 显示详细错误信息

**关键代码**：
```swift
// 先打印原始响应内容（用于调试）
let rawResponse = String(data: data, encoding: .utf8) ?? "无法解析"
print("  原始响应：\(rawResponse)")

// 检查错误响应（HTTP 400/500）
if httpResponse.statusCode == 400 || httpResponse.statusCode == 500 {
    // 尝试解析错误信息
    if let errorJSON = try? JSONSerialization.jsonObject(with: data) as? [String: Any] {
        let errorCode = errorJSON["code"] as? String ?? ""
        let errorMsg = errorJSON["error"] as? String ?? ""
        
        // 根据错误码判断
        if errorMsg.contains("数据库") || errorCode == "DB_ERROR" {
            errorMessage = "数据库连接失败，请稍后重试"
            return
        } else if errorMsg.contains("已注册") || errorCode == "PHONE_EXISTS" {
            errorMessage = "该手机号已注册"
            return
        }
    }
    
    // 无法解析或其他错误
    errorMessage = "服务器错误：\(httpResponse.statusCode)\n\(rawResponse)"
    return
}
```

**提交**：`7c8f0b5` - 🔧 修复注册错误提示：添加详细日志和错误处理

### 后端修复（api/users.php）

**修改内容**：
1. 隐藏生产环境数据库详细信息
2. 区分不同类型的数据库错误
3. 返回简洁的错误信息

**关键代码**：
```php
function getDB() {
    try {
        $db = new PDO(...);
        return $db;
    } catch (Exception $e) {
        $errorMsg = $e->getMessage();
        error_log("数据库连接失败：" . $errorMsg);
        
        // 生产环境隐藏详细信息
        if (strpos($errorMsg, 'Access denied') !== false) {
            error('数据库连接失败，请检查配置', 'DB_ERROR');
        } else {
            error('数据库错误', 'DB_ERROR');
        }
    }
}
```

**提交**：`acc941b` - 🔧 修复数据库错误信息：生产环境隐藏详细信息

---

## 📊 修复状态

### 前端
- ✅ 代码修复完成
- ✅ 编译成功
- ✅ 安装到模拟器（PID: 28013）
- ✅ 已推送到 GitHub

### 后端
- ✅ 代码修复完成
- ⚠️ 推送失败（网络问题）
- **本地提交**：`acc941b`

---

## 🚨 服务器问题

**数据库连接失败原因**（需要检查）：

1. **数据库用户权限**
   ```sql
   SHOW GRANTS FOR 'zhonghuo_user'@'localhost';
   ```

2. **数据库是否存在**
   ```sql
   SHOW DATABASES LIKE 'zhonghuo_db';
   ```

3. **config.php 配置**
   ```php
   $GLOBALS['db_host'] = 'localhost';
   $GLOBALS['db_name'] = 'zhonghuo_db';
   $GLOBALS['db_user'] = 'zhonghuo_user';
   $GLOBALS['db_pass'] = 'Zhonghuo@2026';
   ```

---

## 🛠️ 服务器修复步骤

**请在服务器上执行**：

```bash
# 1. SSH 登录服务器
ssh root@8.136.41.211

# 2. 检查数据库
mysql -u root -p

# 3. 创建数据库（如果不存在）
CREATE DATABASE IF NOT EXISTS zhonghuo_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

# 4. 创建用户并授权
CREATE USER IF NOT EXISTS 'zhonghuo_user'@'localhost' IDENTIFIED BY 'Zhonghuo@2026';
GRANT ALL PRIVILEGES ON zhonghuo_db.* TO 'zhonghuo_user'@'localhost';
FLUSH PRIVILEGES;

# 5. 创建 users 表
USE zhonghuo_db;
CREATE TABLE IF NOT EXISTS users (
    id VARCHAR(64) PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    phone VARCHAR(20) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    last_login_at DATETIME NULL,
    INDEX idx_phone (phone)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

# 6. 创建 sms_codes 表
CREATE TABLE IF NOT EXISTS sms_codes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    phone VARCHAR(20) NOT NULL,
    code VARCHAR(10) NOT NULL,
    action VARCHAR(20) NOT NULL,
    expires_at DATETIME NOT NULL,
    is_used TINYINT DEFAULT 0,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_phone_action (phone, action),
    INDEX idx_expires (expires_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

# 7. 退出
EXIT;

# 8. 拉取最新后端代码
cd /www/wwwroot/zhonghuo.cn
git pull origin main

# 9. 设置权限
chown -R www:www .

# 10. 测试 API
curl -X POST "http://localhost:3395/api/users.php" \
  -H "Content-Type: application/json" \
  -d '{"action":"register","phone":"13800138099","name":"测试","password":"test123"}'
```

**预期响应**：
```json
{
  "success": true,
  "message": "成功",
  "data": {
    "token": "...",
    "user_id": "user_xxx",
    "is_new": true,
    "user": {
      "id": "user_xxx",
      "name": "测试",
      "phone": "13800138099"
    }
  }
}
```

---

## 📝 修改文件清单

### 前端（zhonghuo-app）
- ✅ `AuthView.swift` - 改进错误处理和日志

### 后端（zhonghuo-backend-php）
- ✅ `api/users.php` - 隐藏数据库详细信息

---

## ✅ 验证清单

- [x] 前端错误处理改进
- [x] 前端编译成功
- [x] 前端安装到模拟器
- [x] 前端推送到 GitHub
- [x] 后端错误信息优化
- [ ] 后端推送到 GitHub（网络问题，待重试）
- [ ] 服务器数据库配置（待修复）
- [ ] 服务器代码更新（待部署）
- [ ] 注册功能测试（待数据库修复后）

---

## 🎯 下一步

1. **修复服务器数据库** - 按上述步骤创建数据库和用户
2. **推送后端代码** - 解决网络问题后推送
3. **部署到服务器** - `git pull origin main`
4. **测试注册功能** - 验证是否成功

---

*报告生成时间：2026-03-17 20:45*
