# 🔧 8 个功能修复计划

**时间**: 2026-03-17 15:35

---

## 修复清单

### ✅ 前端修复 (1-4)

1. **所有日期用中文日期显示** 
   - 修改 `DateFormatter` 的 locale 为 `zh_CN`
   - 影响文件：`HomeStatusView.swift`, `TimeCapsuleView.swift`, `WillAssetsView.swift` 等

2. **我的 - 编辑信息内手机号显示**
   - 修改 `EditProfileModal` 中的手机号显示
   - 灰色、不可修改、不标注"不可修改"

3. **我的 - 编辑信息默认使用注册姓名**
   - 确保 `onAppear` 时正确加载 `currentUser.name`

4. **我的 - 签到间隔修改没反应**
   - 修复 `SettingsView` 中的签到间隔保存逻辑
   - 确保同步到 `UserManager` 和 `DataManager`

### ✅ 后端修复 (5-8)

5. **后端 - 签到倒计时精确到秒**
   - 修改 `getCheckInCountdown()` 函数
   - 显示格式：`X 小时 Y 分钟 Z 秒`

6. **后端 - 用户列表显示最后登录 IP**
   - 确保 `users.php` 查询包含 `last_login_ip`
   - 确保登录时记录 IP

7. **后端 - 用户列表显示签到次数**
   - 显示自动签到次数（从 `check_ins` 表统计）

8. **后端 - 用户列表显示胶囊和嘱托数量**
   - 确保查询包含 `capsules` 和 `wills` 数量

---

## 实施步骤

### 第一步：前端修复
1. 修改日期格式化
2. 修改编辑信息 UI
3. 修复签到间隔保存

### 第二步：后端修复
1. 修改 `users.php` 后端代码
2. 修改 `getCheckInCountdown()` 函数
3. 确保登录记录 IP

### 第三步：测试和推送
1. 编译测试
2. 推送到 GitHub

---

**开始时间**: 2026-03-17 15:35
