# 🕐 签到倒计时实现方案

**日期**：2026-03-26  
**需求**：前端本地倒计时，后端同步过期时间

---

## 📊 实现方案

### 核心思路
1. **前端**：使用本地时间简单倒计时（不需要同步服务器时间）
2. **后端**：保存签到过期时间戳，服务器端独立倒计时
3. **同步**：前端签到时上传当前时间戳，后端计算过期时间并保存

---

## 🔧 后端实现

### 1. 数据库字段

**文件**：`database.sql`

```sql
ALTER TABLE `users` 
ADD COLUMN `checkin_expire_at` INT DEFAULT NULL COMMENT '签到过期时间戳（秒级）' 
AFTER `checkin_count`;
```

**迁移脚本**：`add-checkin-expire-field.sql`

---

### 2. 签到 API

**文件**：`api/graphql.php` - `checkIn()` 函数

```php
function checkIn($vars, $userId, $db) {
    if (!$userId) throw new Exception('未授权');
    
    $now = date('Y-m-d H:i:s');
    $nowTimestamp = time();
    
    // 🕐 获取签到间隔（小时）
    $intervalHours = 48; // 默认 48 小时
    $stmt = $db->prepare("SELECT config_value FROM system_config WHERE config_key = 'checkin_interval_hours'");
    $stmt->execute();
    $config = $stmt->fetch();
    if ($config) {
        $intervalHours = (int)$config['config_value'];
    }
    
    // 🕐 计算倒计时结束时间戳
    $checkinTimestamp = $nowTimestamp;
    if (isset($vars['checkinTimestamp'])) {
        $checkinTimestamp = (int)$vars['checkinTimestamp'];
    }
    $expireTimestamp = $checkinTimestamp + ($intervalHours * 3600);
    
    // 更新用户签到时间 + 过期时间戳
    $stmt = $db->prepare('UPDATE users SET last_check_in_date = ?, checkin_count = checkin_count + 1, checkin_expire_at = ? WHERE id = ?');
    $stmt->execute([$now, $expireTimestamp, $userId]);
    
    // 记录签到日志
    $id = uniqid('checkin_');
    $stmt = $db->prepare('INSERT INTO checkin_logs (id, user_id, checkin_date, checkin_time, location) VALUES (?, ?, CURDATE(), ?, ?)');
    $stmt->execute([
        $id,
        $userId,
        $now,
        isset($vars['location']) ? json_encode($vars['location']) : null
    ]);
    
    error_log("✅ 签到成功：userId=$userId, expireTimestamp=$expireTimestamp");
    
    return ['success' => true, 'checkInTime' => $now, 'expireTimestamp' => $expireTimestamp];
}
```

**关键点**：
- 接收 `checkinTimestamp` 参数（前端上传的签到时间戳）
- 计算 `expireTimestamp = checkinTimestamp + (intervalHours * 3600)`
- 保存到数据库 `checkin_expire_at` 字段
- 返回 `expireTimestamp` 给前端

---

## 📱 前端实现

### 1. GraphQL Mutation

**文件**：`UserManager.swift` - `syncCheckInToServer()` 函数

```swift
// 🕐 当前时间戳（秒级）
let checkinTimestamp = Int(Date().timeIntervalSince1970)

// GraphQL Mutation
let mutation = """
mutation {
    checkIn(isAuto: \(isAuto ? "true" : "false"), checkinTimestamp: \(checkinTimestamp)) {
        success
        checkInTime
        expireTimestamp
    }
}
"""
```

**关键点**：
- 生成当前时间戳（秒级）
- 传递给后端 `checkinTimestamp` 参数
- 接收后端返回的 `expireTimestamp`

---

### 2. 本地倒计时

**文件**：`HomeStatusView.swift`

```swift
private func getCheckInStatus() -> (isSafe: Bool, hoursRemaining: Double) {
    let hours = dataManager.settings.checkInInterval.hours
    let offlineThreshold = dataManager.systemConfig.offlineTimeoutHours
    
    // 如果没有签到记录，返回完整的签到间隔时间
    guard let lastCheckIn = dataManager.lastCheckInDate else {
        return (true, Double(hours))
    }
    
    // 🕐 使用本地时间计算倒计时
    let elapsed = Date().timeIntervalSince(lastCheckIn) / 3600
    let remaining = Double(hours) - elapsed
    
    // 安全状态判断
    if remaining > 0 {
        return (true, max(0, remaining))  // 绿色
    } else if remaining > -offlineThreshold {
        return (true, max(0, remaining))  // 橙色
    } else {
        return (false, 0)  // 红色
    }
}
```

**关键点**：
- 使用本地时间简单计算
- 不需要服务器时间同步
- Timer 每秒递减 `secondsRemaining`

---

## 🔄 工作流程

```
┌─────────────────────────────────────────────────────────┐
│  1. 用户打开 App                                         │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  2. 前端自动签到                                         │
│     - 生成 checkinTimestamp = Date().timeIntervalSince1970 │
│     - 调用 GraphQL Mutation                              │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  3. 后端接收签到请求                                     │
│     - 计算 expireTimestamp = checkinTimestamp + 48 小时   │
│     - 保存到数据库 users.checkin_expire_at              │
│     - 返回 expireTimestamp 给前端                        │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  4. 前端更新本地倒计时                                    │
│     - lastCheckInDate = Date()                          │
│     - Timer 每秒递减 secondsRemaining                   │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│  5. 后端独立倒计时（服务器端）                            │
│     - 当前时间 > expireTimestamp → 超时                 │
│     - 可触发后端通知（不依赖前端）                       │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 数据流

### 签到流程

| 步骤 | 前端 | 后端 |
|------|------|------|
| 1 | 生成 `checkinTimestamp` | - |
| 2 | 调用 `checkIn(checkinTimestamp)` | 接收参数 |
| 3 | - | 计算 `expireTimestamp` |
| 4 | - | 保存到 `users.checkin_expire_at` |
| 5 | 接收 `expireTimestamp` | 返回结果 |
| 6 | 更新 `lastCheckInDate = Date()` | - |
| 7 | 本地 Timer 倒计时 | 服务器独立倒计时 |

---

## 🎯 优势

### ✅ 简单可靠
- 前端：本地时间计算，无需复杂同步
- 后端：独立倒计时，不依赖前端

### ✅ 双重保障
- 前端倒计时：提醒用户签到
- 后端倒计时：超时触发通知（即使前端关闭）

### ✅ 时间一致性
- 前端上传签到时间戳
- 后端基于此时间戳计算过期时间
- 避免时区和时间同步问题

---

## 🧪 测试场景

### 1. 正常签到
```
1. 打开 App
2. 自动签到
3. 后端日志：
   ✅ 签到成功：userId=xxx, expireTimestamp=1711613700
4. 前端显示：48:00:00 倒计时
```

### 2. 倒计时进行中
```
1. 签到后 1 小时
2. 前端显示：47:00:00
3. 后端查询：
   SELECT checkin_expire_at FROM users WHERE id = xxx
   → 1711613700
   → 剩余时间 = checkin_expire_at - time() = 47 小时
```

### 3. 超时通知
```
1. 等待 48 小时
2. 前端倒计时归零 → 发送 iMessage
3. 后端 checkin_expire_at < time() → 可触发后端通知
```

---

## 📝 提交记录

**前端**（zhonghuo-app）：
- `623b0cf` - 🕐 简化倒计时方案 - 前端本地倒计时 + 后端同步过期时间

**后端**（zhonghuo-backend-php）：
- `7662dda` - 🕐 添加签到过期时间戳 - 支持后端倒计时监控

---

## 🚀 部署

### 后端部署
```bash
# 1. 执行数据库迁移
ssh root@8.136.41.211
cd /www/wwwroot/zhonghuo.cn
mysql -u zhonghuo -pzhonghuo zhonghuo < add-checkin-expire-field.sql

# 2. 拉取最新代码
git pull origin main

# 3. 重启 PHP-FPM
systemctl restart php-fpm-81

# 4. 验证字段
mysql -u zhonghuo -pzhonghuo zhonghuo -e "DESCRIBE users;" | grep checkin_expire_at
```

### 前端部署
```bash
cd /Users/lishimin/Documents/zhonghuo-app
xcodebuild -scheme 终活 -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
```

---

## 📋 日志示例

### 前端日志
```
🔵 ====== 自动签到检查 ======
👤 当前用户：张三
🔄 打开 App 自动签到，重置倒计时（证明用户安全）...
🔵 开始 GraphQL 签到...
📡 GraphQL 签到响应状态码：200
📡 GraphQL 签到响应：{"data":{"checkIn":{"success":true,"checkInTime":"2026-03-26 17:00:00","expireTimestamp":1711613700}}}
✅ 自动签到成功！倒计时已重置为 48 小时
🔔 已安排推送提醒：剩余 48 小时开始提醒
🔄 updateStatus: secondsRemaining=172800.0, isSafe=true
```

### 后端日志
```
✅ 签到成功：userId=123, expireTimestamp=1711613700, expireTime=2026-03-28 17:00:00
```

---

## ✅ 完成状态

- [x] 数据库添加 `checkin_expire_at` 字段
- [x] 后端签到 API 计算并保存过期时间戳
- [x] 前端签到时上传当前时间戳
- [x] 前端本地倒计时（简单计算）
- [x] 后端独立倒计时（服务器端）
- [x] 迁移脚本
- [x] 部署验证

**实现完成！前端简单倒计时，后端同步监控。** 🎉
